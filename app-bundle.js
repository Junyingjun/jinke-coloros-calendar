/* 今刻 1.1 WebView bundle — generated by scripts/build-web-bundle.mjs */
/* data.jsx */
(() => {
var _excluded = ["completedDateKey", "completedAt", "completionKey", "progressBeforeCompletion"];
function _objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = _objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var s = Object.getOwnPropertySymbols(e); for (r = 0; r < s.length; r++) o = s[r], t.includes(o) || {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function _objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (e.includes(n)) continue; t[n] = r[n]; } return t; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
window.APP_NAME = "今刻";
window.APP_SLUG = "jinke";
var CALENDAR_SPECIAL_DAYS = {
  "2026-08-23": "处暑",
  "2026-09-07": "白露",
  "2026-09-23": "秋分"
};
var SEASONAL_PHASES = [{
  start: "2026-08-23",
  end: "2026-08-27",
  short: "处暑一候",
  full: "处暑一候 · 鹰乃祭鸟"
}, {
  start: "2026-08-28",
  end: "2026-09-01",
  short: "处暑二候",
  full: "处暑二候 · 天地始肃"
}, {
  start: "2026-09-02",
  end: "2026-09-06",
  short: "处暑三候",
  full: "处暑三候 · 禾乃登"
}];
var SOLAR_FESTIVALS = {
  "1-1": "元旦",
  "3-8": "妇女节",
  "5-1": "劳动节",
  "6-1": "儿童节",
  "10-1": "国庆节"
};
var LUNAR_FESTIVALS = {
  "正月-1": "春节",
  "正月-15": "元宵节",
  "五月-5": "端午节",
  "七月-7": "七夕",
  "七月-15": "中元节",
  "八月-15": "中秋节",
  "九月-9": "重阳节",
  "腊月-8": "腊八节"
};
function lunarDayLabel(day) {
  if (day <= 10) return ["", "初一", "初二", "初三", "初四", "初五", "初六", "初七", "初八", "初九", "初十"][day];
  if (day < 20) return "\u5341".concat(["", "一", "二", "三", "四", "五", "六", "七", "八", "九"][day - 10]);
  if (day === 20) return "二十";
  if (day < 30) return "\u5EFF".concat(["", "一", "二", "三", "四", "五", "六", "七", "八", "九"][day - 20]);
  return "三十";
}
window.getCalendarMarker = function getCalendarMarker(dateKey) {
  var date = new Date("".concat(dateKey, "T12:00:00"));
  var solarKey = "".concat(date.getMonth() + 1, "-").concat(date.getDate());
  var solarFestival = SOLAR_FESTIVALS[solarKey];
  var solarTerm = CALENDAR_SPECIAL_DAYS[dateKey];
  var seasonalPhase = SEASONAL_PHASES.find(function (item) {
    return dateKey >= item.start && dateKey <= item.end;
  });
  try {
    var _parts$find, _parts$find2;
    var parts = new Intl.DateTimeFormat("zh-CN-u-ca-chinese", {
      month: "long",
      day: "numeric"
    }).formatToParts(date);
    var month = (_parts$find = parts.find(function (part) {
      return part.type === "month";
    })) === null || _parts$find === void 0 ? void 0 : _parts$find.value;
    var day = Number((_parts$find2 = parts.find(function (part) {
      return part.type === "day";
    })) === null || _parts$find2 === void 0 ? void 0 : _parts$find2.value);
    if (!month || !day) throw new Error("Chinese calendar unavailable");
    var lunarFestival = LUNAR_FESTIVALS["".concat(month, "-").concat(day)];
    var lunarDate = "\u519C\u5386".concat(month).concat(lunarDayLabel(day));
    var headline = solarFestival || solarTerm || lunarFestival;
    return {
      short: headline || "".concat(lunarDate).concat(seasonalPhase ? " \xB7 ".concat(seasonalPhase.short) : ""),
      full: headline ? "".concat(headline, " \xB7 ").concat(lunarDate) : "".concat(lunarDate).concat(seasonalPhase ? " \xB7 ".concat(seasonalPhase.full) : ""),
      source: headline ? "中国节日与历法" : "中国农历与七十二候"
    };
  } catch (_unused) {
    return {
      short: solarFestival || solarTerm || "今日历法",
      full: solarFestival || solarTerm || "今日历法",
      source: "中国公历"
    };
  }
};
window.normalizeCriticalReminderPlan = function normalizeCriticalReminderPlan() {
  var task = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var defaultTime = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : window.JINKE_DDL_REMINDER_TIME || "10:00";
  var defaultMultiple = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : window.JINKE_DDL_REMINDER_MULTIPLE || 5;
  var defaultFinalDays = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : window.JINKE_DDL_REMINDER_FINAL_DAYS || 5;
  var source = task && _typeof(task) === "object" ? task : {};
  var reminderTime = /^(?:[01]\d|2[0-3]):[0-5]\d$/.test(source.reminderTime || "") ? source.reminderTime : defaultTime;
  var reminderMode = source.reminderMode === "daily" ? "final-days" : ["smart", "final-days", "deadline-only"].includes(source.reminderMode) ? source.reminderMode : "smart";
  var deadlineMinutes = /^(?:[01]\d|2[0-3]):[0-5]\d$/.test(source.deadlineTime || "") ? Number(source.deadlineTime.slice(0, 2)) * 60 + Number(source.deadlineTime.slice(3)) : null;
  var reminderMinutes = Number(reminderTime.slice(0, 2)) * 60 + Number(reminderTime.slice(3));
  var inferredLeadMinutes = deadlineMinutes === null || reminderMinutes > deadlineMinutes ? 0 : deadlineMinutes - reminderMinutes;
  var deadlineLeadMinutes = Math.min(1435, Math.max(0, source.deadlineLeadMinutes !== undefined && source.deadlineLeadMinutes !== null ? Math.round((Number(source.deadlineLeadMinutes) || 0) / 5) * 5 : inferredLeadMinutes));
  return {
    reminderEnabled: typeof source.reminderEnabled === "boolean" ? source.reminderEnabled : Boolean(source.deadline),
    reminderTime: reminderTime,
    reminderMode: reminderMode,
    reminderMultiple: Math.max(1, Number(source.reminderMultiple) || Number(defaultMultiple) || 5),
    reminderFinalDays: Math.max(0, source.reminderFinalDays !== undefined && source.reminderFinalDays !== null ? Number(source.reminderFinalDays) || 0 : Number(defaultFinalDays) || 0),
    deadlineLeadMinutes: deadlineLeadMinutes
  };
};
window.getCriticalReminderTriggerTime = function getCriticalReminderTriggerTime() {
  var taskOrPlan = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var plan = window.normalizeCriticalReminderPlan(taskOrPlan);
  if (plan.reminderMode !== "deadline-only" || !/^(?:[01]\d|2[0-3]):[0-5]\d$/.test(taskOrPlan.deadlineTime || "")) return plan.reminderTime;
  var deadlineMinutes = Number(taskOrPlan.deadlineTime.slice(0, 2)) * 60 + Number(taskOrPlan.deadlineTime.slice(3));
  var triggerMinutes = (deadlineMinutes - plan.deadlineLeadMinutes + 1440) % 1440;
  return "".concat(String(Math.floor(triggerMinutes / 60)).padStart(2, "0"), ":").concat(String(triggerMinutes % 60).padStart(2, "0"));
};
window.getCriticalReminderDayOffset = function getCriticalReminderDayOffset() {
  var taskOrPlan = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var plan = window.normalizeCriticalReminderPlan(taskOrPlan);
  if (plan.reminderMode !== "deadline-only" || !/^(?:[01]\d|2[0-3]):[0-5]\d$/.test(taskOrPlan.deadlineTime || "")) return 0;
  var deadlineMinutes = Number(taskOrPlan.deadlineTime.slice(0, 2)) * 60 + Number(taskOrPlan.deadlineTime.slice(3));
  return deadlineMinutes - plan.deadlineLeadMinutes < 0 ? 1 : 0;
};
window.shouldRemindCritical = function shouldRemindCritical(daysLeft, planOrMultiple, finalDays) {
  if (!Number.isFinite(daysLeft) || daysLeft < 0) return false;
  if (planOrMultiple && _typeof(planOrMultiple) === "object") {
    var plan = window.normalizeCriticalReminderPlan(planOrMultiple);
    if (!plan.reminderEnabled) return false;
    if (plan.reminderMode === "final-days") return daysLeft <= plan.reminderFinalDays;
    if (plan.reminderMode === "deadline-only") return daysLeft === window.getCriticalReminderDayOffset(planOrMultiple);
    return daysLeft <= plan.reminderFinalDays || daysLeft % plan.reminderMultiple === 0;
  }
  var safeMultiple = Math.max(1, Number(planOrMultiple) || window.JINKE_DDL_REMINDER_MULTIPLE || 5);
  var safeFinalDays = Math.max(0, finalDays !== undefined && finalDays !== null ? Number(finalDays) || 0 : window.JINKE_DDL_REMINDER_FINAL_DAYS || 0);
  return daysLeft <= safeFinalDays || daysLeft % safeMultiple === 0;
};
window.getCriticalReminder = function getCriticalReminder() {
  var taskOrPlan = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var defaultTime = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : window.JINKE_DDL_REMINDER_TIME || "10:00";
  var defaultMultiple = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : window.JINKE_DDL_REMINDER_MULTIPLE || 5;
  var defaultFinalDays = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : window.JINKE_DDL_REMINDER_FINAL_DAYS || 5;
  var plan = window.normalizeCriticalReminderPlan(taskOrPlan && _typeof(taskOrPlan) === "object" ? taskOrPlan : {}, defaultTime, defaultMultiple, defaultFinalDays);
  if (!plan.reminderEnabled) return "不提醒";
  if (plan.reminderMode === "final-days") return "\u4EC5\u6700\u540E ".concat(plan.reminderFinalDays, " \u5929 \xB7 ").concat(plan.reminderTime);
  if (plan.reminderMode === "deadline-only" && /^(?:[01]\d|2[0-3]):[0-5]\d$/.test(taskOrPlan.deadlineTime || "")) {
    var hours = Math.floor(plan.deadlineLeadMinutes / 60);
    var minutes = plan.deadlineLeadMinutes % 60;
    var lead = "".concat(hours ? "".concat(hours, " \u5C0F\u65F6") : "").concat(hours && minutes ? " " : "").concat(minutes || !hours ? "".concat(minutes, " \u5206\u949F") : "");
    return "\u622A\u6B62\u65F6\u523B\u524D ".concat(lead);
  }
  if (plan.reminderMode === "deadline-only") return "\u4EC5\u622A\u6B62\u65E5 ".concat(plan.reminderTime);
  return "\u6BCF ".concat(plan.reminderMultiple, " \u5929 \xB7 \u6700\u540E ").concat(plan.reminderFinalDays, " \u5929\u6BCF\u5929 \xB7 ").concat(plan.reminderTime);
};
var CALENDAR_WEEKDAYS = ["日", "一", "二", "三", "四", "五", "六"];
var REPEAT_WEEKDAYS = ["一", "二", "三", "四", "五", "六", "日"];
window.repeatDaysFromValue = function repeatDaysFromValue(value, explicitDays) {
  if (Array.isArray(explicitDays)) return _toConsumableArray(new Set(explicitDays.map(Number).filter(function (day) {
    return day >= 1 && day <= 7;
  }))).sort(function (a, b) {
    return a - b;
  });
  var text = String(value || "");
  if (text === "每天") return [1, 2, 3, 4, 5, 6, 7];
  if (text === "工作日") return [1, 2, 3, 4, 5];
  if (text === "周末" || text === "周六、日") return [6, 7];
  if (text === "仅一次") return [];
  var days = [];
  REPEAT_WEEKDAYS.forEach(function (label, index) {
    if (text.includes(label) && !days.includes(index + 1)) days.push(index + 1);
  });
  return days;
};
window.repeatLabelFromDays = function repeatLabelFromDays(days) {
  var normalized = window.repeatDaysFromValue("", days);
  var key = normalized.join(",");
  if (!key) return "仅一次";
  if (key === "1,2,3,4,5,6,7") return "每天";
  if (key === "1,2,3,4,5") return "工作日";
  if (key === "6,7") return "周末";
  return "\u5468".concat(normalized.map(function (day) {
    return REPEAT_WEEKDAYS[day - 1];
  }).join("、"));
};
window.taskOccursOnDate = function taskOccursOnDate(task, dateKey) {
  var _window$APP_DATA;
  var fallbackDateKey = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : (_window$APP_DATA = window.APP_DATA) === null || _window$APP_DATA === void 0 || (_window$APP_DATA = _window$APP_DATA.today) === null || _window$APP_DATA === void 0 ? void 0 : _window$APP_DATA.dateKey;
  if (task !== null && task !== void 0 && task.activeFrom && dateKey < task.activeFrom) return false;
  if (task !== null && task !== void 0 && task.activeUntil && dateKey > task.activeUntil) return false;
  var days = window.repeatDaysFromValue(task === null || task === void 0 ? void 0 : task.repeat, task === null || task === void 0 ? void 0 : task.repeatDays);
  if (!days.length) return ((task === null || task === void 0 ? void 0 : task.scheduledDateKey) || (task === null || task === void 0 ? void 0 : task.dateKey) || fallbackDateKey) === dateKey;
  var date = parseCalendarDateKey(dateKey);
  var weekday = date.getDay() === 0 ? 7 : date.getDay();
  return days.includes(weekday);
};
window.normalizeCriticalCompletion = function normalizeCriticalCompletion(task) {
  var _window$APP_DATA2;
  var fallbackDateKey = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : (_window$APP_DATA2 = window.APP_DATA) === null || _window$APP_DATA2 === void 0 || (_window$APP_DATA2 = _window$APP_DATA2.today) === null || _window$APP_DATA2 === void 0 ? void 0 : _window$APP_DATA2.dateKey;
  var done = Boolean((task === null || task === void 0 ? void 0 : task.done) || (task === null || task === void 0 ? void 0 : task.status) === "completed");
  if (!done) return _objectSpread(_objectSpread({}, task), {}, {
    done: false,
    status: (task === null || task === void 0 ? void 0 : task.status) === "completed" ? "active" : (task === null || task === void 0 ? void 0 : task.status) || "active"
  });
  return _objectSpread(_objectSpread({}, task), {}, {
    done: true,
    status: "completed",
    completedDateKey: task.completedDateKey || fallbackDateKey,
    progressBeforeCompletion: Number.isFinite(task.progressBeforeCompletion) ? task.progressBeforeCompletion : Number(task.progress) || 0,
    progress: 100
  });
};
window.completeCriticalForDate = function completeCriticalForDate(task, dateKey) {
  if (task !== null && task !== void 0 && task.done && task !== null && task !== void 0 && task.completedDateKey) return window.normalizeCriticalCompletion(task, dateKey);
  return window.normalizeCriticalCompletion(_objectSpread(_objectSpread({}, task), {}, {
    done: true,
    status: "completed",
    completedDateKey: dateKey,
    completedAt: new Date().toISOString(),
    progressBeforeCompletion: Number(task === null || task === void 0 ? void 0 : task.progress) || 0,
    progress: 100
  }), dateKey);
};
window.moveCriticalCompletion = function moveCriticalCompletion(task, dateKey, requestedTime) {
  if (!(task !== null && task !== void 0 && task.done) || !/^\d{4}-\d{2}-\d{2}$/.test(String(dateKey || ""))) return _objectSpread({}, task);
  var previous = task.completedAt ? new Date(task.completedAt) : new Date();
  var fallbackHour = Number.isNaN(previous.getTime()) ? new Date().getHours() : previous.getHours();
  var fallbackMinute = Number.isNaN(previous.getTime()) ? new Date().getMinutes() : previous.getMinutes();
  var timeMatch = String(requestedTime || "").match(/^(\d{1,2}):(\d{2})$/);
  var hour = timeMatch ? Math.min(23, Math.max(0, Number(timeMatch[1]))) : fallbackHour;
  var minute = timeMatch ? Math.min(59, Math.max(0, Number(timeMatch[2]))) : fallbackMinute;
  var _dateKey$split$map = dateKey.split("-").map(Number),
    _dateKey$split$map2 = _slicedToArray(_dateKey$split$map, 3),
    year = _dateKey$split$map2[0],
    month = _dateKey$split$map2[1],
    day = _dateKey$split$map2[2];
  return _objectSpread(_objectSpread({}, task), {}, {
    completedDateKey: dateKey,
    completedAt: new Date(year, month - 1, day, hour, minute, 0, 0).toISOString(),
    completionKey: "".concat(task.id, ":").concat(dateKey)
  });
};
window.uncompleteCriticalTask = function uncompleteCriticalTask(task) {
  var _ref = task || {},
    completedDateKey = _ref.completedDateKey,
    completedAt = _ref.completedAt,
    completionKey = _ref.completionKey,
    progressBeforeCompletion = _ref.progressBeforeCompletion,
    rest = _objectWithoutProperties(_ref, _excluded);
  return _objectSpread(_objectSpread({}, rest), {}, {
    done: false,
    status: "active",
    progress: Number.isFinite(progressBeforeCompletion) ? progressBeforeCompletion : Number(rest.progress) || 0
  });
};
window.criticalTaskVisibleOnTodayDate = function criticalTaskVisibleOnTodayDate(task, dateKey) {
  if (task !== null && task !== void 0 && task.done) return task.completedDateKey === dateKey;
  return Boolean(task === null || task === void 0 ? void 0 : task.deadline);
};
window.countScheduledTasksOnDate = function countScheduledTasksOnDate() {
  var _window$APP_DATA3;
  var dailyTasks = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var criticalTasks = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  var dateKey = arguments.length > 2 ? arguments[2] : undefined;
  var fallbackDateKey = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : (_window$APP_DATA3 = window.APP_DATA) === null || _window$APP_DATA3 === void 0 || (_window$APP_DATA3 = _window$APP_DATA3.today) === null || _window$APP_DATA3 === void 0 ? void 0 : _window$APP_DATA3.dateKey;
  var dailyLoad = dailyTasks.filter(function (task) {
    return window.taskOccursOnDate(task, dateKey, fallbackDateKey);
  }).length;
  var target = parseCalendarDateKey(dateKey);
  var deadlineLoad = criticalTasks.filter(function (task) {
    if (task !== null && task !== void 0 && task.done) return task.completedDateKey === dateKey;
    if (!(task !== null && task !== void 0 && task.deadline) || !Number.isFinite(task.daysLeft)) return false;
    var anchor = parseCalendarDateKey(task.anchorDateKey || fallbackDateKey);
    var elapsedDays = Math.round((target.getTime() - anchor.getTime()) / 86400000);
    return task.daysLeft - elapsedDays === 0;
  }).length;
  return dailyLoad + deadlineLoad;
};
function parseCalendarDateKey(dateKey) {
  var _dateKey$split$map3 = dateKey.split("-").map(Number),
    _dateKey$split$map4 = _slicedToArray(_dateKey$split$map3, 3),
    year = _dateKey$split$map4[0],
    month = _dateKey$split$map4[1],
    day = _dateKey$split$map4[2];
  return new Date(year, month - 1, day, 12);
}
function calendarDateKey(date) {
  return "".concat(date.getFullYear(), "-").concat(String(date.getMonth() + 1).padStart(2, "0"), "-").concat(String(date.getDate()).padStart(2, "0"));
}
window.getDateMeta = function getDateMeta(dateKey) {
  var date = parseCalendarDateKey(dateKey);
  return {
    dateKey: dateKey,
    year: date.getFullYear(),
    month: date.getMonth() + 1,
    date: date.getDate(),
    day: CALENDAR_WEEKDAYS[date.getDay()]
  };
};
window.getWeekDates = function getWeekDates(dateKey) {
  var selected = parseCalendarDateKey(dateKey);
  var mondayOffset = (selected.getDay() + 6) % 7;
  var monday = new Date(selected);
  monday.setDate(selected.getDate() - mondayOffset);
  return Array.from({
    length: 7
  }, function (_, index) {
    var date = new Date(monday);
    date.setDate(monday.getDate() + index);
    var key = calendarDateKey(date);
    return window.getDateMeta(key);
  });
};
window.getMonthDates = function getMonthDates(dateKey) {
  var selected = parseCalendarDateKey(dateKey);
  var year = selected.getFullYear();
  var monthIndex = selected.getMonth();
  var first = new Date(year, monthIndex, 1, 12);
  var mondayOffset = (first.getDay() + 6) % 7;
  var gridStart = new Date(first);
  gridStart.setDate(first.getDate() - mondayOffset);
  return Array.from({
    length: 42
  }, function (_, index) {
    var date = new Date(gridStart);
    date.setDate(gridStart.getDate() + index);
    var key = calendarDateKey(date);
    return _objectSpread(_objectSpread({}, window.getDateMeta(key)), {}, {
      muted: date.getMonth() !== monthIndex
    });
  });
};
window.shiftDateKeyByMonth = function shiftDateKeyByMonth(dateKey, offset) {
  var selected = parseCalendarDateKey(dateKey);
  var targetMonth = new Date(selected.getFullYear(), selected.getMonth() + offset, 1, 12);
  var lastDate = new Date(targetMonth.getFullYear(), targetMonth.getMonth() + 1, 0, 12).getDate();
  targetMonth.setDate(Math.min(selected.getDate(), lastDate));
  return calendarDateKey(targetMonth);
};
window.shiftDateKeyByDays = function shiftDateKeyByDays(dateKey, offset) {
  var selected = parseCalendarDateKey(dateKey);
  selected.setDate(selected.getDate() + Number(offset || 0));
  return calendarDateKey(selected);
};
var STARTUP_DATE = new Date();
var STARTUP_DATE_KEY = calendarDateKey(STARTUP_DATE);
var STARTUP_DATE_META = window.getDateMeta(STARTUP_DATE_KEY);
var STARTUP_WEEK = window.getWeekDates(STARTUP_DATE_KEY);
window.APP_DATA = {
  today: {
    date: "".concat(STARTUP_DATE_META.month, "\u6708").concat(STARTUP_DATE_META.date, "\u65E5"),
    dateKey: STARTUP_DATE_KEY,
    weekday: "\u661F\u671F".concat(STARTUP_DATE_META.day),
    solar: window.getCalendarMarker(STARTUP_DATE_KEY).short
  },
  week: STARTUP_WEEK.map(function (item) {
    return {
      day: item.day,
      date: item.date,
      dateKey: item.dateKey,
      today: item.dateKey === STARTUP_DATE_KEY,
      active: item.dateKey === STARTUP_DATE_KEY
    };
  }),
  dailyTasks: [{
    id: "demo-daily",
    demo: true,
    activeFrom: STARTUP_DATE_KEY,
    time: "09:00",
    title: "演示：每天喝水",
    note: "向左滑动可编辑或删除",
    repeat: "每天",
    repeatDays: [1, 2, 3, 4, 5, 6, 7],
    reminder: "到点提醒",
    alertMode: "inherit",
    soundId: "inherit",
    done: false
  }],
  criticalTasks: [{
    id: "demo-ddl",
    demo: true,
    title: "演示：完成第一个 DDL",
    note: "向左滑动可编辑或删除",
    deadline: "7天后",
    daysLeft: 7,
    deadlineTime: null,
    reminderEnabled: true,
    reminderTime: "10:00",
    reminderMode: "smart",
    reminderMultiple: 5,
    reminderFinalDays: 5,
    reminder: "每 5 天 · 最后 5 天每天 · 10:00",
    alertMode: "inherit",
    soundId: "inherit",
    progress: 0
  }],
  history: [],
  monthRanking: [],
  yearRanking: [],
  ddlRanking: []
};
})();

/* voice-domain-model.js */
(() => {
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
(function () {
  var CN_DIGITS = {
    "零": 0,
    "〇": 0,
    "一": 1,
    "二": 2,
    "两": 2,
    "三": 3,
    "四": 4,
    "五": 5,
    "六": 6,
    "七": 7,
    "八": 8,
    "九": 9
  };
  var NUMBER_SOURCE = "[零〇一二三四五六七八九十两\\d]{1,3}";
  var PERIOD_SOURCE = "凌晨|清晨|早晨|早上|上午|中午|下午|傍晚|晚上|夜里|夜间";
  var PERIOD_CANONICAL = {
    清晨: "凌晨",
    早晨: "早上",
    夜里: "晚上",
    夜间: "晚上"
  };
  var NIGHT_ACTIONS = /洗漱|刷牙|睡觉|睡眠|上床|休息|晚安/;
  var MORNING_ACTIONS = /起床|早餐|晨练|早饭/;
  var NOON_ACTIONS = /午饭|午餐|午休/;
  var CLAUSE_BOUNDARY = /[，,。；;！？!?、\n]/;
  function normalizeTranscript(value) {
    var normalized = String(value || "").trim();
    normalized = normalized.replace(/\bd\s*[，,、.\s]*d\s*[，,、.\s]*l\b/ig, "ddl").replace(/(?:滴|迪|低|弟|地|的)[，,、.\s]*(?:滴|迪|低|弟|地|的)[，,、.\s]*(?:艾|爱|挨)(?:尔|耳|儿|乐|了|勒)?/g, "ddl").replace(/(?:戴德莱恩|代德莱恩|带的来因|带的赖因|戴的来因|代的来因|得来因)/g, "deadline").replace(/[﹕：]/g, ":").replace(/[；;]/g, "，").replace(/\s+/g, " ");
    var previous = "";
    while (normalized !== previous) {
      previous = normalized;
      normalized = normalized.replace(/([\u3400-\u9fff\d])\s+(?=[\u3400-\u9fff\d])/g, "$1");
    }
    var number = "([零〇一二三四五六七八九十两\\d]{1,3})";
    var timeBoundary = "(?=$|[，,。！？!?、]|凌晨|清晨|早晨|早上|上午|中午|下午|傍晚|晚上|夜里|夜间|洗漱|刷牙|睡觉|睡眠|休息|起床|出发|回来|回家|看书|学习|健身|吃饭|开会|上课|下班)";
    normalized = normalized.replace(new RegExp("".concat(number, "\\s*[\u5E97\u7535]\\s*"), "g"), "$1点").replace(new RegExp("".concat(number, "\u70B9\\s*[\u529E\u4F34\u73ED\u7248\u822C]").concat(timeBoundary), "g"), "$1点半");
    return normalized.trim();
  }
  function parseNumber(value) {
    if (!value) return null;
    if (/^\d+$/.test(value)) return Number(value);
    if (value === "十") return 10;
    if (value.includes("十")) {
      var _value$split = value.split("十"),
        _value$split2 = _slicedToArray(_value$split, 2),
        tens = _value$split2[0],
        ones = _value$split2[1];
      return (tens ? CN_DIGITS[tens] : 1) * 10 + (ones ? CN_DIGITS[ones] : 0);
    }
    return value.split("").reduce(function (total, char) {
      var _CN_DIGITS$char;
      return total * 10 + ((_CN_DIGITS$char = CN_DIGITS[char]) !== null && _CN_DIGITS$char !== void 0 ? _CN_DIGITS$char : 0);
    }, 0);
  }
  function clauseBounds(text, index) {
    var start = 0;
    var end = text.length;
    for (var cursor = index - 1; cursor >= 0; cursor -= 1) {
      if (CLAUSE_BOUNDARY.test(text[cursor])) {
        start = cursor + 1;
        break;
      }
    }
    for (var _cursor = index; _cursor < text.length; _cursor += 1) {
      if (CLAUSE_BOUNDARY.test(text[_cursor])) {
        end = _cursor;
        break;
      }
    }
    return {
      start: start,
      end: end,
      text: text.slice(start, end)
    };
  }
  function closestClausePeriod(text, mentionStart, mentionEnd) {
    var bounds = clauseBounds(text, mentionStart);
    var clause = bounds.text;
    var matches = _toConsumableArray(clause.matchAll(new RegExp(PERIOD_SOURCE, "g"))).map(function (match) {
      return {
        value: match[0],
        start: bounds.start + match.index,
        end: bounds.start + match.index + match[0].length
      };
    }).filter(function (item) {
      return item.end <= mentionStart || item.start >= mentionEnd;
    }).map(function (item) {
      return _objectSpread(_objectSpread({}, item), {}, {
        distance: item.end <= mentionStart ? mentionStart - item.end : item.start - mentionEnd
      });
    }).sort(function (a, b) {
      return a.distance - b.distance;
    });
    return matches[0] || null;
  }
  function inferPeriodFromClause(clause, hour) {
    if (NIGHT_ACTIONS.test(clause) && (hour === 12 || hour >= 1 && hour <= 11)) return "晚上";
    if (MORNING_ACTIONS.test(clause) && hour >= 1 && hour <= 11) return "早上";
    if (NOON_ACTIONS.test(clause) && hour >= 1 && hour <= 12) return "中午";
    return "";
  }
  function normalizeClock(hourValue, minuteValue, periodValue) {
    var hour = Number(hourValue);
    var minute = Number(minuteValue);
    var period = PERIOD_CANONICAL[periodValue] || periodValue || "";
    if (["下午", "傍晚"].includes(period) && hour < 12) hour += 12;
    if (period === "晚上") hour = hour === 0 || hour === 12 ? 24 : hour < 12 ? hour + 12 : hour;
    if (period === "中午" && hour < 11) hour += 12;
    if (period === "凌晨" && hour === 12) hour = 0;
    hour = Math.min(24, Math.max(0, hour));
    minute = Math.min(55, Math.max(0, Math.round(minute / 5) * 5));
    if (hour === 24) minute = 0;
    return {
      hour: hour,
      minute: minute,
      value: "".concat(String(hour).padStart(2, "0"), ":").concat(String(minute).padStart(2, "0")),
      period: period,
      dayBoundary: hour === 24 ? "end" : "start"
    };
  }
  function buildTimeMention(text, match, kind) {
    var beforePeriod = match[1] || "";
    var hour = parseNumber(match[2]);
    var rawMinute = kind === "colon" ? Number(match[3]) : match[3] === "半" ? 30 : parseNumber(String(match[3] || "").replace("分", "")) || 0;
    var afterPeriod = match[4] || "";
    var start = match.index;
    var end = start + match[0].length;
    var qualifier = beforePeriod || afterPeriod;
    var qualifierSource = "";
    var qualifierBinding = qualifier ? beforePeriod ? "before" : "after" : "";
    if (!qualifier) {
      var nearby = closestClausePeriod(text, start, end);
      if (nearby) {
        qualifier = nearby.value;
        qualifierSource = nearby.value;
        qualifierBinding = "clause";
      }
    }
    var clause = clauseBounds(text, start).text;
    if (!qualifier) {
      qualifier = inferPeriodFromClause(clause, hour);
      if (qualifier) qualifierBinding = "semantic";
    }
    var clock = normalizeClock(hour, rawMinute, qualifier);
    var source = match[0].trim();
    var sources = _toConsumableArray(new Set([source, qualifierSource].filter(Boolean)));
    return _objectSpread(_objectSpread({}, clock), {}, {
      source: source,
      sources: sources,
      start: start,
      end: end,
      qualifierBinding: qualifierBinding,
      confidence: qualifierBinding === "before" || qualifierBinding === "after" ? 0.99 : qualifierBinding === "clause" ? 0.92 : qualifierBinding === "semantic" ? 0.78 : 0.72
    });
  }
  function extractTimeMentions(rawText) {
    var text = normalizeTranscript(rawText);
    var mentions = [];
    var occupied = [];
    var colonPattern = new RegExp("(".concat(PERIOD_SOURCE, ")?\\s*(").concat(NUMBER_SOURCE, ")\\s*:\\s*(\\d{1,2})\\s*(").concat(PERIOD_SOURCE, ")?"), "g");
    var spokenPattern = new RegExp("(".concat(PERIOD_SOURCE, ")?\\s*(").concat(NUMBER_SOURCE, ")\\s*[\u70B9\u65F6\u7535]\\s*(\u534A|").concat(NUMBER_SOURCE, "\u5206?)?\\s*(").concat(PERIOD_SOURCE, ")?"), "g");
    var _iterator = _createForOfIteratorHelper(text.matchAll(colonPattern)),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var match = _step.value;
        var mention = buildTimeMention(text, match, "colon");
        mentions.push(mention);
        occupied.push([mention.start, mention.end]);
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    var _iterator2 = _createForOfIteratorHelper(text.matchAll(spokenPattern)),
      _step2;
    try {
      var _loop = function _loop() {
        var match = _step2.value;
        var start = match.index;
        var end = start + match[0].length;
        if (occupied.some(function (_ref) {
          var _ref2 = _slicedToArray(_ref, 2),
            from = _ref2[0],
            to = _ref2[1];
          return start < to && end > from;
        })) return 1;
        mentions.push(buildTimeMention(text, match, "spoken"));
      };
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        if (_loop()) continue;
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
    return mentions.sort(function (a, b) {
      return a.start - b.start;
    });
  }
  function addScore(scores, intent, amount, evidence) {
    var current = scores.get(intent) || {
      intent: intent,
      score: 0,
      evidence: []
    };
    current.score += amount;
    if (evidence) current.evidence.push(evidence);
    scores.set(intent, current);
  }
  function classifyIntent(rawText) {
    var text = normalizeTranscript(rawText);
    var scores = new Map();
    addScore(scores, "create", 0.5, "domain-default");
    var rules = [["clear-all", /(清空|清除|删除|移除|删掉).*(全部|所有)|(全部|所有).*(清空|清除|删除|移除|删掉)/, 12], ["delete", /(删除|移除|删掉|清除)/, 8], ["uncomplete", /(取消勾选|取消完成|标记为未完成|恢复未完成)/, 12], ["complete", /(打勾|勾选|标记完成|做完|完成了|^完成)/, 8], ["extend", /(延期|延长|再续期|续期\s*[零〇一二三四五六七八九十两\d]+\s*天)/, 10], ["edit", /(修改|更改|改成|改为|改名|更名|改到|改在|调到|调整|挪到|提前到|延后到|设置)/, 8], ["query", /(有什么|有哪些|列出|告诉我|汇总|查询|多少|还剩什么|还有什么|需要做什么|该做什么)/, 9], ["navigate", /(打开|进入|查看|切换|回到)/, 5], ["create", /(创建|添加|新增|记下|记一下|提醒我|安排一个|安排一条)/, 10]];
    rules.forEach(function (_ref3) {
      var _ref4 = _slicedToArray(_ref3, 3),
        intent = _ref4[0],
        pattern = _ref4[1],
        score = _ref4[2];
      var match = text.match(pattern);
      if (match) addScore(scores, intent, score, match[0]);
    });
    if (/(每天|每日|工作日|周末|每周|星期|\d{1,2}:\d{2}|[零〇一二三四五六七八九十两\d]+[点时电])/.test(text)) addScore(scores, "create", 2, "task-slots");
    if (/(任务|事项|日程|安排)/.test(text)) {
      for (var _i = 0, _arr = ["create", "delete", "edit", "complete", "query", "clear-all"]; _i < _arr.length; _i++) {
        var intent = _arr[_i];
        if (scores.has(intent)) addScore(scores, intent, 1, "domain-object");
      }
    }
    var ranked = _toConsumableArray(scores.values()).sort(function (a, b) {
      return b.score - a.score;
    });
    var top = ranked[0] || {
      intent: "clarify",
      score: 0,
      evidence: []
    };
    var second = ranked[1] || {
      score: 0
    };
    var margin = top.score - second.score;
    var confidence = Math.max(0.35, Math.min(0.99, 0.55 + top.score / 30 + margin / 24));
    return {
      intent: top.intent,
      confidence: confidence,
      evidence: top.evidence,
      ranked: ranked
    };
  }
  function normalizeComparable(value) {
    return normalizeTranscript(value).toLowerCase().replace(/(?:任务|事项|日程|安排|ddl|deadline)/ig, "").replace(/[\s\d０-９·。、，,：:（）()\-]/g, "");
  }
  function levenshtein(left, right) {
    if (!left) return right.length;
    if (!right) return left.length;
    var row = Array.from({
      length: right.length + 1
    }, function (_, index) {
      return index;
    });
    for (var i = 1; i <= left.length; i += 1) {
      var diagonal = row[0];
      row[0] = i;
      for (var j = 1; j <= right.length; j += 1) {
        var previous = row[j];
        row[j] = Math.min(row[j] + 1, row[j - 1] + 1, diagonal + (left[i - 1] === right[j - 1] ? 0 : 1));
        diagonal = previous;
      }
    }
    return row[right.length];
  }
  function similarity(leftValue, rightValue) {
    var left = normalizeComparable(leftValue);
    var right = normalizeComparable(rightValue);
    if (!left || !right) return 0;
    if (left.includes(right) || right.includes(left)) return 1;
    var distance = levenshtein(left, right);
    return Math.max(0, 1 - distance / Math.max(left.length, right.length));
  }
  function analyze(rawText) {
    var normalized = normalizeTranscript(rawText);
    return {
      raw: String(rawText || ""),
      normalized: normalized,
      intent: classifyIntent(normalized),
      times: extractTimeMentions(normalized),
      domain: "jinke-app-functions",
      modelVersion: 1
    };
  }
  function scoreCandidate(candidate) {
    var context = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    var text = normalizeTranscript((candidate === null || candidate === void 0 ? void 0 : candidate.text) || "");
    if (!text) return -Infinity;
    var analysis = analyze(text);
    var acoustic = Math.max(0, Math.min(1, Number(candidate === null || candidate === void 0 ? void 0 : candidate.confidence) || 0));
    var knownTasks = [].concat(_toConsumableArray(Array.isArray(context.dailyTasks) ? context.dailyTasks : []), _toConsumableArray(Array.isArray(context.criticalTasks) ? context.criticalTasks : []));
    var targetSimilarity = knownTasks.reduce(function (best, task) {
      return Math.max(best, similarity(text, (task === null || task === void 0 ? void 0 : task.title) || ""));
    }, 0);
    var hasOperation = /(创建|添加|新增|删除|移除|清除|完成|勾选|修改|更改|改到|延期|续期|查询|有什么|打开|切换)/.test(text);
    var hasTaskSlot = analysis.times.length > 0 || /(每天|工作日|周末|星期|周[一二三四五六日天]|ddl|deadline|截止|无期限|无ddl)/i.test(text);
    var hasExplicitTaskType = /(?:有|无)?(?:ddl|deadline)|关键(?:任务|事项)|日常(?:任务|事项)/i.test(text);
    var domainFit = Math.min(1, (analysis.intent.confidence || 0) * 0.55 + (hasOperation ? 0.2 : 0) + (hasTaskSlot ? 0.15 : 0) + targetSimilarity * 0.25);
    return acoustic * 0.68 + domainFit * 0.27 + (hasExplicitTaskType ? 0.05 : 0);
  }
  function rankCandidates(rawCandidates) {
    var context = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    var candidates = (Array.isArray(rawCandidates) ? rawCandidates : []).map(function (candidate, index) {
      return _objectSpread(_objectSpread({}, candidate), {}, {
        text: normalizeTranscript((candidate === null || candidate === void 0 ? void 0 : candidate.text) || ""),
        index: index
      });
    }).filter(function (candidate) {
      return candidate.text;
    }).map(function (candidate) {
      return _objectSpread(_objectSpread({}, candidate), {}, {
        score: scoreCandidate(candidate, context)
      });
    }).sort(function (left, right) {
      return right.score - left.score || left.index - right.index;
    });
    return {
      best: candidates[0] || null,
      candidates: candidates
    };
  }
  window.JINKE_DOMAIN_NLU = Object.freeze({
    normalizeTranscript: normalizeTranscript,
    parseNumber: parseNumber,
    extractTimeMentions: extractTimeMentions,
    classifyIntent: classifyIntent,
    similarity: similarity,
    analyze: analyze,
    scoreCandidate: scoreCandidate,
    rankCandidates: rankCandidates
  });
})();
})();

/* primitives.jsx */
(() => {
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function Icon(_ref) {
  var name = _ref.name,
    _ref$size = _ref.size,
    size = _ref$size === void 0 ? 20 : _ref$size,
    _ref$strokeWidth = _ref.strokeWidth,
    strokeWidth = _ref$strokeWidth === void 0 ? 1.8 : _ref$strokeWidth;
  var common = {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: strokeWidth,
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": true
  };
  var paths = {
    menu: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M5 7h14M5 12h14M5 17h14"
    })),
    chevronDown: React.createElement("path", {
      d: "m7 9.5 5 5 5-5"
    }),
    chevronRight: React.createElement("path", {
      d: "m9 6 6 6-6 6"
    }),
    back: React.createElement("path", {
      d: "m15 18-6-6 6-6"
    }),
    calendar: React.createElement(React.Fragment, null, React.createElement("rect", {
      x: "3",
      y: "5",
      width: "18",
      height: "16",
      rx: "3"
    }), React.createElement("path", {
      d: "M8 3v4M16 3v4M3 10h18"
    })),
    flag: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M5 21V4"
    }), React.createElement("path", {
      d: "M5 5h10l-1 4 3 3H5"
    })),
    mic: React.createElement(React.Fragment, null, React.createElement("rect", {
      x: "9",
      y: "3",
      width: "6",
      height: "12",
      rx: "3"
    }), React.createElement("path", {
      d: "M5 11a7 7 0 0 0 14 0M12 18v3M9 21h6"
    })),
    check: React.createElement("path", {
      d: "m6.5 12.5 3.4 3.3 7.6-8"
    }),
    history: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M3 12a9 9 0 1 0 3-6.7L3 8"
    }), React.createElement("path", {
      d: "M3 3v5h5M12 7v5l3 2"
    })),
    chart: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M4 20V10M10 20V4M16 20v-7M22 20H2"
    })),
    year: React.createElement(React.Fragment, null, React.createElement("rect", {
      x: "3",
      y: "4",
      width: "18",
      height: "17",
      rx: "3"
    }), React.createElement("path", {
      d: "M8 2v4M16 2v4M3 9h18M8 13h3v3H8z"
    })),
    bell: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"
    }), React.createElement("path", {
      d: "M10 21h4"
    })),
    spark: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "m12 3 1.3 4.2L17.5 8.5l-4.2 1.3L12 14l-1.3-4.2-4.2-1.3 4.2-1.3L12 3Z"
    }), React.createElement("path", {
      d: "m18.5 15 .7 2.3 2.3.7-2.3.7-.7 2.3-.7-2.3-2.3-.7 2.3-.7.7-2.3Z"
    })),
    settings: React.createElement(React.Fragment, null, React.createElement("circle", {
      cx: "12",
      cy: "12",
      r: "3"
    }), React.createElement("path", {
      d: "M19.4 15a1.7 1.7 0 0 0 .34 1.88l.06.06-2.83 2.83-.06-.06a1.7 1.7 0 0 0-1.88-.34 1.7 1.7 0 0 0-1.03 1.56V21h-4v-.08A1.7 1.7 0 0 0 9 19.36a1.7 1.7 0 0 0-1.88.34l-.06.06-2.83-2.83.06-.06A1.7 1.7 0 0 0 4.63 15a1.7 1.7 0 0 0-1.55-1H3v-4h.08A1.7 1.7 0 0 0 4.64 9a1.7 1.7 0 0 0-.34-1.88l-.06-.06 2.83-2.83.06.06A1.7 1.7 0 0 0 9 4.63 1.7 1.7 0 0 0 10 3.08V3h4v.08A1.7 1.7 0 0 0 15 4.64a1.7 1.7 0 0 0 1.88-.34l.06-.06 2.83 2.83-.06.06A1.7 1.7 0 0 0 19.37 9a1.7 1.7 0 0 0 1.55 1H21v4h-.08A1.7 1.7 0 0 0 19.4 15Z"
    })),
    keyboard: React.createElement(React.Fragment, null, React.createElement("rect", {
      x: "3",
      y: "6",
      width: "18",
      height: "12",
      rx: "2"
    }), React.createElement("path", {
      d: "M7 10h.01M11 10h.01M15 10h.01M18 10h.01M6 14h12"
    })),
    close: React.createElement("path", {
      d: "m6 6 12 12M18 6 6 18"
    }),
    clock: React.createElement(React.Fragment, null, React.createElement("circle", {
      cx: "12",
      cy: "12",
      r: "9"
    }), React.createElement("path", {
      d: "M12 7v5l3 2"
    })),
    repeat: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "m17 2 4 4-4 4"
    }), React.createElement("path", {
      d: "M3 11V9a3 3 0 0 1 3-3h15M7 22l-4-4 4-4"
    }), React.createElement("path", {
      d: "M21 13v2a3 3 0 0 1-3 3H3"
    })),
    archive: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M4 7h16v13H4zM3 4h18v3H3z"
    }), React.createElement("path", {
      d: "M9 11h6"
    })),
    refresh: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M20 7v5h-5"
    }), React.createElement("path", {
      d: "M4 17v-5h5"
    }), React.createElement("path", {
      d: "M6.1 8.3A7 7 0 0 1 18.7 7L20 12M4 12l1.3 5A7 7 0 0 0 18 15.7"
    })),
    shield: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M12 3 4.5 6v5.5c0 4.5 3 7.6 7.5 9.5 4.5-1.9 7.5-5 7.5-9.5V6L12 3Z"
    }), React.createElement("path", {
      d: "m9 12 2 2 4-5"
    })),
    update: React.createElement(React.Fragment, null, React.createElement("path", {
      d: "M12 3a9 9 0 1 0 8.2 5.3"
    }), React.createElement("path", {
      d: "M20 3v5h-5"
    }), React.createElement("path", {
      d: "M12 7v6"
    }), React.createElement("path", {
      d: "m9.5 10.5 2.5 2.5 2.5-2.5"
    }), React.createElement("path", {
      d: "M9 17h6"
    }))
  };
  return React.createElement("svg", common, paths[name] || paths.spark);
}
function PhoneFrame(_ref2) {
  var children = _ref2.children,
    _ref2$variant = _ref2.variant,
    variant = _ref2$variant === void 0 ? "phone" : _ref2$variant;
  return React.createElement("div", {
    className: "phone-shell phone-shell-".concat(variant),
    "data-device": variant
  }, React.createElement("div", {
    className: "phone-content"
  }, children));
}
function IconButton(_ref3) {
  var name = _ref3.name,
    label = _ref3.label,
    onClick = _ref3.onClick;
  return React.createElement("button", {
    className: "icon-button",
    "aria-label": label,
    onClick: onClick
  }, React.createElement(Icon, {
    name: name
  }));
}
function SectionHeader(_ref4) {
  var title = _ref4.title,
    note = _ref4.note,
    noteTone = _ref4.noteTone;
  return React.createElement("div", {
    className: "section-head"
  }, React.createElement("h2", {
    className: "section-title"
  }, title), note ? React.createElement("span", {
    className: "section-note ".concat(noteTone ? "is-".concat(noteTone) : "")
  }, note) : null);
}
function displayDeadline(deadline) {
  var match = String(deadline || "").match(/^(\d{4})-(\d{2})-(\d{2})$/);
  return match ? "".concat(Number(match[2]), "\u6708").concat(Number(match[3]), "\u65E5") : deadline;
}
function SwipeTaskActions(_ref5) {
  var label = _ref5.label,
    onEdit = _ref5.onEdit,
    onDelete = _ref5.onDelete,
    children = _ref5.children;
  var revealRatio = 0.4;
  var _React$useState = React.useState(0),
    _React$useState2 = _slicedToArray(_React$useState, 2),
    offsetRatio = _React$useState2[0],
    setOffsetRatio = _React$useState2[1];
  var gestureRef = React.useRef(null);
  var offsetRef = React.useRef(0);
  var suppressClickRef = React.useRef(false);
  var rootRef = React.useRef(null);
  var instanceIdRef = React.useRef("swipe-".concat(Math.random().toString(36).slice(2)));
  var settle = function settle(next) {
    offsetRef.current = next;
    setOffsetRatio(next);
  };
  React.useEffect(function () {
    var closeFromOutside = function closeFromOutside(event) {
      if (offsetRef.current < 0 && rootRef.current && !rootRef.current.contains(event.target)) settle(0);
    };
    var closeAnother = function closeAnother(event) {
      if (event.detail !== instanceIdRef.current && offsetRef.current < 0) settle(0);
    };
    var closeOnScroll = function closeOnScroll() {
      if (offsetRef.current < 0) settle(0);
    };
    document.addEventListener("pointerdown", closeFromOutside, true);
    document.addEventListener("scroll", closeOnScroll, true);
    window.addEventListener("jinke-swipe-open", closeAnother);
    return function () {
      document.removeEventListener("pointerdown", closeFromOutside, true);
      document.removeEventListener("scroll", closeOnScroll, true);
      window.removeEventListener("jinke-swipe-open", closeAnother);
    };
  }, []);
  var onPointerDown = function onPointerDown(event) {
    if (event.button !== undefined && event.button !== 0) return;
    gestureRef.current = {
      pointerId: event.pointerId,
      startX: event.clientX,
      startY: event.clientY,
      startOffset: offsetRef.current,
      horizontal: false,
      moved: false
    };
  };
  var onPointerMove = function onPointerMove(event) {
    var gesture = gestureRef.current;
    if (!gesture || gesture.pointerId !== event.pointerId) return;
    var dx = event.clientX - gesture.startX;
    var dy = event.clientY - gesture.startY;
    if (!gesture.horizontal && Math.max(Math.abs(dx), Math.abs(dy)) > 14) {
      var _event$currentTarget$, _event$currentTarget;
      if (Math.abs(dx) <= Math.abs(dy) * 1.2) return;
      gesture.horizontal = true;
      (_event$currentTarget$ = (_event$currentTarget = event.currentTarget).setPointerCapture) === null || _event$currentTarget$ === void 0 || _event$currentTarget$.call(_event$currentTarget, event.pointerId);
    }
    if (!gesture.horizontal) return;
    gesture.moved = true;
    var width = Math.max(1, event.currentTarget.getBoundingClientRect().width);
    settle(Math.max(-revealRatio, Math.min(0, gesture.startOffset + dx / width)));
  };
  var onPointerEnd = function onPointerEnd(event) {
    var _event$currentTarget$2, _event$currentTarget2, _event$currentTarget$3, _event$currentTarget3;
    var gesture = gestureRef.current;
    if (!gesture || gesture.pointerId !== event.pointerId) return;
    if (gesture.moved) {
      suppressClickRef.current = true;
      window.setTimeout(function () {
        suppressClickRef.current = false;
      }, 0);
      var next = offsetRef.current <= -0.13 ? -revealRatio : 0;
      settle(next);
      if (next < 0) window.dispatchEvent(new CustomEvent("jinke-swipe-open", {
        detail: instanceIdRef.current
      }));
    }
    gestureRef.current = null;
    if ((_event$currentTarget$2 = (_event$currentTarget2 = event.currentTarget).hasPointerCapture) !== null && _event$currentTarget$2 !== void 0 && _event$currentTarget$2.call(_event$currentTarget2, event.pointerId)) (_event$currentTarget$3 = (_event$currentTarget3 = event.currentTarget).releasePointerCapture) === null || _event$currentTarget$3 === void 0 || _event$currentTarget$3.call(_event$currentTarget3, event.pointerId);
  };
  var onClickCapture = function onClickCapture(event) {
    var _event$target$closest, _event$target;
    if ((_event$target$closest = (_event$target = event.target).closest) !== null && _event$target$closest !== void 0 && _event$target$closest.call(_event$target, ".swipe-action")) return;
    if (suppressClickRef.current || offsetRef.current < 0) {
      event.preventDefault();
      event.stopPropagation();
      settle(0);
    }
  };
  return React.createElement("div", {
    ref: rootRef,
    className: "swipe-task ".concat(offsetRatio < 0 ? "is-open" : ""),
    "aria-label": label,
    onPointerDown: onPointerDown,
    onPointerMove: onPointerMove,
    onPointerUp: onPointerEnd,
    onPointerCancel: onPointerEnd,
    onClickCapture: onClickCapture
  }, React.createElement("div", {
    className: "swipe-actions",
    "aria-hidden": offsetRatio === 0
  }, React.createElement("button", {
    className: "swipe-action swipe-edit",
    type: "button",
    onClick: function onClick() {
      settle(0);
      onEdit();
    }
  }, "\u7F16\u8F91"), React.createElement("button", {
    className: "swipe-action swipe-delete",
    type: "button",
    onClick: function onClick() {
      settle(0);
      onDelete();
    }
  }, "\u5220\u9664")), React.createElement("div", {
    className: "swipe-task-content",
    style: {
      transform: "translateX(".concat(offsetRatio * 100, "%)")
    }
  }, children));
}
function DailyTaskRow(_ref6) {
  var task = _ref6.task,
    onToggle = _ref6.onToggle,
    _onEdit = _ref6.onEdit,
    _onDelete = _ref6.onDelete;
  return React.createElement(SwipeTaskActions, {
    label: "".concat(task.title, "\uFF0C\u5411\u5DE6\u6ED1\u52A8\u53EF\u7F16\u8F91\u6216\u5220\u9664"),
    onEdit: function onEdit() {
      return _onEdit(task);
    },
    onDelete: function onDelete() {
      return _onDelete(task);
    }
  }, React.createElement("button", {
    className: "daily-row task-toggle-row",
    type: "button",
    "aria-label": task.done ? "\u53D6\u6D88\u5B8C\u6210 ".concat(task.title) : "\u5B8C\u6210 ".concat(task.title),
    onClick: function onClick() {
      return onToggle(task.id);
    }
  }, React.createElement("span", {
    className: "task-time ".concat(task.endTime ? "task-time-range" : "")
  }, React.createElement("span", null, task.time), task.endTime ? React.createElement("span", null, "\u2192 ", task.endTime) : null), React.createElement("span", {
    className: "check-button ".concat(task.done ? "done" : ""),
    "aria-hidden": "true"
  }, task.done ? React.createElement(Icon, {
    name: "check",
    size: 15,
    strokeWidth: 2.3
  }) : null), React.createElement("span", {
    className: "task-copy"
  }, React.createElement("span", {
    className: "task-edit-body"
  }, React.createElement("span", {
    className: "task-title ".concat(task.done ? "done" : "")
  }, task.title), task.note ? React.createElement("span", {
    className: "task-note"
  }, task.note) : null, React.createElement("span", {
    className: "task-tags"
  }, React.createElement("span", {
    className: "repeat-pill"
  }, task.repeat), task.reminder && task.reminder !== "到点提醒" ? React.createElement("span", {
    className: "reminder-pill"
  }, task.reminder) : null)))));
}
function CriticalTaskRow(_ref7) {
  var _task$completedDateKe, _task$completedDateKe2;
  var task = _ref7.task,
    onToggle = _ref7.onToggle,
    onOpen = _ref7.onOpen,
    _onDelete2 = _ref7.onDelete;
  var dueCopy = task.done ? "已完成" : task.daysLeft === null ? "无 DDL" : task.daysLeft < 0 ? "\u903E\u671F ".concat(Math.abs(task.daysLeft), " \u5929") : task.daysLeft === 0 ? "今天" : "\u5269 ".concat(task.daysLeft, " \u5929");
  return React.createElement(SwipeTaskActions, {
    label: "".concat(task.title, "\uFF0C\u5411\u5DE6\u6ED1\u52A8\u53EF\u7F16\u8F91\u6216\u5220\u9664"),
    onEdit: function onEdit() {
      return onOpen(task);
    },
    onDelete: function onDelete() {
      return _onDelete2(task);
    }
  }, React.createElement("button", {
    className: "critical-row pressable ".concat(task.done ? "done" : ""),
    "aria-label": task.done ? "\u53D6\u6D88\u5B8C\u6210 ".concat(task.title) : "\u5B8C\u6210 ".concat(task.title),
    onClick: function onClick() {
      return onToggle(task.id);
    }
  }, React.createElement("div", {
    className: "critical-top"
  }, React.createElement("div", {
    className: "critical-title-wrap"
  }, React.createElement("span", {
    className: "critical-check ".concat(task.done ? "done" : "")
  }, task.done ? React.createElement(Icon, {
    name: "check",
    size: 13,
    strokeWidth: 2.3
  }) : null), React.createElement("div", null, React.createElement("div", {
    className: "critical-title ".concat(task.done ? "done" : "")
  }, task.title), task.note ? React.createElement("div", {
    className: "critical-note"
  }, task.note) : null)), React.createElement("span", {
    className: "days-left ".concat(task.daysLeft <= 0 ? "today" : "")
  }, dueCopy)), React.createElement("div", {
    className: "critical-meta"
  }, React.createElement("span", null, task.done ? "".concat(Number((_task$completedDateKe = task.completedDateKey) === null || _task$completedDateKe === void 0 ? void 0 : _task$completedDateKe.slice(5, 7)), "\u6708").concat(Number((_task$completedDateKe2 = task.completedDateKey) === null || _task$completedDateKe2 === void 0 ? void 0 : _task$completedDateKe2.slice(8, 10)), "\u65E5\u5B8C\u6210") : task.deadline ? [displayDeadline(task.deadline), task.time].filter(Boolean).join(" · ") : task.reminder), React.createElement("div", {
    className: "mini-progress",
    "aria-label": "\u5B8C\u6210 ".concat(task.progress, "%")
  }, React.createElement("span", {
    style: {
      width: "".concat(task.progress, "%")
    }
  })))));
}
function BottomNav(_ref8) {
  var activeTab = _ref8.activeTab,
    onTabChange = _ref8.onTabChange,
    onVoice = _ref8.onVoice;
  return React.createElement("nav", {
    className: "bottom-nav",
    "aria-label": "\u4E3B\u5BFC\u822A"
  }, React.createElement("button", {
    className: "nav-tab ".concat(activeTab === "today" ? "active" : ""),
    onClick: function onClick() {
      return onTabChange("today");
    }
  }, React.createElement(Icon, {
    name: "calendar",
    size: 21
  }), React.createElement("span", {
    className: "nav-label"
  }, "\u4ECA\u65E5")), React.createElement("div", {
    className: "voice-slot"
  }, React.createElement("button", {
    className: "voice-button",
    "aria-label": "\u5168\u5C40\u8BED\u97F3\u63A7\u5236",
    onClick: onVoice
  }, React.createElement(Icon, {
    name: "mic",
    size: 27,
    strokeWidth: 2
  }))), React.createElement("button", {
    className: "nav-tab ".concat(activeTab === "critical" ? "active" : ""),
    onClick: function onClick() {
      return onTabChange("critical");
    }
  }, React.createElement(Icon, {
    name: "flag",
    size: 21
  }), React.createElement("span", {
    className: "nav-label"
  }, "\u5173\u952E")));
}
function Sheet(_ref9) {
  var _sheetRef$current2;
  var children = _ref9.children,
    onClose = _ref9.onClose,
    labelledBy = _ref9.labelledBy,
    label = _ref9.label,
    _ref9$depth = _ref9.depth,
    depth = _ref9$depth === void 0 ? 0 : _ref9$depth;
  var _React$useState3 = React.useState(0),
    _React$useState4 = _slicedToArray(_React$useState3, 2),
    dragY = _React$useState4[0],
    setDragY = _React$useState4[1];
  var _React$useState5 = React.useState(false),
    _React$useState6 = _slicedToArray(_React$useState5, 2),
    dragging = _React$useState6[0],
    setDragging = _React$useState6[1];
  var _React$useState7 = React.useState(false),
    _React$useState8 = _slicedToArray(_React$useState7, 2),
    dismissing = _React$useState8[0],
    setDismissing = _React$useState8[1];
  var sheetRef = React.useRef(null);
  var dragRef = React.useRef(null);
  var dismissingRef = React.useRef(false);
  React.useEffect(function () {
    var dismiss = function dismiss(event) {
      var _event$detail, _window$matchMedia, _window;
      if (Number.isFinite((_event$detail = event.detail) === null || _event$detail === void 0 ? void 0 : _event$detail.depth) && event.detail.depth !== depth) return;
      if (dismissingRef.current) return;
      dismissingRef.current = true;
      setDragging(false);
      setDismissing(true);
      var detail = event.detail || {};
      var reduced = (_window$matchMedia = (_window = window).matchMedia) === null || _window$matchMedia === void 0 || (_window$matchMedia = _window$matchMedia.call(_window, "(prefers-reduced-motion: reduce)")) === null || _window$matchMedia === void 0 ? void 0 : _window$matchMedia.matches;
      window.setTimeout(function () {
        var _detail$complete;
        if (detail.completed) return;
        detail.completed = true;
        (_detail$complete = detail.complete) === null || _detail$complete === void 0 || _detail$complete.call(detail);
      }, reduced ? 1 : 230);
    };
    window.addEventListener("jinke-sheet-dismiss", dismiss);
    return function () {
      return window.removeEventListener("jinke-sheet-dismiss", dismiss);
    };
  }, [depth]);
  var onHandlePointerDown = function onHandlePointerDown(event) {
    var _event$currentTarget$4, _event$currentTarget4;
    if (event.button !== undefined && event.button !== 0) return;
    dragRef.current = {
      pointerId: event.pointerId,
      startY: event.clientY,
      lastY: event.clientY,
      lastAt: performance.now(),
      velocity: 0
    };
    setDragging(true);
    (_event$currentTarget$4 = (_event$currentTarget4 = event.currentTarget).setPointerCapture) === null || _event$currentTarget$4 === void 0 || _event$currentTarget$4.call(_event$currentTarget4, event.pointerId);
  };
  var onHandlePointerMove = function onHandlePointerMove(event) {
    var gesture = dragRef.current;
    if (!gesture || gesture.pointerId !== event.pointerId) return;
    var delta = event.clientY - gesture.startY;
    var next = delta >= 0 ? delta : -Math.min(30, Math.abs(delta) * 0.2);
    var now = performance.now();
    var elapsed = Math.max(1, now - gesture.lastAt);
    gesture.velocity = (event.clientY - gesture.lastY) / elapsed;
    gesture.lastY = event.clientY;
    gesture.lastAt = now;
    setDragY(next);
    event.preventDefault();
  };
  var finishHandleGesture = function finishHandleGesture(event) {
    var _sheetRef$current, _event$currentTarget$5, _event$currentTarget5, _event$currentTarget$6, _event$currentTarget6;
    var cancelled = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
    var gesture = dragRef.current;
    if (!gesture || gesture.pointerId !== event.pointerId) return;
    var height = Math.max(1, ((_sheetRef$current = sheetRef.current) === null || _sheetRef$current === void 0 ? void 0 : _sheetRef$current.getBoundingClientRect().height) || 1);
    var shouldDismiss = !cancelled && (dragY >= Math.min(120, height * 0.16) || gesture.velocity > 0.55);
    dragRef.current = null;
    setDragging(false);
    if ((_event$currentTarget$5 = (_event$currentTarget5 = event.currentTarget).hasPointerCapture) !== null && _event$currentTarget$5 !== void 0 && _event$currentTarget$5.call(_event$currentTarget5, event.pointerId)) (_event$currentTarget$6 = (_event$currentTarget6 = event.currentTarget).releasePointerCapture) === null || _event$currentTarget$6 === void 0 || _event$currentTarget$6.call(_event$currentTarget6, event.pointerId);
    if (shouldDismiss) onClose();else setDragY(0);
  };
  var scrimOpacity = Math.min(1, Math.max(0, 1 - Math.max(0, dragY) / Math.max(240, (((_sheetRef$current2 = sheetRef.current) === null || _sheetRef$current2 === void 0 ? void 0 : _sheetRef$current2.offsetHeight) || 480) * 0.7)));
  var sheetStyle = {
    "--sheet-drag-y": "".concat(dragY, "px"),
    "--sheet-stack-offset": "".concat(Math.min(3, depth) * 8, "px"),
    "--sheet-scrim-z": 70 + depth * 20,
    "--sheet-z": 80 + depth * 20
  };
  return React.createElement(React.Fragment, null, React.createElement("button", {
    className: "scrim ".concat(dismissing ? "is-dismissing" : ""),
    "data-sheet-depth": depth,
    style: {
      "--scrim-drag-opacity": scrimOpacity,
      "--sheet-scrim-z": 70 + depth * 20
    },
    "aria-label": "\u5173\u95ED",
    onClick: onClose
  }), React.createElement("section", {
    ref: sheetRef,
    className: "sheet ".concat(dragging ? "is-dragging" : "", " ").concat(dragY < 0 ? "is-pulling" : "", " ").concat(dismissing ? "is-dismissing" : ""),
    "data-sheet-depth": depth,
    style: sheetStyle,
    role: "dialog",
    "aria-modal": "true",
    "aria-labelledby": labelledBy || undefined,
    "aria-label": label || undefined
  }, React.createElement("button", {
    className: "sheet-drag-handle",
    type: "button",
    "aria-label": "\u4E0A\u4E0B\u62D6\u52A8\uFF0C\u5411\u4E0B\u5173\u95ED",
    onPointerDown: onHandlePointerDown,
    onPointerMove: onHandlePointerMove,
    onPointerUp: function onPointerUp(event) {
      return finishHandleGesture(event);
    },
    onPointerCancel: function onPointerCancel(event) {
      return finishHandleGesture(event, true);
    }
  }, React.createElement("span", {
    className: "sheet-handle"
  })), children));
}
function BackHeader(_ref10) {
  var title = _ref10.title,
    onBack = _ref10.onBack;
  return React.createElement("div", {
    className: "back-header"
  }, React.createElement(IconButton, {
    name: "back",
    label: "\u8FD4\u56DE",
    onClick: onBack
  }), React.createElement("h1", null, title));
}
function BarRow(_ref11) {
  var item = _ref11.item;
  return React.createElement("div", {
    className: "bar-row"
  }, React.createElement("span", {
    className: "bar-label"
  }, item.label), React.createElement("div", {
    className: "bar-track"
  }, React.createElement("div", {
    className: "bar-value",
    style: {
      width: "".concat(item.value, "%")
    }
  })), React.createElement("span", {
    className: "bar-detail"
  }, item.detail));
}
Object.assign(window, {
  Icon: Icon,
  PhoneFrame: PhoneFrame,
  IconButton: IconButton,
  SectionHeader: SectionHeader,
  SwipeTaskActions: SwipeTaskActions,
  DailyTaskRow: DailyTaskRow,
  CriticalTaskRow: CriticalTaskRow,
  BottomNav: BottomNav,
  Sheet: Sheet,
  BackHeader: BackHeader,
  BarRow: BarRow
});
})();

/* screens.jsx */
(() => {
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
var _window = window,
  APP_DATA = _window.APP_DATA,
  getCalendarMarker = _window.getCalendarMarker,
  getCriticalReminder = _window.getCriticalReminder,
  normalizeCriticalReminderPlan = _window.normalizeCriticalReminderPlan,
  shouldRemindCritical = _window.shouldRemindCritical,
  getDateMeta = _window.getDateMeta,
  getWeekDates = _window.getWeekDates,
  getMonthDates = _window.getMonthDates,
  shiftDateKeyByMonth = _window.shiftDateKeyByMonth,
  repeatDaysFromValue = _window.repeatDaysFromValue,
  repeatLabelFromDays = _window.repeatLabelFromDays,
  Icon = _window.Icon,
  IconButton = _window.IconButton,
  SectionHeader = _window.SectionHeader,
  DailyTaskRow = _window.DailyTaskRow,
  CriticalTaskRow = _window.CriticalTaskRow,
  Sheet = _window.Sheet,
  BackHeader = _window.BackHeader,
  BarRow = _window.BarRow;
var WEEKDAY_BUTTONS = ["一", "二", "三", "四", "五", "六", "日"];
function SegmentedChoice(_ref) {
  var label = _ref.label,
    value = _ref.value,
    options = _ref.options,
    onChange = _ref.onChange,
    _ref$compact = _ref.compact,
    compact = _ref$compact === void 0 ? false : _ref$compact;
  return React.createElement("div", {
    className: "jinke-segments ".concat(compact ? "compact" : ""),
    role: "radiogroup",
    "aria-label": label
  }, options.map(function (option) {
    var _ref2 = Array.isArray(option) ? option : [option, option],
      _ref3 = _slicedToArray(_ref2, 2),
      optionValue = _ref3[0],
      optionLabel = _ref3[1];
    var selected = String(value) === String(optionValue);
    return React.createElement("button", {
      type: "button",
      role: "radio",
      "aria-checked": selected,
      className: selected ? "selected" : "",
      onClick: function onClick() {
        return onChange(optionValue);
      },
      key: optionValue
    }, optionLabel);
  }));
}
function SoundChoice(_ref4) {
  var sound = _ref4.sound,
    selected = _ref4.selected,
    _ref4$disabled = _ref4.disabled,
    disabled = _ref4$disabled === void 0 ? false : _ref4$disabled,
    onSelect = _ref4.onSelect,
    onPreview = _ref4.onPreview;
  var sourceLabel = sound.source === "system-alarm" ? "系统闹铃" : sound.source === "local" ? "本地音频" : "内置提示音";
  return React.createElement("div", {
    className: "sound-choice ".concat(selected ? "selected" : "")
  }, React.createElement("button", {
    type: "button",
    className: "sound-choice-main",
    disabled: disabled,
    onClick: onSelect
  }, React.createElement("span", null, sound.name), React.createElement("small", null, sourceLabel)), React.createElement("button", {
    type: "button",
    className: "sound-preview",
    disabled: disabled,
    "aria-label": "\u8BD5\u542C".concat(sound.name),
    onClick: onPreview
  }, "\u8BD5\u542C"));
}
function ReminderSoundPicker(_ref5) {
  var _sounds$find;
  var _ref5$task = _ref5.task,
    task = _ref5$task === void 0 ? {} : _ref5$task,
    _ref5$sounds = _ref5.sounds,
    sounds = _ref5$sounds === void 0 ? [] : _ref5$sounds,
    _ref5$defaultAlertMod = _ref5.defaultAlertMode,
    defaultAlertMode = _ref5$defaultAlertMod === void 0 ? "sound" : _ref5$defaultAlertMod,
    _ref5$defaultSoundId = _ref5.defaultSoundId,
    defaultSoundId = _ref5$defaultSoundId === void 0 ? "chime" : _ref5$defaultSoundId,
    onChange = _ref5.onChange,
    _onPreview = _ref5.onPreview,
    onImport = _ref5.onImport,
    onPickSystemAlarm = _ref5.onPickSystemAlarm;
  var alertMode = task.alertMode || "inherit";
  var soundId = task.soundId || "inherit";
  var effectiveMode = alertMode === "inherit" ? defaultAlertMode : alertMode;
  var defaultModeLabel = defaultAlertMode === "silent" ? "默认静音" : "默认响铃";
  var builtInSounds = sounds.filter(function (sound) {
    return sound.source === "built-in";
  });
  var systemAlarmSounds = sounds.filter(function (sound) {
    return sound.source === "system-alarm";
  });
  var localSounds = sounds.filter(function (sound) {
    return sound.source === "local";
  });
  var update = function update(changes) {
    return onChange(_objectSpread(_objectSpread({}, task), changes));
  };
  var selectSound = function selectSound(id) {
    return update({
      soundId: id,
      alertMode: alertMode === "silent" ? "sound" : alertMode
    });
  };
  return React.createElement("div", {
    className: "task-sound-picker",
    "aria-label": "\u4EFB\u52A1\u63D0\u9192\u58F0\u97F3"
  }, React.createElement("div", {
    className: "sound-picker-head"
  }, React.createElement("span", null, "\u63D0\u9192\u65B9\u5F0F"), React.createElement("strong", null, effectiveMode === "silent" ? "静音" : "响铃")), React.createElement(SegmentedChoice, {
    label: "\u4EFB\u52A1\u63D0\u9192\u65B9\u5F0F",
    value: alertMode,
    options: [["inherit", defaultModeLabel], ["sound", "响铃"], ["silent", "静音"]],
    onChange: function onChange(next) {
      return update({
        alertMode: next
      });
    },
    compact: true
  }), effectiveMode === "sound" ? React.createElement(React.Fragment, null, React.createElement("div", {
    className: "sound-picker-head sound-picker-subhead"
  }, React.createElement("span", null, "\u63D0\u9192\u97F3\u6548"), React.createElement("strong", null, soundId === "inherit" ? "使用默认" : "单独设置")), React.createElement("div", {
    className: "sound-library-section"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u9ED8\u8BA4"), React.createElement("div", {
    className: "sound-choice ".concat(soundId === "inherit" ? "selected" : "")
  }, React.createElement("button", {
    type: "button",
    className: "sound-choice-main",
    onClick: function onClick() {
      return update({
        soundId: "inherit"
      });
    }
  }, React.createElement("span", null, "\u9ED8\u8BA4\u97F3\u6548"), React.createElement("small", null, ((_sounds$find = sounds.find(function (sound) {
    return sound.id === defaultSoundId;
  })) === null || _sounds$find === void 0 ? void 0 : _sounds$find.name) || "今刻清音")), React.createElement("button", {
    type: "button",
    className: "sound-preview",
    "aria-label": "\u8BD5\u542C\u9ED8\u8BA4\u97F3\u6548",
    onClick: function onClick() {
      return _onPreview(defaultSoundId);
    }
  }, "\u8BD5\u542C"))), React.createElement("div", {
    className: "sound-library-section"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u63D0\u793A\u97F3"), React.createElement("div", {
    className: "sound-choice-list"
  }, builtInSounds.map(function (sound) {
    return React.createElement(SoundChoice, {
      sound: sound,
      selected: soundId === sound.id,
      onSelect: function onSelect() {
        return selectSound(sound.id);
      },
      onPreview: function onPreview() {
        return _onPreview(sound.id);
      },
      key: sound.id
    });
  }))), React.createElement("div", {
    className: "sound-library-section system-alarm-library"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u95F9\u94C3\u5E93"), React.createElement("button", {
    type: "button",
    className: "sound-library-entry pressable",
    onClick: function onClick() {
      return onPickSystemAlarm(function (id) {
        return selectSound(id);
      }, soundId);
    }
  }, React.createElement("span", null, React.createElement("strong", null, "\u624B\u673A\u7CFB\u7EDF\u95F9\u94C3"), React.createElement("small", null, "\u4F7F\u7528 ColorOS \u95F9\u949F\u97F3\u91CF\uFF0C\u9002\u5408\u8D77\u5E8A\u63D0\u9192")), React.createElement("b", null, "\u8FDB\u5165\u9009\u62E9")), systemAlarmSounds.length ? React.createElement("div", {
    className: "sound-choice-list"
  }, systemAlarmSounds.map(function (sound) {
    return React.createElement(SoundChoice, {
      sound: sound,
      selected: soundId === sound.id,
      onSelect: function onSelect() {
        return selectSound(sound.id);
      },
      onPreview: function onPreview() {
        return _onPreview(sound.id);
      },
      key: sound.id
    });
  })) : null), React.createElement("div", {
    className: "sound-library-section"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u672C\u5730\u97F3\u9891"), localSounds.length ? React.createElement("div", {
    className: "sound-choice-list"
  }, localSounds.map(function (sound) {
    return React.createElement(SoundChoice, {
      sound: sound,
      selected: soundId === sound.id,
      onSelect: function onSelect() {
        return selectSound(sound.id);
      },
      onPreview: function onPreview() {
        return _onPreview(sound.id);
      },
      key: sound.id
    });
  })) : null, React.createElement("button", {
    type: "button",
    className: "sound-import-button pressable",
    onClick: function onClick() {
      return onImport(function (id) {
        return selectSound(id);
      });
    }
  }, "\uFF0B \u5BFC\u5165\u672C\u5730\u97F3\u6548"))) : null);
}
function WeekdayPicker(_ref6) {
  var value = _ref6.value,
    repeatDays = _ref6.repeatDays,
    onChange = _ref6.onChange;
  var selectedDays = repeatDaysFromValue(value, repeatDays);
  var toggleDay = function toggleDay(day) {
    var next = selectedDays.includes(day) ? selectedDays.filter(function (item) {
      return item !== day;
    }) : [].concat(_toConsumableArray(selectedDays), [day]).sort(function (a, b) {
      return a - b;
    });
    onChange(next, repeatLabelFromDays(next));
  };
  return React.createElement("div", {
    className: "weekday-picker",
    role: "group",
    "aria-label": "\u91CD\u590D\u661F\u671F"
  }, React.createElement("div", {
    className: "weekday-summary"
  }, React.createElement("span", null, "\u91CD\u590D"), React.createElement("strong", null, repeatLabelFromDays(selectedDays))), React.createElement("div", {
    className: "weekday-buttons"
  }, WEEKDAY_BUTTONS.map(function (label, index) {
    return React.createElement("button", {
      type: "button",
      "aria-pressed": selectedDays.includes(index + 1),
      className: selectedDays.includes(index + 1) ? "selected" : "",
      onClick: function onClick() {
        return toggleDay(index + 1);
      },
      key: label
    }, label);
  })));
}
function Stepper(_ref7) {
  var label = _ref7.label,
    value = _ref7.value,
    min = _ref7.min,
    max = _ref7.max,
    _ref7$step = _ref7.step,
    step = _ref7$step === void 0 ? 1 : _ref7$step,
    _ref7$wrap = _ref7.wrap,
    wrap = _ref7$wrap === void 0 ? false : _ref7$wrap,
    onChange = _ref7.onChange,
    _ref7$format = _ref7.format,
    format = _ref7$format === void 0 ? function (item) {
      return item;
    } : _ref7$format;
  var safeValue = Math.min(max, Math.max(min, Number(value) || 0));
  var decrease = function decrease() {
    return onChange(wrap && safeValue <= min ? max : Math.max(min, safeValue - step));
  };
  var increase = function increase() {
    return onChange(wrap && safeValue >= max ? min : Math.min(max, safeValue + step));
  };
  return React.createElement("div", {
    className: "jinke-stepper",
    role: "group",
    "aria-label": label
  }, React.createElement("button", {
    type: "button",
    "aria-label": "".concat(label, "\u51CF\u5C11"),
    disabled: !wrap && safeValue <= min,
    onClick: decrease
  }, "\u2212"), React.createElement("output", {
    "aria-label": label
  }, format(safeValue)), React.createElement("button", {
    type: "button",
    "aria-label": "".concat(label, "\u589E\u52A0"),
    disabled: !wrap && safeValue >= max,
    onClick: increase
  }, "\uFF0B"));
}
function parseClock(value) {
  var fallback = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "09:00";
  var match = String(value || "").match(/^(\d{1,2}):(\d{2})$/);
  var source = match || fallback.match(/^(\d{1,2}):(\d{2})$/);
  var minute = Math.min(55, Math.max(0, Math.round(Number((source === null || source === void 0 ? void 0 : source[2]) || 0) / 5) * 5));
  return {
    hour: Math.min(24, Math.max(0, Number((source === null || source === void 0 ? void 0 : source[1]) || 9))),
    minute: minute
  };
}
function TimePicker(_ref8) {
  var label = _ref8.label,
    value = _ref8.value,
    onChange = _ref8.onChange,
    _ref8$allowUnset = _ref8.allowUnset,
    allowUnset = _ref8$allowUnset === void 0 ? true : _ref8$allowUnset,
    _ref8$maxHour = _ref8.maxHour,
    maxHour = _ref8$maxHour === void 0 ? 24 : _ref8$maxHour;
  var enabled = Boolean(value && value !== "待定");
  var clock = parseClock(value);
  var update = function update(hour, minute) {
    return onChange("".concat(String(hour).padStart(2, "0"), ":").concat(String(minute).padStart(2, "0")));
  };
  return React.createElement("div", {
    className: "jinke-time-picker",
    "aria-label": label
  }, React.createElement("div", {
    className: "picker-head"
  }, React.createElement("span", null, label), allowUnset ? React.createElement("button", {
    type: "button",
    "aria-pressed": enabled,
    onClick: function onClick() {
      return onChange(enabled ? null : "09:00");
    }
  }, enabled ? "清除" : "设置") : null), enabled || !allowUnset ? React.createElement("div", {
    className: "time-stepper-row"
  }, React.createElement(Stepper, {
    label: "".concat(label, "\u5C0F\u65F6"),
    value: Math.min(maxHour, clock.hour),
    min: 0,
    max: maxHour,
    wrap: true,
    onChange: function onChange(hour) {
      return update(hour, clock.minute);
    },
    format: function format(item) {
      return String(item).padStart(2, "0");
    }
  }), React.createElement("span", {
    className: "time-colon"
  }, ":"), React.createElement(Stepper, {
    label: "".concat(label, "\u5206\u949F"),
    value: clock.minute,
    min: 0,
    max: 55,
    step: 5,
    wrap: true,
    onChange: function onChange(minute) {
      return update(clock.hour, minute);
    },
    format: function format(item) {
      return String(item).padStart(2, "0");
    }
  })) : React.createElement("span", {
    className: "picker-empty"
  }, "\u672A\u8BBE\u7F6E"));
}
function reminderToMinutes(value) {
  var _text$match, _text$match2;
  var text = String(value || "到点提醒");
  if (text === "不提醒") return null;
  if (text === "到点提醒") return 0;
  var hours = Number(((_text$match = text.match(/(\d+)\s*小时/)) === null || _text$match === void 0 ? void 0 : _text$match[1]) || 0);
  var minutes = Number(((_text$match2 = text.match(/(\d+)\s*分钟/)) === null || _text$match2 === void 0 ? void 0 : _text$match2[1]) || 0);
  return Math.min(1435, Math.max(0, Math.round((hours * 60 + minutes) / 5) * 5));
}
function reminderFromMinutes(totalMinutes) {
  if (totalMinutes === null) return "不提醒";
  var safe = Math.min(1435, Math.max(0, Math.round((Number(totalMinutes) || 0) / 5) * 5));
  if (safe === 0) return "到点提醒";
  var hours = Math.floor(safe / 60);
  var minutes = safe % 60;
  return "\u63D0\u524D".concat(hours ? "".concat(hours, "\u5C0F\u65F6") : "").concat(minutes ? "".concat(minutes, "\u5206\u949F") : "");
}
function ReminderPicker(_ref9) {
  var value = _ref9.value,
    onChange = _ref9.onChange;
  var total = reminderToMinutes(value);
  var enabled = total !== null;
  var safe = total || 0;
  var hours = Math.floor(safe / 60);
  var minutes = safe % 60;
  var update = function update(nextHours, nextMinutes) {
    return onChange(reminderFromMinutes(Math.min(1435, nextHours * 60 + nextMinutes)));
  };
  return React.createElement("div", {
    className: "jinke-reminder-picker",
    "aria-label": "\u63D0\u524D\u63D0\u9192"
  }, React.createElement("div", {
    className: "picker-head"
  }, React.createElement("span", null, "\u63D0\u9192"), React.createElement("button", {
    type: "button",
    "aria-pressed": enabled,
    className: enabled ? "enabled" : "",
    onClick: function onClick() {
      return onChange(enabled ? "不提醒" : "到点提醒");
    }
  }, enabled ? "已开启" : "不提醒")), enabled ? React.createElement(React.Fragment, null, React.createElement("div", {
    className: "reminder-stepper-row"
  }, React.createElement("div", null, React.createElement("small", null, "\u5C0F\u65F6"), React.createElement(Stepper, {
    label: "\u63D0\u524D\u5C0F\u65F6",
    value: hours,
    min: 0,
    max: 23,
    wrap: true,
    onChange: function onChange(next) {
      return update(next, minutes);
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u5206\u949F"), React.createElement(Stepper, {
    label: "\u63D0\u524D\u5206\u949F",
    value: minutes,
    min: 0,
    max: 55,
    step: 5,
    wrap: true,
    onChange: function onChange(next) {
      return update(hours, next);
    }
  }))), React.createElement("output", {
    className: "reminder-summary"
  }, reminderFromMinutes(safe))) : null);
}
function deadlineToDateInput(deadline) {
  if (!deadline) return "";
  if (/^\d{4}-\d{2}-\d{2}$/.test(deadline)) return deadline;
  var today = new Date();
  var relative = String(deadline).match(/^(今天截止|今天|明天|后天|(\d+)天后)$/);
  if (relative) {
    var days = relative[1] === "明天" ? 1 : relative[1] === "后天" ? 2 : Number(relative[2] || 0);
    var _target = new Date(today.getFullYear(), today.getMonth(), today.getDate() + days, 12);
    return "".concat(_target.getFullYear(), "-").concat(String(_target.getMonth() + 1).padStart(2, "0"), "-").concat(String(_target.getDate()).padStart(2, "0"));
  }
  var absolute = String(deadline).match(/(\d{1,2})月(\d{1,2})日/);
  if (!absolute) return "";
  var target = new Date(today.getFullYear(), Number(absolute[1]) - 1, Number(absolute[2]), 12);
  if (target < new Date(today.getFullYear(), today.getMonth(), today.getDate(), 12)) target = new Date(today.getFullYear() + 1, Number(absolute[1]) - 1, Number(absolute[2]), 12);
  return "".concat(target.getFullYear(), "-").concat(String(target.getMonth() + 1).padStart(2, "0"), "-").concat(String(target.getDate()).padStart(2, "0"));
}
function dateInputToDeadline(value) {
  if (!value) return null;
  return value;
}
function DatePicker(_ref10) {
  var value = _ref10.value,
    onChange = _ref10.onChange,
    _ref10$label = _ref10.label,
    label = _ref10$label === void 0 ? "截止日期" : _ref10$label,
    _ref10$allowPast = _ref10.allowPast,
    allowPast = _ref10$allowPast === void 0 ? false : _ref10$allowPast;
  var today = new Date();
  var iso = deadlineToDateInput(value) || "".concat(today.getFullYear(), "-").concat(String(today.getMonth() + 1).padStart(2, "0"), "-").concat(String(today.getDate()).padStart(2, "0"));
  var _iso$split$map = iso.split("-").map(Number),
    _iso$split$map2 = _slicedToArray(_iso$split$map, 3),
    year = _iso$split$map2[0],
    month = _iso$split$map2[1],
    day = _iso$split$map2[2];
  var maxDay = new Date(year, month, 0).getDate();
  var minYear = allowPast ? 1900 : today.getFullYear();
  var update = function update(nextYear, nextMonth, nextDay) {
    var safeDay = Math.min(new Date(nextYear, nextMonth, 0).getDate(), nextDay);
    var candidate = new Date(nextYear, nextMonth - 1, safeDay, 12);
    var floor = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 12);
    var next = !allowPast && candidate < floor ? floor : candidate;
    onChange(dateInputToDeadline("".concat(next.getFullYear(), "-").concat(String(next.getMonth() + 1).padStart(2, "0"), "-").concat(String(next.getDate()).padStart(2, "0"))));
  };
  return React.createElement("div", {
    className: "jinke-date-picker",
    "aria-label": label
  }, React.createElement("div", {
    className: "picker-head"
  }, React.createElement("span", null, label), React.createElement("button", {
    type: "button",
    "aria-pressed": Boolean(value),
    onClick: function onClick() {
      return onChange(value ? null : dateInputToDeadline(iso));
    }
  }, value ? "清除" : "设置")), value ? React.createElement("div", {
    className: "date-stepper-row"
  }, React.createElement("div", null, React.createElement("small", null, "\u5E74"), React.createElement(Stepper, {
    label: "\u622A\u6B62\u5E74\u4EFD",
    value: year,
    min: minYear,
    max: today.getFullYear() + 10,
    wrap: true,
    onChange: function onChange(next) {
      return update(next, month, day);
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u6708"), React.createElement(Stepper, {
    label: "\u622A\u6B62\u6708\u4EFD",
    value: month,
    min: 1,
    max: 12,
    wrap: true,
    onChange: function onChange(next) {
      return update(year, next, day);
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u65E5"), React.createElement(Stepper, {
    label: "\u622A\u6B62\u65E5\u671F",
    value: day,
    min: 1,
    max: maxDay,
    wrap: true,
    onChange: function onChange(next) {
      return update(year, month, next);
    }
  }))) : React.createElement("span", {
    className: "picker-empty"
  }, "\u672A\u8BBE\u7F6E"));
}
function CompletionDatePicker(_ref11) {
  var value = _ref11.value,
    onChange = _ref11.onChange;
  var today = new Date();
  var todayKey = "".concat(today.getFullYear(), "-").concat(String(today.getMonth() + 1).padStart(2, "0"), "-").concat(String(today.getDate()).padStart(2, "0"));
  var iso = /^\d{4}-\d{2}-\d{2}$/.test(String(value || "")) ? value : todayKey;
  var _iso$split$map3 = iso.split("-").map(Number),
    _iso$split$map4 = _slicedToArray(_iso$split$map3, 3),
    year = _iso$split$map4[0],
    month = _iso$split$map4[1],
    day = _iso$split$map4[2];
  var maxDay = new Date(year, month, 0).getDate();
  var minYear = 1900;
  var update = function update(nextYear, nextMonth, nextDay) {
    var safeDay = Math.min(new Date(nextYear, nextMonth, 0).getDate(), nextDay);
    var candidate = new Date(nextYear, nextMonth - 1, safeDay, 12);
    var ceiling = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 12);
    var next = candidate > ceiling ? ceiling : candidate;
    onChange("".concat(next.getFullYear(), "-").concat(String(next.getMonth() + 1).padStart(2, "0"), "-").concat(String(next.getDate()).padStart(2, "0")));
  };
  return React.createElement("div", {
    className: "jinke-date-picker completion-date-picker",
    "aria-label": "\u5B8C\u6210\u65E5\u671F"
  }, React.createElement("div", {
    className: "picker-head"
  }, React.createElement("span", null, "\u5B8C\u6210\u65E5\u671F"), React.createElement("strong", null, year, "\u5E74", month, "\u6708", day, "\u65E5")), React.createElement("div", {
    className: "date-stepper-row"
  }, React.createElement("div", null, React.createElement("small", null, "\u5E74"), React.createElement(Stepper, {
    label: "\u5B8C\u6210\u5E74\u4EFD",
    value: year,
    min: minYear,
    max: today.getFullYear(),
    wrap: true,
    onChange: function onChange(next) {
      return update(next, month, day);
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u6708"), React.createElement(Stepper, {
    label: "\u5B8C\u6210\u6708\u4EFD",
    value: month,
    min: 1,
    max: 12,
    wrap: true,
    onChange: function onChange(next) {
      return update(year, next, day);
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u65E5"), React.createElement(Stepper, {
    label: "\u5B8C\u6210\u65E5\u671F",
    value: day,
    min: 1,
    max: maxDay,
    wrap: true,
    onChange: function onChange(next) {
      return update(year, month, next);
    }
  }))));
}
function deadlineDaysFromToday(deadline) {
  var iso = deadlineToDateInput(deadline);
  if (!iso) return 1;
  var _iso$split$map5 = iso.split("-").map(Number),
    _iso$split$map6 = _slicedToArray(_iso$split$map5, 3),
    year = _iso$split$map6[0],
    month = _iso$split$map6[1],
    day = _iso$split$map6[2];
  var today = new Date();
  var start = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 12);
  var end = new Date(year, month - 1, day, 12);
  return Math.max(1, Math.round((end - start) / 86400000));
}
function DeadlineLeadPicker(_ref12) {
  var value = _ref12.value,
    onChange = _ref12.onChange;
  var safe = Math.min(1435, Math.max(0, Math.round((Number(value) || 0) / 5) * 5));
  var hours = Math.floor(safe / 60);
  var minutes = safe % 60;
  var update = function update(nextHours, nextMinutes) {
    return onChange(Math.min(1435, nextHours * 60 + nextMinutes));
  };
  return React.createElement("div", {
    className: "deadline-lead-picker",
    "aria-label": "\u622A\u6B62\u65F6\u523B\u524D\u63D0\u9192"
  }, React.createElement("span", {
    className: "control-caption"
  }, "\u622A\u6B62\u65F6\u523B\u524D"), React.createElement("div", {
    className: "reminder-stepper-row"
  }, React.createElement("div", null, React.createElement("small", null, "\u5C0F\u65F6"), React.createElement(Stepper, {
    label: "\u622A\u6B62\u524D\u5C0F\u65F6",
    value: hours,
    min: 0,
    max: 23,
    wrap: true,
    onChange: function onChange(next) {
      return update(next, minutes);
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u5206\u949F"), React.createElement(Stepper, {
    label: "\u622A\u6B62\u524D\u5206\u949F",
    value: minutes,
    min: 0,
    max: 55,
    step: 5,
    wrap: true,
    onChange: function onChange(next) {
      return update(hours, next);
    }
  }))));
}
function CriticalReminderPlanPicker(_ref13) {
  var task = _ref13.task,
    onChange = _ref13.onChange;
  var plan = normalizeCriticalReminderPlan(task);
  var enabled = Boolean(task.deadline && plan.reminderEnabled);
  var finalDaysMax = deadlineDaysFromToday(task.deadline);
  var boundedFinalDays = Math.min(Math.max(1, plan.reminderFinalDays || 1), finalDaysMax);
  var commit = function commit(changes) {
    var next = _objectSpread(_objectSpread(_objectSpread({}, task), plan), changes);
    next.reminder = getCriticalReminder(next);
    onChange(next);
  };
  return React.createElement("div", {
    className: "critical-reminder-picker ".concat(!task.deadline ? "disabled" : ""),
    "aria-label": "\u63D0\u9192\u8BA1\u5212"
  }, React.createElement("div", {
    className: "picker-head"
  }, React.createElement("span", null, "\u63D0\u9192\u8BA1\u5212"), React.createElement("button", {
    type: "button",
    "aria-pressed": enabled,
    disabled: !task.deadline,
    onClick: function onClick() {
      return commit({
        reminderEnabled: !enabled
      });
    }
  }, enabled ? "已开启" : "不提醒")), !task.deadline ? React.createElement("span", {
    className: "picker-empty"
  }, "\u8BBE\u7F6E\u622A\u6B62\u65E5\u671F\u540E\u53EF\u5F00\u542F") : enabled ? React.createElement(React.Fragment, null, plan.reminderMode !== "deadline-only" || !task.deadlineTime ? React.createElement(TimePicker, {
    label: "\u63D0\u9192\u65F6\u523B",
    value: plan.reminderTime,
    allowUnset: false,
    onChange: function onChange(reminderTime) {
      return commit({
        reminderTime: reminderTime || "10:00"
      });
    }
  }) : React.createElement(DeadlineLeadPicker, {
    value: plan.deadlineLeadMinutes,
    onChange: function onChange(deadlineLeadMinutes) {
      return commit({
        deadlineLeadMinutes: deadlineLeadMinutes
      });
    }
  }), React.createElement("div", {
    className: "critical-plan-frequency"
  }, React.createElement("span", {
    className: "control-caption"
  }, "\u63D0\u9192\u9891\u7387"), React.createElement(SegmentedChoice, {
    label: "\u63D0\u9192\u9891\u7387",
    value: plan.reminderMode,
    options: [["smart", "智能节点"], ["final-days", "仅最后 X 天"], ["deadline-only", "仅截止日"]],
    onChange: function onChange(reminderMode) {
      return commit(reminderMode === "final-days" ? {
        reminderMode: reminderMode,
        reminderFinalDays: boundedFinalDays
      } : {
        reminderMode: reminderMode
      });
    },
    compact: true
  })), plan.reminderMode === "smart" ? React.createElement("div", {
    className: "critical-plan-rule-grid"
  }, React.createElement("div", null, React.createElement("small", null, "\u95F4\u9694\u5929\u6570"), React.createElement(Stepper, {
    label: "\u95F4\u9694\u5929\u6570",
    value: plan.reminderMultiple,
    min: 1,
    max: 30,
    onChange: function onChange(reminderMultiple) {
      return commit({
        reminderMultiple: reminderMultiple
      });
    }
  })), React.createElement("div", null, React.createElement("small", null, "\u6700\u540E\u6BCF\u5929\u63D0\u9192"), React.createElement(Stepper, {
    label: "\u6700\u540E\u6BCF\u5929\u63D0\u9192\u5929\u6570",
    value: plan.reminderFinalDays,
    min: 0,
    max: 30,
    onChange: function onChange(reminderFinalDays) {
      return commit({
        reminderFinalDays: reminderFinalDays
      });
    }
  }))) : null, plan.reminderMode === "final-days" ? React.createElement("div", {
    className: "critical-plan-rule-single"
  }, React.createElement("small", null, "\u6700\u540E\u6BCF\u5929\u63D0\u9192"), React.createElement(Stepper, {
    label: "\u6700\u540E\u6BCF\u5929\u63D0\u9192\u5929\u6570",
    value: boundedFinalDays,
    min: 1,
    max: finalDaysMax,
    wrap: true,
    onChange: function onChange(reminderFinalDays) {
      return commit({
        reminderFinalDays: reminderFinalDays
      });
    }
  }), React.createElement("span", null, "\u5929")) : null, React.createElement("output", {
    className: "critical-plan-summary"
  }, getCriticalReminder(_objectSpread(_objectSpread(_objectSpread({}, task), plan), {}, {
    reminderFinalDays: plan.reminderMode === "final-days" ? boundedFinalDays : plan.reminderFinalDays
  })))) : null);
}
function WeekStrip(_ref14) {
  var selectedDateKey = _ref14.selectedDateKey,
    todayDateKey = _ref14.todayDateKey,
    onSelectDate = _ref14.onSelectDate,
    getDateLoad = _ref14.getDateLoad;
  return React.createElement("div", {
    className: "week-strip",
    "aria-label": "\u672C\u5468"
  }, getWeekDates(selectedDateKey).map(function (item) {
    var isToday = item.dateKey === todayDateKey;
    var isSelected = item.dateKey === selectedDateKey;
    var load = Math.max(0, Number(getDateLoad === null || getDateLoad === void 0 ? void 0 : getDateLoad(item.dateKey)) || 0);
    return React.createElement("button", {
      className: "day-cell ".concat(isToday ? "today" : "", " ").concat(isSelected && !isToday ? "selected" : ""),
      "aria-current": isToday ? "date" : undefined,
      "aria-pressed": isSelected,
      onClick: function onClick() {
        return onSelectDate(item.dateKey);
      },
      key: item.dateKey
    }, React.createElement("span", {
      className: "day-name"
    }, item.day), React.createElement("span", {
      className: "day-number"
    }, item.date), React.createElement("span", {
      className: "load-dots"
    }, Array.from({
      length: Math.min(load, 3)
    }).map(function (_, index) {
      return React.createElement("span", {
        key: index
      });
    })));
  }));
}
function MonthPeekPanel(_ref15) {
  var selectedDateKey = _ref15.selectedDateKey,
    todayDateKey = _ref15.todayDateKey,
    onSelectDate = _ref15.onSelectDate,
    getDateLoad = _ref15.getDateLoad;
  var selected = getDateMeta(selectedDateKey);
  var monthDays = getMonthDates(selectedDateKey);
  return React.createElement("div", {
    className: "peek-panel"
  }, React.createElement("div", {
    className: "peek-head"
  }, React.createElement("button", {
    className: "month-nav-button",
    type: "button",
    "aria-label": "\u4E0A\u4E2A\u6708",
    onClick: function onClick() {
      return onSelectDate(shiftDateKeyByMonth(selectedDateKey, -1));
    }
  }, React.createElement(Icon, {
    name: "back",
    size: 17
  })), React.createElement("span", {
    className: "peek-title"
  }, selected.year, " \u5E74 ", selected.month, " \u6708"), React.createElement("button", {
    className: "month-nav-button",
    type: "button",
    "aria-label": "\u4E0B\u4E2A\u6708",
    onClick: function onClick() {
      return onSelectDate(shiftDateKeyByMonth(selectedDateKey, 1));
    }
  }, React.createElement(Icon, {
    name: "chevronRight",
    size: 17
  }))), React.createElement("div", {
    className: "month-weekdays",
    "aria-hidden": "true"
  }, ["一", "二", "三", "四", "五", "六", "日"].map(function (day) {
    return React.createElement("span", {
      key: day
    }, day);
  })), React.createElement("div", {
    className: "month-grid"
  }, monthDays.map(function (item) {
    var today = item.dateKey === todayDateKey;
    var isSelected = item.dateKey === selectedDateKey;
    var load = Math.max(0, Number(getDateLoad === null || getDateLoad === void 0 ? void 0 : getDateLoad(item.dateKey)) || 0);
    return React.createElement("button", {
      type: "button",
      className: "month-day ".concat(item.muted ? "muted" : "", " ").concat(today ? "today" : "", " ").concat(isSelected && !today ? "selected" : "", " ").concat(load ? "busy" : ""),
      "aria-label": "".concat(item.year, "\u5E74").concat(item.month, "\u6708").concat(item.date, "\u65E5"),
      "aria-current": today ? "date" : undefined,
      "aria-pressed": isSelected,
      onClick: function onClick() {
        return onSelectDate(item.dateKey);
      },
      key: item.dateKey
    }, item.date);
  })), React.createElement("button", {
    className: "peek-today-action",
    type: "button",
    onClick: function onClick() {
      return onSelectDate(todayDateKey);
    }
  }, "\u4ECA\u5929"));
}
function ViewMenu(_ref16) {
  var onSelect = _ref16.onSelect,
    onClose = _ref16.onClose;
  return React.createElement(Sheet, {
    onClose: onClose,
    label: "\u68C0\u89C6\u65B9\u5F0F"
  }, React.createElement("div", {
    className: "menu-list menu-list-first"
  }, [{
    id: "day",
    title: "日",
    icon: "calendar"
  }, {
    id: "month",
    title: "月",
    icon: "year"
  }].map(function (item) {
    return React.createElement("button", {
      className: "menu-row",
      key: item.id,
      onClick: function onClick() {
        return onSelect(item.id);
      }
    }, React.createElement("span", {
      className: "menu-icon"
    }, React.createElement(Icon, {
      name: item.icon,
      size: 18
    })), React.createElement("span", {
      className: "menu-title"
    }, item.title), React.createElement(Icon, {
      name: "chevronRight",
      size: 17
    }));
  })));
}
function TodayScreen(_ref17) {
  var tasks = _ref17.tasks,
    deadlineTasks = _ref17.deadlineTasks,
    onToggle = _ref17.onToggle,
    onEdit = _ref17.onEdit,
    onDeleteDaily = _ref17.onDeleteDaily,
    onToggleCritical = _ref17.onToggleCritical,
    onOpenCritical = _ref17.onOpenCritical,
    onDeleteCritical = _ref17.onDeleteCritical,
    onMenu = _ref17.onMenu,
    viewMode = _ref17.viewMode,
    onOpenView = _ref17.onOpenView,
    selectedDateKey = _ref17.selectedDateKey,
    todayDateKey = _ref17.todayDateKey,
    onSelectDate = _ref17.onSelectDate,
    getDateLoad = _ref17.getDateLoad,
    onOpenDayArchive = _ref17.onOpenDayArchive;
  var done = tasks.filter(function (task) {
    return task.done;
  }).length;
  var percent = Math.round(done / Math.max(tasks.length, 1) * 100);
  var allComplete = tasks.length > 0 && done === tasks.length;
  var selectedDay = getDateMeta(selectedDateKey);
  var isToday = selectedDateKey === todayDateKey;
  var dateLabel = "".concat(selectedDay.month, "\u6708").concat(selectedDay.date, "\u65E5");
  var calendarMarker = getCalendarMarker(selectedDateKey);
  return React.createElement("main", {
    className: "screen today-screen"
  }, React.createElement("div", {
    className: "fold-divider",
    "aria-hidden": "true"
  }), React.createElement("div", {
    className: "top-row"
  }, React.createElement(IconButton, {
    name: "menu",
    label: "\u66F4\u591A",
    onClick: onMenu
  }), React.createElement("button", {
    className: "view-trigger pressable",
    onClick: onOpenView
  }, viewMode === "day" ? "日" : "月", React.createElement(Icon, {
    name: "chevronDown",
    size: 15
  }))), React.createElement("div", {
    className: "today-responsive-grid"
  }, React.createElement("div", {
    className: "today-left-pane"
  }, React.createElement("section", {
    className: "today-calendar-pane",
    "aria-label": "\u65E5\u671F\u4E0E\u68C0\u89C6"
  }, React.createElement("div", {
    className: "page-title-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "eyebrow-line"
  }, React.createElement("p", {
    className: "eyebrow"
  }, "\u661F\u671F", selectedDay.day, calendarMarker.short ? " \xB7 ".concat(calendarMarker.short) : ""), React.createElement("button", {
    className: "day-archive-trigger",
    "aria-label": "\u67E5\u770B".concat(dateLabel, "\u662F\u4EC0\u4E48\u65E5\u5B50"),
    onClick: onOpenDayArchive
  }, React.createElement(Icon, {
    name: "spark",
    size: 13
  }))), React.createElement("h1", {
    className: "page-title"
  }, dateLabel)), React.createElement("div", {
    className: "page-meta"
  }, done, "/", tasks.length, " \u5DF2\u5B8C\u6210")), viewMode === "month" ? React.createElement(MonthPeekPanel, {
    selectedDateKey: selectedDateKey,
    todayDateKey: todayDateKey,
    onSelectDate: onSelectDate,
    getDateLoad: getDateLoad
  }) : React.createElement(WeekStrip, {
    selectedDateKey: selectedDateKey,
    todayDateKey: todayDateKey,
    onSelectDate: onSelectDate,
    getDateLoad: getDateLoad
  }), React.createElement("div", {
    className: "progress-line",
    "aria-label": "".concat(dateLabel, "\u5B8C\u6210 ").concat(percent, "%")
  }, React.createElement("div", {
    className: "progress-track"
  }, React.createElement("div", {
    className: "progress-fill",
    style: {
      width: "".concat(percent, "%")
    }
  })), React.createElement("span", {
    className: "progress-copy"
  }, percent, "%"))), deadlineTasks.length ? React.createElement("section", {
    className: "today-ddl-pane",
    "aria-label": "\u5F53\u5929\u5173\u952E\u4E8B\u9879"
  }, React.createElement(SectionHeader, {
    title: deadlineTasks.some(function (task) {
      return !task.deadline;
    }) ? "关键" : "DDL"
  }), React.createElement("div", {
    className: "critical-stack daily-ddl-stack"
  }, deadlineTasks.map(function (task) {
    return React.createElement(CriticalTaskRow, {
      task: task,
      onToggle: onToggleCritical,
      onOpen: onOpenCritical,
      onDelete: onDeleteCritical,
      key: task.id
    });
  }))) : null), React.createElement("section", {
    className: "today-daily-pane",
    "aria-label": "\u65E5\u5E38\u4E8B\u9879"
  }, React.createElement(SectionHeader, {
    title: isToday ? "今天" : "\u661F\u671F".concat(selectedDay.day),
    note: allComplete ? "全部完成" : "".concat(tasks.length - done, " \u9879\u5F85\u5B8C\u6210"),
    noteTone: allComplete ? "complete" : ""
  }), tasks.some(function (task) {
    return task.demo;
  }) ? React.createElement("div", {
    className: "demo-guide"
  }, "\u5DE6\u6ED1\u5220\u9664\u6F14\u793A\uFF0C\u70B9\u4E0B\u65B9\u8BED\u97F3\u952E\u521B\u5EFA\u7B2C\u4E00\u9879\u65E5\u7A0B") : null, React.createElement("div", {
    className: "task-list"
  }, tasks.map(function (task) {
    return React.createElement(DailyTaskRow, {
      task: task,
      onToggle: onToggle,
      onEdit: onEdit,
      onDelete: onDeleteDaily,
      key: task.id
    });
  })), !tasks.length ? React.createElement("div", {
    className: "empty-guide"
  }, "\u70B9\u4E0B\u65B9\u8BED\u97F3\u952E\uFF0C\u521B\u5EFA\u7B2C\u4E00\u9879\u65E5\u7A0B") : null)));
}
var DAY_ARCHIVE_TABS = [{
  id: "china",
  label: "中国"
}, {
  id: "holidays",
  label: "全球节日"
}, {
  id: "events",
  label: "历史事件"
}, {
  id: "people",
  label: "人物纪念"
}];
var ON_THIS_DAY_MEMORY = new Map();
var ON_THIS_DAY_REQUESTS = new Map();
function normalizeOnThisDayItems(items, source) {
  var kind = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "";
  var seen = new Set();
  return (items || []).filter(function (item) {
    var _item$year;
    var key = "".concat((_item$year = item === null || item === void 0 ? void 0 : item.year) !== null && _item$year !== void 0 ? _item$year : "", ":").concat((item === null || item === void 0 ? void 0 : item.text) || "");
    if (!(item !== null && item !== void 0 && item.text) || seen.has(key)) return false;
    seen.add(key);
    return true;
  }).slice(0, 80).map(function (item) {
    var _item$pages;
    return {
      text: item.text,
      year: Number.isFinite(item.year) ? item.year : null,
      source: source,
      kind: kind,
      url: ((_item$pages = item.pages) === null || _item$pages === void 0 || (_item$pages = _item$pages[0]) === null || _item$pages === void 0 || (_item$pages = _item$pages.content_urls) === null || _item$pages === void 0 || (_item$pages = _item$pages.desktop) === null || _item$pages === void 0 ? void 0 : _item$pages.page) || ""
    };
  });
}
function mergePeopleItems(births, deaths) {
  var seen = new Set();
  return [].concat(_toConsumableArray(normalizeOnThisDayItems(births, "Wikimedia · 人物纪念", "诞辰")), _toConsumableArray(normalizeOnThisDayItems(deaths, "Wikimedia · 人物纪念", "逝世"))).filter(function (item) {
    var _item$year2;
    var key = "".concat((_item$year2 = item.year) !== null && _item$year2 !== void 0 ? _item$year2 : "", ":").concat(item.text);
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  }).slice(0, 120);
}
function getArchiveCacheKey(category, month, day) {
  return "".concat(month, "-").concat(day, "-").concat(category);
}
function readArchiveCache(category, month, day) {
  var key = getArchiveCacheKey(category, month, day);
  if (ON_THIS_DAY_MEMORY.has(key)) return ON_THIS_DAY_MEMORY.get(key);
  try {
    var _legacy$data, _legacy$data2, _legacy$data3;
    var cached = JSON.parse(localStorage.getItem("jinke-onthisday-v2-".concat(key)) || "null");
    if (Array.isArray(cached === null || cached === void 0 ? void 0 : cached.items)) {
      ON_THIS_DAY_MEMORY.set(key, cached.items);
      return cached.items;
    }
    var legacy = JSON.parse(localStorage.getItem("jinke-onthisday-".concat(month, "-").concat(day)) || "null");
    var legacyItems = category === "people" ? mergePeopleItems((legacy === null || legacy === void 0 || (_legacy$data = legacy.data) === null || _legacy$data === void 0 ? void 0 : _legacy$data.births) || [], (legacy === null || legacy === void 0 || (_legacy$data2 = legacy.data) === null || _legacy$data2 === void 0 ? void 0 : _legacy$data2.deaths) || []) : legacy === null || legacy === void 0 || (_legacy$data3 = legacy.data) === null || _legacy$data3 === void 0 ? void 0 : _legacy$data3[category];
    if (Array.isArray(legacyItems) && legacyItems.length) {
      ON_THIS_DAY_MEMORY.set(key, legacyItems);
      return legacyItems;
    }
  } catch (_unused) {}
  return null;
}
function requestOnThisDayType(type, month, day) {
  var controller = new AbortController();
  var timeout = window.setTimeout(function () {
    return controller.abort();
  }, 18000);
  return fetch("https://api.wikimedia.org/feed/v1/wikipedia/zh/onthisday/".concat(type, "/").concat(month, "/").concat(day), {
    headers: {
      Accept: "application/json"
    },
    signal: controller.signal
  }).then(function (response) {
    if (!response.ok) throw new Error("On this day unavailable");
    return response.json();
  }).finally(function () {
    return window.clearTimeout(timeout);
  });
}
function loadArchiveCategory(category, month, day) {
  var key = getArchiveCacheKey(category, month, day);
  var cached = readArchiveCache(category, month, day);
  if (cached) return Promise.resolve(cached);
  if (ON_THIS_DAY_REQUESTS.has(key)) return ON_THIS_DAY_REQUESTS.get(key);
  var types = category === "people" ? ["births", "deaths"] : [category];
  var request = Promise.all(types.map(function (type) {
    return requestOnThisDayType(type, month, day);
  })).then(function (responses) {
    var _responses$, _responses$2, _responses$3;
    var items = category === "people" ? mergePeopleItems((_responses$ = responses[0]) === null || _responses$ === void 0 ? void 0 : _responses$.births, (_responses$2 = responses[1]) === null || _responses$2 === void 0 ? void 0 : _responses$2.deaths) : normalizeOnThisDayItems((_responses$3 = responses[0]) === null || _responses$3 === void 0 ? void 0 : _responses$3[category], "Wikimedia \xB7 ".concat(category === "holidays" ? "全球节日" : "历史事件"));
    ON_THIS_DAY_MEMORY.set(key, items);
    try {
      localStorage.setItem("jinke-onthisday-v2-".concat(key), JSON.stringify({
        savedAt: Date.now(),
        items: items
      }));
    } catch (_unused2) {}
    return items;
  }).finally(function () {
    return ON_THIS_DAY_REQUESTS.delete(key);
  });
  ON_THIS_DAY_REQUESTS.set(key, request);
  return request;
}
function CalendarDaySheet(_ref18) {
  var dateKey = _ref18.dateKey,
    onClose = _ref18.onClose,
    active = _ref18.active,
    index = _ref18.index,
    onActiveChange = _ref18.onActiveChange,
    onIndexChange = _ref18.onIndexChange;
  var _React$useState = React.useState({
      holidays: [],
      events: [],
      people: []
    }),
    _React$useState2 = _slicedToArray(_React$useState, 2),
    remote = _React$useState2[0],
    setRemote = _React$useState2[1];
  var _React$useState3 = React.useState(active === "china" ? "ready" : "loading"),
    _React$useState4 = _slicedToArray(_React$useState3, 2),
    status = _React$useState4[0],
    setStatus = _React$useState4[1];
  var marker = getCalendarMarker(dateKey);
  var _dateKey$split = dateKey.split("-"),
    _dateKey$split2 = _slicedToArray(_dateKey$split, 3),
    month = _dateKey$split2[1],
    day = _dateKey$split2[2];
  var dateLabel = "".concat(Number(month), "\u6708").concat(Number(day), "\u65E5");
  React.useEffect(function () {
    if (active === "china") {
      setStatus("ready");
      return undefined;
    }
    var activeRequest = true;
    var cached = readArchiveCache(active, month, day);
    if (cached) {
      setRemote(function (current) {
        return _objectSpread(_objectSpread({}, current), {}, _defineProperty({}, active, cached));
      });
      setStatus("ready");
    } else {
      setStatus("loading");
    }
    loadArchiveCategory(active, month, day).then(function (items) {
      if (!activeRequest) return;
      setRemote(function (current) {
        return _objectSpread(_objectSpread({}, current), {}, _defineProperty({}, active, items));
      });
      setStatus("ready");
    }).catch(function () {
      if (activeRequest) setStatus(function (current) {
        return current === "ready" ? current : "offline";
      });
    });
    return function () {
      activeRequest = false;
    };
  }, [active, month, day]);
  var sections = _objectSpread({
    china: [{
      text: marker.full,
      year: null,
      source: marker.source
    }]
  }, remote);
  var items = sections[active] || [];
  var current = items.length ? items[index % items.length] : null;
  var selectTab = function selectTab(tab) {
    onActiveChange(tab);
    onIndexChange(0);
  };
  return React.createElement(Sheet, {
    onClose: onClose,
    labelledBy: "day-archive-title"
  }, React.createElement("div", {
    className: "day-archive-head"
  }, React.createElement("div", null, React.createElement("p", {
    className: "day-archive-date"
  }, dateLabel), React.createElement("h2", {
    className: "sheet-title",
    id: "day-archive-title"
  }, "\u4ECA\u5929\u662F\u4EC0\u4E48\u65E5\u5B50")), React.createElement("button", {
    className: "sheet-close",
    "aria-label": "\u5173\u95ED",
    onClick: onClose
  }, React.createElement(Icon, {
    name: "close"
  }))), React.createElement("div", {
    className: "day-archive-tabs",
    role: "tablist",
    "aria-label": "\u65E5\u671F\u5185\u5BB9\u5206\u7C7B"
  }, DAY_ARCHIVE_TABS.map(function (tab) {
    return React.createElement("button", {
      role: "tab",
      "aria-selected": active === tab.id,
      className: active === tab.id ? "active" : "",
      onClick: function onClick() {
        return selectTab(tab.id);
      },
      key: tab.id
    }, tab.label);
  })), React.createElement("div", {
    className: "day-archive-card",
    "aria-live": "polite"
  }, current ? React.createElement(React.Fragment, null, current.kind ? React.createElement("span", {
    className: "day-archive-kind"
  }, current.kind) : null, current.year ? React.createElement("span", {
    className: "day-archive-year"
  }, current.year > 0 ? "".concat(current.year, " \u5E74") : "\u516C\u5143\u524D ".concat(Math.abs(current.year), " \u5E74")) : null, React.createElement("p", {
    className: "day-archive-story"
  }, current.text), current.url ? React.createElement("a", {
    className: "day-archive-source source-link",
    href: current.url,
    target: "_blank",
    rel: "noreferrer"
  }, current.source, React.createElement(Icon, {
    name: "chevronRight",
    size: 12
  })) : React.createElement("span", {
    className: "day-archive-source"
  }, current.source)) : React.createElement("p", {
    className: "day-archive-empty"
  }, status === "loading" ? "正在读取全球日期档案…" : "当前分类暂时没有中文条目")), React.createElement("div", {
    className: "day-archive-actions"
  }, React.createElement("span", null, current ? "".concat(index % items.length + 1, " / ").concat(items.length) : status === "offline" ? "离线" : "联网"), React.createElement("button", {
    className: "secondary-button day-archive-next",
    disabled: items.length < 2,
    onClick: function onClick() {
      return onIndexChange((index + 1) % items.length);
    }
  }, React.createElement(Icon, {
    name: "refresh",
    size: 16
  }), "\u6362\u4E00\u6761")));
}
function CriticalScreen(_ref19) {
  var tasks = _ref19.tasks,
    onToggle = _ref19.onToggle,
    onOpen = _ref19.onOpen,
    onDelete = _ref19.onDelete,
    onMenu = _ref19.onMenu,
    onOpenReminders = _ref19.onOpenReminders;
  var withDDL = tasks.filter(function (task) {
    return task.deadline;
  });
  var withoutDDL = tasks.filter(function (task) {
    return !task.deadline;
  });
  return React.createElement("main", {
    className: "screen critical-screen"
  }, React.createElement("div", {
    className: "fold-divider",
    "aria-hidden": "true"
  }), React.createElement("div", {
    className: "top-row"
  }, React.createElement(IconButton, {
    name: "menu",
    label: "\u66F4\u591A",
    onClick: onMenu
  }), React.createElement("button", {
    className: "view-trigger pressable",
    type: "button",
    "aria-label": "\u6253\u5F00\u5173\u952E\u63D0\u9192\u8BBE\u7F6E",
    onClick: onOpenReminders
  }, React.createElement(Icon, {
    name: "bell",
    size: 16
  }))), React.createElement("div", {
    className: "page-title-row"
  }, React.createElement("h1", {
    className: "page-title"
  }, "\u5173\u952E\u4E8B\u9879"), React.createElement("div", {
    className: "page-meta"
  }, tasks.length, " \u4EF6")), React.createElement("div", {
    className: "critical-responsive-grid"
  }, React.createElement("section", {
    className: "critical-pane critical-with-ddl",
    "aria-label": "\u6709 DDL \u7684\u5173\u952E\u4E8B\u9879"
  }, React.createElement(SectionHeader, {
    title: "\u6709 DDL"
  }), withDDL.some(function (task) {
    return task.demo;
  }) ? React.createElement("div", {
    className: "demo-guide"
  }, "\u5DE6\u6ED1\u5220\u9664\u6F14\u793A\uFF0C\u70B9\u4E0B\u65B9\u8BED\u97F3\u952E\u521B\u5EFA\u7B2C\u4E00\u4E2A DDL") : null, React.createElement("div", {
    className: "critical-stack"
  }, withDDL.map(function (task) {
    return React.createElement(CriticalTaskRow, {
      task: task,
      onToggle: onToggle,
      onOpen: onOpen,
      onDelete: onDelete,
      key: task.id
    });
  })), !withDDL.length ? React.createElement("div", {
    className: "empty-guide"
  }, "\u70B9\u4E0B\u65B9\u8BED\u97F3\u952E\uFF0C\u521B\u5EFA\u7B2C\u4E00\u4E2A DDL") : null), React.createElement("section", {
    className: "critical-pane critical-without-ddl",
    "aria-label": "\u65E0 DDL \u7684\u5173\u952E\u4E8B\u9879"
  }, React.createElement(SectionHeader, {
    title: "\u65E0 DDL",
    note: "\u957F\u671F\u5173\u6CE8"
  }), React.createElement("div", {
    className: "critical-stack"
  }, withoutDDL.map(function (task) {
    return React.createElement(CriticalTaskRow, {
      task: task,
      onToggle: onToggle,
      onOpen: onOpen,
      onDelete: onDelete,
      key: task.id
    });
  })))));
}
function Waveform() {
  return React.createElement("div", {
    className: "waveform",
    "aria-hidden": "true"
  }, [11, 20, 26, 16, 24, 18, 9].map(function (height, index) {
    return React.createElement("span", {
      key: index,
      style: {
        height: height
      }
    });
  }));
}
function VoiceComposer(_ref20) {
  var _editableTask$title;
  var phase = _ref20.phase,
    transcript = _ref20.transcript,
    parsedCommand = _ref20.parsedCommand,
    draftTask = _ref20.draftTask,
    onDraftTaskChange = _ref20.onDraftTaskChange,
    onTranscript = _ref20.onTranscript,
    onStop = _ref20.onStop,
    onUseInputMethod = _ref20.onUseInputMethod,
    onConfirm = _ref20.onConfirm,
    onClose = _ref20.onClose,
    speechAvailable = _ref20.speechAvailable,
    speechStatus = _ref20.speechStatus,
    sounds = _ref20.sounds,
    defaultAlertMode = _ref20.defaultAlertMode,
    defaultSoundId = _ref20.defaultSoundId,
    onPreviewSound = _ref20.onPreviewSound,
    onImportSound = _ref20.onImportSound,
    onPickSystemAlarm = _ref20.onPickSystemAlarm;
  var text = transcript.trim();
  var editableTask = draftTask || parsedCommand.task;
  var updateDraft = function updateDraft(field, value) {
    return onDraftTaskChange(_objectSpread(_objectSpread({}, draftTask || parsedCommand.task), {}, _defineProperty({}, field, value)));
  };
  var updateRepeat = function updateRepeat(repeatDays, repeat) {
    return onDraftTaskChange(_objectSpread(_objectSpread({}, draftTask || parsedCommand.task), {}, {
      repeatDays: repeatDays,
      repeat: repeat,
      hasRepeat: repeatDays.length > 0
    }));
  };
  var updateType = function updateType(type) {
    var next = _objectSpread(_objectSpread({}, draftTask || parsedCommand.task), {}, {
      type: type
    });
    if (type === "critical") {
      var plan = normalizeCriticalReminderPlan(_objectSpread(_objectSpread({}, next), {}, {
        reminderEnabled: Boolean(next.deadline)
      }));
      onDraftTaskChange(_objectSpread(_objectSpread(_objectSpread({}, next), plan), {}, {
        deadlineTime: next.hasTime && next.time !== "待定" ? next.time : null,
        reminder: getCriticalReminder(_objectSpread(_objectSpread({}, next), plan))
      }));
    } else onDraftTaskChange(next);
  };
  var updateCriticalDeadlineTime = function updateCriticalDeadlineTime(deadlineTime) {
    return onDraftTaskChange(_objectSpread(_objectSpread({}, draftTask || parsedCommand.task), {}, {
      deadlineTime: deadlineTime || null,
      time: deadlineTime || "待定",
      hasTime: Boolean(deadlineTime)
    }));
  };
  var confirm = function confirm() {
    if (parsedCommand.intent === "create") {
      onConfirm(_objectSpread(_objectSpread({}, parsedCommand), {}, {
        heading: editableTask.title.trim(),
        task: _objectSpread(_objectSpread({}, editableTask), {}, {
          title: editableTask.title.trim()
        })
      }));
    } else {
      onConfirm(parsedCommand);
    }
  };
  var canConfirm = parsedCommand.valid && (parsedCommand.intent !== "create" || Boolean(editableTask === null || editableTask === void 0 || (_editableTask$title = editableTask.title) === null || _editableTask$title === void 0 ? void 0 : _editableTask$title.trim()));
  var statusText = {
    starting: "正在启动离线语音…",
    "requesting-permission": "等待麦克风授权…",
    "preparing-model": "正在准备离线中文模型…",
    "model-ready": "离线中文模型已就绪…",
    listening: "正在听…",
    processing: "正在处理…",
    "permission-denied": "麦克风权限未开启",
    error: "离线语音组件启动失败"
  }[speechStatus] || "正在听…";
  return React.createElement(Sheet, {
    onClose: onClose,
    labelledBy: phase === "listening" ? "voice-title" : undefined,
    label: phase === "review" ? "确认语音指令" : undefined
  }, phase === "listening" ? React.createElement(React.Fragment, null, React.createElement("h2", {
    className: "sheet-title",
    id: "voice-title"
  }, "\u4ECA\u523B\u52A9\u624B"), React.createElement("div", {
    className: "voice-stage"
  }, React.createElement("button", {
    className: "voice-orbit pressable",
    onClick: onStop,
    "aria-label": "\u505C\u6B62\u5E76\u5904\u7406"
  }, React.createElement(Waveform, null)), React.createElement("div", {
    className: "voice-transcript"
  }, text || statusText)), React.createElement("input", {
    className: "composer-input",
    value: transcript,
    onFocus: onUseInputMethod,
    onChange: function onChange(event) {
      return onTranscript(event.target.value);
    },
    placeholder: "\u8F93\u5165\u64CD\u4F5C\u6216\u5B89\u6392",
    "aria-label": speechAvailable ? "输入操作或安排" : "语音不可用，请输入操作或安排"
  }), React.createElement("div", {
    className: "button-row"
  }, React.createElement("button", {
    className: "secondary-button pressable",
    onClick: onClose
  }, "\u53D6\u6D88"), React.createElement("button", {
    className: "primary-button pressable",
    onClick: onStop
  }, "\u505C\u6B62\u5E76\u5904\u7406"))) : React.createElement(React.Fragment, null, parsedCommand.intent === "create" && editableTask !== null && editableTask !== void 0 && editableTask.span ? React.createElement("div", {
    className: "edit-form voice-edit-form edit-form-first"
  }, React.createElement("label", {
    className: "edit-field"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u540D\u79F0"), React.createElement("input", {
    className: "edit-input title-input",
    value: editableTask.title,
    onChange: function onChange(event) {
      return updateDraft("title", event.target.value);
    }
  })), React.createElement("div", {
    className: "parsed-card"
  }, React.createElement("div", {
    className: "field-list"
  }, React.createElement("div", {
    className: "field-row"
  }, React.createElement("span", {
    className: "field-label"
  }, "\u53BB\u7A0B DDL"), React.createElement("span", {
    className: "field-value"
  }, editableTask.span.start.deadline)), React.createElement("div", {
    className: "field-row"
  }, React.createElement("span", {
    className: "field-label"
  }, "\u8FD4\u7A0B DDL"), React.createElement("span", {
    className: "field-value"
  }, editableTask.span.end.deadline)))), React.createElement("label", {
    className: "edit-field stacked"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u5907\u6CE8"), React.createElement("textarea", {
    className: "edit-input edit-textarea",
    rows: "2",
    value: editableTask.note || "",
    onChange: function onChange(event) {
      return updateDraft("note", event.target.value);
    }
  }))) : parsedCommand.intent === "create" && editableTask ? React.createElement("div", {
    className: "edit-form voice-edit-form edit-form-first"
  }, React.createElement("label", {
    className: "edit-field"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u540D\u79F0"), React.createElement("input", {
    className: "edit-input title-input",
    value: editableTask.title,
    onChange: function onChange(event) {
      return updateDraft("title", event.target.value);
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u7C7B\u578B"), React.createElement(SegmentedChoice, {
    label: "\u4EFB\u52A1\u7C7B\u578B",
    value: editableTask.type,
    options: [["daily", "日常事务"], ["critical", "关键事务"]],
    onChange: updateType
  })), editableTask.type === "daily" ? React.createElement(React.Fragment, null, React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(TimePicker, {
    label: "\u65F6\u95F4",
    value: editableTask.time === "待定" ? null : editableTask.time,
    onChange: function onChange(time) {
      return updateDraft("time", time || "待定");
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(TimePicker, {
    label: "\u7ED3\u675F",
    value: editableTask.endTime || null,
    onChange: function onChange(time) {
      return updateDraft("endTime", time || null);
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(WeekdayPicker, {
    value: editableTask.repeat,
    repeatDays: editableTask.repeatDays,
    onChange: updateRepeat
  }))) : React.createElement(React.Fragment, null, React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(DatePicker, {
    value: editableTask.deadline,
    onChange: function onChange(deadline) {
      return updateDraft("deadline", deadline);
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(TimePicker, {
    label: "\u622A\u6B62\u65F6\u523B",
    value: editableTask.deadlineTime || (editableTask.time === "待定" ? null : editableTask.time),
    onChange: updateCriticalDeadlineTime
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(CriticalReminderPlanPicker, {
    task: editableTask,
    onChange: onDraftTaskChange
  }))), editableTask.type === "daily" ? React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(ReminderPicker, {
    value: editableTask.reminder || "到点提醒",
    onChange: function onChange(reminder) {
      return updateDraft("reminder", reminder);
    }
  })) : null, React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(ReminderSoundPicker, {
    task: editableTask,
    sounds: sounds,
    defaultAlertMode: defaultAlertMode,
    defaultSoundId: defaultSoundId,
    onChange: onDraftTaskChange,
    onPreview: onPreviewSound,
    onImport: onImportSound,
    onPickSystemAlarm: onPickSystemAlarm
  })), React.createElement("label", {
    className: "edit-field stacked"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u5907\u6CE8"), React.createElement("textarea", {
    className: "edit-input edit-textarea",
    rows: "2",
    value: editableTask.note || "",
    onChange: function onChange(event) {
      return updateDraft("note", event.target.value);
    }
  }))) : React.createElement("div", {
    className: "parsed-card parsed-card-first"
  }, React.createElement("div", {
    className: "parsed-title"
  }, parsedCommand.heading), React.createElement("div", {
    className: "field-list"
  }, parsedCommand.rows.map(function (_ref21, index) {
    var _ref22 = _slicedToArray(_ref21, 2),
      label = _ref22[0],
      value = _ref22[1];
    return React.createElement("div", {
      className: "field-row",
      key: "".concat(label, "-").concat(index)
    }, React.createElement("span", {
      className: "field-label"
    }, label), React.createElement("span", {
      className: "field-value"
    }, value));
  }))), !parsedCommand.valid ? React.createElement("p", {
    className: "voice-command-error",
    role: "alert"
  }, parsedCommand.error) : null, React.createElement("div", {
    className: "button-row"
  }, parsedCommand.intent !== "query" ? React.createElement("button", {
    className: "secondary-button pressable",
    onClick: onClose
  }, "\u53D6\u6D88") : null, React.createElement("button", {
    className: "primary-button accent pressable",
    onClick: confirm,
    disabled: !canConfirm
  }, parsedCommand.confirmLabel))));
}
function DailyEditSheet(_ref23) {
  var task = _ref23.task,
    draft = _ref23.draft,
    onDraftChange = _ref23.onDraftChange,
    onSave = _ref23.onSave,
    onClose = _ref23.onClose,
    sounds = _ref23.sounds,
    defaultAlertMode = _ref23.defaultAlertMode,
    defaultSoundId = _ref23.defaultSoundId,
    onPreviewSound = _ref23.onPreviewSound,
    onImportSound = _ref23.onImportSound,
    onPickSystemAlarm = _ref23.onPickSystemAlarm;
  if (!task || !draft) return null;
  var update = function update(field, value) {
    return onDraftChange(_objectSpread(_objectSpread({}, draft), {}, _defineProperty({}, field, value)));
  };
  var updateRepeat = function updateRepeat(repeatDays, repeat) {
    return onDraftChange(_objectSpread(_objectSpread({}, draft), {}, {
      repeatDays: repeatDays,
      repeat: repeat
    }));
  };
  return React.createElement(Sheet, {
    onClose: onClose,
    label: "\u7F16\u8F91\u4EFB\u52A1"
  }, React.createElement("div", {
    className: "edit-form edit-form-first"
  }, React.createElement("label", {
    className: "edit-field"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u540D\u79F0"), React.createElement("input", {
    className: "edit-input title-input",
    value: draft.title,
    onChange: function onChange(event) {
      return update("title", event.target.value);
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(TimePicker, {
    label: "\u65F6\u95F4",
    value: draft.time === "待定" ? null : draft.time,
    onChange: function onChange(time) {
      return update("time", time || "待定");
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(TimePicker, {
    label: "\u7ED3\u675F",
    value: draft.endTime || null,
    onChange: function onChange(time) {
      return update("endTime", time || null);
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(WeekdayPicker, {
    value: draft.repeat,
    repeatDays: draft.repeatDays,
    onChange: updateRepeat
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(ReminderPicker, {
    value: draft.reminder,
    onChange: function onChange(reminder) {
      return update("reminder", reminder);
    }
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(ReminderSoundPicker, {
    task: draft,
    sounds: sounds,
    defaultAlertMode: defaultAlertMode,
    defaultSoundId: defaultSoundId,
    onChange: onDraftChange,
    onPreview: onPreviewSound,
    onImport: onImportSound,
    onPickSystemAlarm: onPickSystemAlarm
  })), React.createElement("label", {
    className: "edit-field stacked"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u5907\u6CE8"), React.createElement("textarea", {
    className: "edit-input edit-textarea",
    rows: "3",
    value: draft.note,
    onChange: function onChange(event) {
      return update("note", event.target.value);
    }
  }))), React.createElement("div", {
    className: "button-row"
  }, React.createElement("button", {
    className: "secondary-button pressable",
    onClick: onClose
  }, "\u53D6\u6D88"), React.createElement("button", {
    className: "primary-button accent pressable",
    onClick: function onClick() {
      return onSave(task.id, _objectSpread(_objectSpread({}, draft), {}, {
        title: draft.title.trim()
      }));
    },
    disabled: !draft.title.trim()
  }, "\u4FDD\u5B58\u4FEE\u6539")));
}
function MoreSheet(_ref24) {
  var onClose = _ref24.onClose,
    onOpen = _ref24.onOpen,
    themeMode = _ref24.themeMode,
    onThemeChange = _ref24.onThemeChange;
  var rows = [["history", "历史记录", "history"], ["month", "月度复盘", "chart"], ["year", "年度复盘", "year"], ["permissions", "通知与权限", "bell"], ["voice", "语音模型", "spark"], ["version", "版本更新", "update"], ["settings", "设置", "settings"]];
  return React.createElement(Sheet, {
    onClose: onClose,
    label: "\u66F4\u591A\u83DC\u5355"
  }, React.createElement("div", {
    className: "theme-switch theme-switch-first",
    role: "group",
    "aria-label": "\u5916\u89C2"
  }, [['dark', '暗色'], ['system', '系统'], ['light', '亮色']].map(function (_ref25) {
    var _ref26 = _slicedToArray(_ref25, 2),
      mode = _ref26[0],
      label = _ref26[1];
    return React.createElement("button", {
      className: "theme-option ".concat(themeMode === mode ? "active" : ""),
      "aria-pressed": themeMode === mode,
      key: mode,
      onClick: function onClick() {
        return onThemeChange(mode);
      }
    }, label);
  })), React.createElement("div", {
    className: "menu-list"
  }, rows.map(function (_ref27) {
    var _ref28 = _slicedToArray(_ref27, 3),
      id = _ref28[0],
      title = _ref28[1],
      icon = _ref28[2];
    return React.createElement("button", {
      className: "menu-row",
      key: id,
      onClick: function onClick() {
        return onOpen(id);
      }
    }, React.createElement("span", {
      className: "menu-icon"
    }, React.createElement(Icon, {
      name: icon,
      size: 18
    })), React.createElement("span", {
      className: "menu-title"
    }, title), React.createElement(Icon, {
      name: "chevronRight",
      size: 17
    }));
  })));
}
function CriticalDetailSheet(_ref29) {
  var _draft$progress;
  var task = _ref29.task,
    draft = _ref29.draft,
    renewDays = _ref29.renewDays,
    onRenewDaysChange = _ref29.onRenewDaysChange,
    onDraftChange = _ref29.onDraftChange,
    onClose = _ref29.onClose,
    onRenew = _ref29.onRenew,
    onSave = _ref29.onSave,
    sounds = _ref29.sounds,
    defaultAlertMode = _ref29.defaultAlertMode,
    defaultSoundId = _ref29.defaultSoundId,
    onPreviewSound = _ref29.onPreviewSound,
    onImportSound = _ref29.onImportSound,
    onPickSystemAlarm = _ref29.onPickSystemAlarm;
  if (!task || !draft) return null;
  var update = function update(field, value) {
    return onDraftChange(_objectSpread(_objectSpread({}, draft), {}, _defineProperty({}, field, value)));
  };
  var updateDeadlineTime = function updateDeadlineTime(deadlineTime) {
    return onDraftChange(_objectSpread(_objectSpread({}, draft), {}, {
      deadlineTime: deadlineTime || null,
      time: deadlineTime || null
    }));
  };
  var dueCopy = task.daysLeft === null ? "无 DDL" : task.daysLeft < 0 ? "\u903E\u671F ".concat(Math.abs(task.daysLeft), " \u5929") : task.daysLeft === 0 ? "今天截止" : "\u5269 ".concat(task.daysLeft, " \u5929");
  return React.createElement(Sheet, {
    onClose: onClose,
    label: "\u7F16\u8F91\u5173\u952E\u4E8B\u9879"
  }, React.createElement("div", {
    className: "top-row"
  }, React.createElement("span", {
    className: "days-left ".concat(task.daysLeft <= 0 ? "today" : "")
  }, dueCopy), React.createElement(IconButton, {
    name: "close",
    label: "\u5173\u95ED",
    onClick: onClose
  })), React.createElement("div", {
    className: "edit-form critical-edit-form"
  }, React.createElement("label", {
    className: "edit-field"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u540D\u79F0"), React.createElement("input", {
    className: "edit-input title-input",
    value: draft.title,
    onChange: function onChange(event) {
      return update("title", event.target.value);
    }
  })), task.done ? React.createElement("div", {
    className: "edit-field stacked custom-control-field completion-date-field",
    "aria-label": "\u8BBE\u7F6E\u5B8C\u6210\u65E5\u671F"
  }, React.createElement(CompletionDatePicker, {
    value: draft.completedDateKey,
    onChange: function onChange(completedDateKey) {
      return update("completedDateKey", completedDateKey);
    }
  })) : null, React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(DatePicker, {
    value: draft.deadline,
    onChange: function onChange(deadline) {
      return update("deadline", deadline);
    },
    allowPast: task.done
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(TimePicker, {
    label: "\u622A\u6B62\u65F6\u523B",
    value: draft.deadlineTime || draft.time,
    onChange: updateDeadlineTime
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(CriticalReminderPlanPicker, {
    task: draft,
    onChange: onDraftChange
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement(ReminderSoundPicker, {
    task: draft,
    sounds: sounds,
    defaultAlertMode: defaultAlertMode,
    defaultSoundId: defaultSoundId,
    onChange: onDraftChange,
    onPreview: onPreviewSound,
    onImport: onImportSound,
    onPickSystemAlarm: onPickSystemAlarm
  })), React.createElement("div", {
    className: "edit-field stacked custom-control-field"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u5B8C\u6210\u5EA6"), React.createElement(SegmentedChoice, {
    label: "\u5B8C\u6210\u5EA6",
    value: (_draft$progress = draft.progress) !== null && _draft$progress !== void 0 ? _draft$progress : 0,
    options: [0, 25, 50, 75, 100].map(function (value) {
      return [value, "".concat(value, "%")];
    }),
    onChange: function onChange(progress) {
      return update("progress", Number(progress));
    },
    compact: true
  })), React.createElement("label", {
    className: "edit-field stacked"
  }, React.createElement("span", {
    className: "edit-label"
  }, "\u5907\u6CE8"), React.createElement("textarea", {
    className: "edit-input edit-textarea",
    rows: "2",
    value: draft.note || "",
    onChange: function onChange(event) {
      return update("note", event.target.value);
    }
  }))), React.createElement("button", {
    className: "primary-button accent pressable save-wide",
    onClick: function onClick() {
      return onSave(task.id, _objectSpread(_objectSpread({}, draft), {}, {
        title: draft.title.trim()
      }));
    },
    disabled: !draft.title.trim()
  }, "\u4FDD\u5B58\u4FEE\u6539"), task.deadline ? React.createElement("div", {
    className: "renew-panel"
  }, React.createElement("div", {
    className: "renew-heading"
  }, React.createElement(Icon, {
    name: "refresh",
    size: 17
  }), React.createElement("span", null, "\u7EED\u671F"), React.createElement("small", null, "\u5728\u5F53\u524D\u622A\u6B62\u65E5\u671F\u540E\u5EF6\u957F")), React.createElement("div", {
    className: "renew-controls"
  }, React.createElement(Stepper, {
    label: "\u7EED\u671F\u5929\u6570",
    value: renewDays,
    min: 1,
    max: 365,
    onChange: onRenewDaysChange
  }), React.createElement("span", {
    className: "renew-days-unit"
  }, "\u5929"), React.createElement("button", {
    className: "secondary-button pressable renew-confirm",
    onClick: function onClick() {
      return onRenew(task.id, renewDays);
    }
  }, "\u786E\u8BA4\u7EED\u671F"))) : null);
}
function HistoryScreen(_ref30) {
  var items = _ref30.items,
    onBack = _ref30.onBack;
  return React.createElement("main", {
    className: "screen secondary"
  }, React.createElement(BackHeader, {
    title: "\u5386\u53F2\u8BB0\u5F55",
    onBack: onBack
  }), React.createElement("div", {
    className: "history-list"
  }, items.map(function (item) {
    return React.createElement("div", {
      className: "history-row",
      key: item.id
    }, React.createElement("span", {
      className: "history-check"
    }, React.createElement(Icon, {
      name: "check",
      size: 16,
      strokeWidth: 2.2
    })), React.createElement("div", null, React.createElement("div", {
      className: "history-title"
    }, item.title), React.createElement("div", {
      className: "history-meta"
    }, "\u5B8C\u6210\u4E8E ", item.completed)), React.createElement("span", {
      className: "history-days"
    }, "\u63D0\u524D ", item.leadDays, " \u5929"));
  }), !items.length ? React.createElement("div", {
    className: "empty-guide"
  }, "\u8FD8\u6CA1\u6709\u5DF2\u5B8C\u6210\u7684\u5173\u952E\u4E8B\u9879") : null));
}
function reportDateKey(date) {
  return "".concat(date.getFullYear(), "-").concat(String(date.getMonth() + 1).padStart(2, "0"), "-").concat(String(date.getDate()).padStart(2, "0"));
}
function reportPeriod(type, todayDateKey) {
  var _todayDateKey$split$m = todayDateKey.split("-").map(Number),
    _todayDateKey$split$m2 = _slicedToArray(_todayDateKey$split$m, 3),
    year = _todayDateKey$split$m2[0],
    month = _todayDateKey$split$m2[1],
    day = _todayDateKey$split$m2[2];
  var today = new Date(year, month - 1, day, 12);
  if (type === "month") {
    var start = new Date(today.getFullYear(), today.getMonth() - 1, 1, 12);
    var end = new Date(today.getFullYear(), today.getMonth(), 0, 12);
    return {
      start: start,
      end: end
    };
  }
  var targetYear = today.getFullYear() - 1;
  return {
    start: new Date(targetYear, 0, 1, 12),
    end: new Date(targetYear, 11, 31, 12)
  };
}
function buildReport(type, dailyTasks, dailyCompletionByDate, history, todayDateKey) {
  var _reportPeriod = reportPeriod(type, todayDateKey),
    start = _reportPeriod.start,
    end = _reportPeriod.end;
  var dateKeys = [];
  for (var cursor = new Date(start); cursor <= end; cursor.setDate(cursor.getDate() + 1)) dateKeys.push(reportDateKey(cursor));
  var ranking = dailyTasks.map(function (task) {
    var occurrences = dateKeys.filter(function (dateKey) {
      return window.taskOccursOnDate(task, dateKey, todayDateKey);
    });
    var done = occurrences.filter(function (dateKey) {
      return Boolean(dailyCompletionByDate["".concat(task.id, ":").concat(dateKey)]);
    }).length;
    return {
      label: task.title,
      value: occurrences.length ? Math.round(done / occurrences.length * 100) : 0,
      detail: "".concat(done, "/").concat(occurrences.length),
      total: occurrences.length
    };
  }).filter(function (item) {
    return item.total > 0;
  }).sort(function (left, right) {
    return right.value - left.value || right.total - left.total || left.label.localeCompare(right.label);
  });
  var startKey = reportDateKey(start);
  var endKey = reportDateKey(end);
  var ddlRanking = history.filter(function (item) {
    return !item.completedDateKey || item.completedDateKey >= startKey && item.completedDateKey <= endKey;
  }).sort(function (left, right) {
    return Number(right.leadDays || 0) - Number(left.leadDays || 0);
  }).map(function (item, index) {
    return {
      rank: index + 1,
      title: item.title,
      days: Number(item.leadDays || 0)
    };
  });
  return {
    ranking: ranking,
    ddlRanking: ddlRanking
  };
}
function ReportScreen(_ref31) {
  var type = _ref31.type,
    _ref31$dailyTasks = _ref31.dailyTasks,
    dailyTasks = _ref31$dailyTasks === void 0 ? [] : _ref31$dailyTasks,
    _ref31$dailyCompletio = _ref31.dailyCompletionByDate,
    dailyCompletionByDate = _ref31$dailyCompletio === void 0 ? {} : _ref31$dailyCompletio,
    _ref31$history = _ref31.history,
    history = _ref31$history === void 0 ? [] : _ref31$history,
    todayDateKey = _ref31.todayDateKey,
    onBack = _ref31.onBack;
  var monthly = type === "month";
  var report = buildReport(type, dailyTasks, dailyCompletionByDate, history, todayDateKey || APP_DATA.today.dateKey);
  var hasData = report.ranking.length > 0 || report.ddlRanking.length > 0;
  return React.createElement("main", {
    className: "screen secondary"
  }, React.createElement(BackHeader, {
    title: monthly ? "月度复盘" : "年度复盘",
    onBack: onBack
  }), hasData ? React.createElement(React.Fragment, null, React.createElement(SectionHeader, {
    title: "\u5B8C\u6210\u6BD4\u4F8B\u6392\u540D",
    note: monthly ? "上月" : "全年"
  }), React.createElement("div", {
    className: "ranking-list"
  }, report.ranking.map(function (item) {
    return React.createElement(BarRow, {
      item: item,
      key: item.label
    });
  })), React.createElement(SectionHeader, {
    title: "\u6700\u957F\u63D0\u524D\u5B8C\u6210\u7684 DDL",
    note: monthly ? "前 5" : "前 10"
  }), React.createElement("div", null, report.ddlRanking.slice(0, monthly ? 5 : 10).map(function (item) {
    return React.createElement("div", {
      className: "ddl-rank",
      key: "".concat(item.rank, "-").concat(item.title)
    }, React.createElement("span", {
      className: "rank-number"
    }, String(item.rank).padStart(2, "0")), React.createElement("span", {
      className: "rank-title"
    }, item.title), React.createElement("span", {
      className: "rank-days"
    }, "\u63D0\u524D ", item.days, " \u5929"));
  }))) : React.createElement("div", {
    className: "empty-guide report-empty"
  }, "\u5B8C\u6210\u4EFB\u52A1\u540E\uFF0C\u8FD9\u91CC\u4F1A\u751F\u6210\u771F\u5B9E\u590D\u76D8"));
}
function DeleteConfirmSheet(_ref32) {
  var target = _ref32.target,
    selectedDateKey = _ref32.selectedDateKey,
    onClose = _ref32.onClose,
    onConfirm = _ref32.onConfirm;
  if (!(target !== null && target !== void 0 && target.task)) return null;
  var isDaily = target.kind === "daily";
  var selectedMeta = selectedDateKey ? getDateMeta(selectedDateKey) : null;
  return React.createElement(Sheet, {
    onClose: onClose,
    label: "\u786E\u8BA4\u5220\u9664\u4EFB\u52A1"
  }, React.createElement("div", {
    className: "delete-confirm-title"
  }, target.task.title), isDaily ? React.createElement("div", {
    className: "delete-choice-list"
  }, React.createElement("button", {
    className: "delete-choice pressable",
    type: "button",
    onClick: function onClick() {
      return onConfirm("future");
    }
  }, React.createElement("span", null, "\u4EC5\u5220\u9664\u540E\u7EED\u5B89\u6392"), React.createElement("small", null, "\u4FDD\u7559 ", selectedMeta ? "".concat(selectedMeta.month, "\u6708").concat(selectedMeta.date, "\u65E5") : "所选日期", "\u4E4B\u524D\u7684\u8BB0\u5F55\u4E0E\u7EDF\u8BA1")), React.createElement("button", {
    className: "delete-choice destructive pressable",
    type: "button",
    onClick: function onClick() {
      return onConfirm("all");
    }
  }, React.createElement("span", null, "\u5220\u9664\u5168\u90E8\u8BB0\u5F55"), React.createElement("small", null, "\u6E05\u9664\u8FC7\u53BB\u5B8C\u6210\u8BB0\u5F55\uFF0C\u5E76\u4ECE\u6708\u5EA6\u4E0E\u5E74\u5EA6\u590D\u76D8\u79FB\u9664")), React.createElement("button", {
    className: "secondary-button pressable save-wide",
    type: "button",
    onClick: onClose
  }, "\u53D6\u6D88")) : React.createElement("div", {
    className: "button-row"
  }, React.createElement("button", {
    className: "secondary-button pressable",
    type: "button",
    onClick: onClose
  }, "\u53D6\u6D88"), React.createElement("button", {
    className: "primary-button accent pressable",
    type: "button",
    onClick: function onClick() {
      return onConfirm("all");
    }
  }, "\u5220\u9664")));
}
function PermissionsScreen(_ref33) {
  var capabilities = _ref33.capabilities,
    onOpenCapability = _ref33.onOpenCapability,
    onBack = _ref33.onBack;
  var known = Boolean(capabilities);
  var state = function state(value) {
    var yes = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "已开启";
    var no = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "未开启";
    return known ? value ? yes : no : "仅手机检测";
  };
  var rows = [{
    key: "microphone",
    title: "麦克风",
    note: "语音识别与助手指令",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.microphone, "已授权", "未授权"),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.microphone
  }, {
    key: null,
    title: "离线中文识别",
    note: "Vosk 中文模型随 APK 内置",
    status: known ? capabilities !== null && capabilities !== void 0 && capabilities.offlineSpeechReady ? "已加载" : capabilities !== null && capabilities !== void 0 && capabilities.offlineSpeechBundled ? "已内置" : "组件缺失" : "APK 内置",
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.offlineSpeechBundled
  }, {
    key: "notifications",
    title: "通知",
    note: "锁屏和通知中心提醒",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.notifications),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.notifications
  }, {
    key: "exactAlarm",
    title: "精确闹钟",
    note: "按设定时间触发日常与 DDL 提醒",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.exactAlarm),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.exactAlarm
  }, {
    key: "fullScreenIntent",
    title: "锁屏提醒",
    note: "息屏时亮屏并显示任务",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.fullScreenIntent),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.fullScreenIntent
  }, {
    key: "background",
    title: "ColorOS 后台运行",
    note: "自启动和后台活动由 ColorOS 管理",
    status: capabilities !== null && capabilities !== void 0 && capabilities.backgroundConfigured ? "已配置" : "点击管理",
    ok: true
  }, {
    key: "battery",
    title: "电池优化",
    note: "后台提醒不被系统休眠",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.batteryUnrestricted, "不限制", "受限制"),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.batteryUnrestricted
  }, {
    key: "installUpdates",
    title: "安装更新",
    note: "从 GitHub 安装新版 APK",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.installUpdates, "已允许", "未允许"),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.installUpdates
  }, {
    key: null,
    title: "联网",
    note: "节日资料和版本检测",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.network, "可用", "当前离线"),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.network
  }, {
    key: null,
    title: "开机恢复提醒",
    note: "重启后重新安排全部任务闹钟",
    status: state(capabilities === null || capabilities === void 0 ? void 0 : capabilities.bootRestore, "已内置", "组件缺失"),
    ok: capabilities === null || capabilities === void 0 ? void 0 : capabilities.bootRestore
  }];
  return React.createElement("main", {
    className: "screen secondary"
  }, React.createElement(BackHeader, {
    title: "\u6743\u9650\u4E0E\u7EC4\u4EF6",
    onBack: onBack
  }), React.createElement("div", {
    className: "notice-preview"
  }, React.createElement("div", {
    className: "notice-app"
  }, React.createElement("span", {
    className: "notice-mark"
  }, React.createElement("img", {
    src: "./assets/app-icon-512.png",
    alt: ""
  })), "\u4ECA\u523B \xB7 \u73B0\u5728"), React.createElement("div", {
    className: "notice-title"
  }, "\u5199\u4ECA\u65E5\u65E5\u5FD7"), React.createElement("div", {
    className: "notice-copy"
  }, "\u8BB0\u5F55\u4ECA\u5929\u5B8C\u6210\u4E86\u4EC0\u4E48\uFF0C\u8FD8\u6709\u4EC0\u4E48\u9700\u8981\u8C03\u6574\u3002")), React.createElement("div", {
    style: {
      marginTop: 17
    }
  }, rows.map(function (row) {
    var content = React.createElement(React.Fragment, null, React.createElement("div", null, React.createElement("div", {
      className: "permission-title"
    }, row.title), React.createElement("div", {
      className: "permission-note"
    }, row.note)), React.createElement("span", {
      className: "status-badge ".concat(row.ok ? "" : "needs-action")
    }, row.status));
    return row.key ? React.createElement("button", {
      type: "button",
      className: "permission-row permission-action",
      onClick: function onClick() {
        return onOpenCapability(row.key);
      },
      key: row.title
    }, content) : React.createElement("div", {
      className: "permission-row",
      key: row.title
    }, content);
  })));
}
function SettingsScreen(_ref34) {
  var alertMode = _ref34.alertMode,
    defaultSoundId = _ref34.defaultSoundId,
    sounds = _ref34.sounds,
    onAlertModeChange = _ref34.onAlertModeChange,
    onDefaultSoundChange = _ref34.onDefaultSoundChange,
    onPreviewSound = _ref34.onPreviewSound,
    onImportSound = _ref34.onImportSound,
    onPickSystemAlarm = _ref34.onPickSystemAlarm,
    onOpenPermissions = _ref34.onOpenPermissions,
    onBack = _ref34.onBack;
  var builtInSounds = sounds.filter(function (sound) {
    return sound.source === "built-in";
  });
  var systemAlarmSounds = sounds.filter(function (sound) {
    return sound.source === "system-alarm";
  });
  var localSounds = sounds.filter(function (sound) {
    return sound.source === "local";
  });
  var selectSound = function selectSound(id) {
    onDefaultSoundChange(id);
    if (alertMode === "silent") onAlertModeChange("sound");
  };
  return React.createElement("main", {
    className: "screen secondary settings-screen"
  }, React.createElement(BackHeader, {
    title: "\u8BBE\u7F6E",
    onBack: onBack
  }), React.createElement("section", {
    className: "settings-section"
  }, React.createElement("div", {
    className: "settings-section-title"
  }, "\u9ED8\u8BA4\u63D0\u9192\u65B9\u5F0F"), React.createElement(SegmentedChoice, {
    label: "\u9ED8\u8BA4\u63D0\u9192\u65B9\u5F0F",
    value: alertMode,
    options: [["sound", "响铃提醒"], ["silent", "静音提醒"]],
    onChange: onAlertModeChange
  })), React.createElement("section", {
    className: "settings-section ".concat(alertMode === "silent" ? "muted" : "")
  }, React.createElement("div", {
    className: "settings-section-title"
  }, "\u9ED8\u8BA4\u97F3\u6548"), React.createElement("div", {
    className: "sound-library-section settings-sound-list"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u63D0\u793A\u97F3"), React.createElement("div", {
    className: "sound-choice-list"
  }, builtInSounds.map(function (sound) {
    return React.createElement(SoundChoice, {
      sound: sound,
      selected: defaultSoundId === sound.id,
      disabled: alertMode === "silent",
      onSelect: function onSelect() {
        return onDefaultSoundChange(sound.id);
      },
      onPreview: function onPreview() {
        return onPreviewSound(sound.id);
      },
      key: sound.id
    });
  }))), React.createElement("div", {
    className: "sound-library-section system-alarm-library"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u95F9\u94C3\u5E93"), React.createElement("button", {
    type: "button",
    className: "sound-library-entry pressable",
    onClick: function onClick() {
      return onPickSystemAlarm(selectSound, defaultSoundId);
    }
  }, React.createElement("span", null, React.createElement("strong", null, "\u624B\u673A\u7CFB\u7EDF\u95F9\u94C3"), React.createElement("small", null, "\u8C03\u7528 ColorOS \u95F9\u94C3\u5E93\u4E0E\u95F9\u949F\u97F3\u91CF")), React.createElement("b", null, "\u8FDB\u5165\u9009\u62E9")), systemAlarmSounds.length ? React.createElement("div", {
    className: "sound-choice-list"
  }, systemAlarmSounds.map(function (sound) {
    return React.createElement(SoundChoice, {
      sound: sound,
      selected: defaultSoundId === sound.id,
      disabled: alertMode === "silent",
      onSelect: function onSelect() {
        return onDefaultSoundChange(sound.id);
      },
      onPreview: function onPreview() {
        return onPreviewSound(sound.id);
      },
      key: sound.id
    });
  })) : null), React.createElement("div", {
    className: "sound-library-section"
  }, React.createElement("div", {
    className: "sound-library-title"
  }, "\u672C\u5730\u97F3\u9891"), localSounds.length ? React.createElement("div", {
    className: "sound-choice-list"
  }, localSounds.map(function (sound) {
    return React.createElement(SoundChoice, {
      sound: sound,
      selected: defaultSoundId === sound.id,
      disabled: alertMode === "silent",
      onSelect: function onSelect() {
        return onDefaultSoundChange(sound.id);
      },
      onPreview: function onPreview() {
        return onPreviewSound(sound.id);
      },
      key: sound.id
    });
  })) : null, React.createElement("button", {
    type: "button",
    className: "sound-import-button pressable",
    onClick: function onClick() {
      return onImportSound(selectSound);
    }
  }, "\uFF0B \u5BFC\u5165\u672C\u5730\u97F3\u6548"))), React.createElement("button", {
    className: "permission-link pressable settings-permission-link",
    type: "button",
    onClick: onOpenPermissions
  }, React.createElement("span", {
    className: "permission-title"
  }, "\u901A\u77E5\u3001\u9501\u5C4F\u4E0E\u540E\u53F0\u6743\u9650"), React.createElement(Icon, {
    name: "chevronRight",
    size: 17
  })));
}
function CriticalReminderScreen(_ref35) {
  var tasks = _ref35.tasks,
    reminderTime = _ref35.reminderTime,
    onReminderTimeChange = _ref35.onReminderTimeChange,
    reminderMultiple = _ref35.reminderMultiple,
    onReminderMultipleChange = _ref35.onReminderMultipleChange,
    reminderFinalDays = _ref35.reminderFinalDays,
    onReminderFinalDaysChange = _ref35.onReminderFinalDaysChange,
    onOpenPermissions = _ref35.onOpenPermissions,
    onBack = _ref35.onBack;
  var reminderTasks = tasks.filter(function (task) {
    return task.deadline && shouldRemindCritical(task.daysLeft, task);
  });
  var summary = reminderTasks.length ? reminderTasks.map(function (task) {
    return "".concat(task.title).concat(task.daysLeft === 0 ? "今天截止" : "\u5269".concat(task.daysLeft, "\u5929"));
  }).join("；") : "今天没有到达提醒节点的关键事项。";
  return React.createElement("main", {
    className: "screen secondary critical-reminder-screen"
  }, React.createElement(BackHeader, {
    title: "\u5173\u952E\u63D0\u9192",
    onBack: onBack
  }), React.createElement("div", {
    className: "reminder-time-card"
  }, React.createElement("span", null, React.createElement("span", {
    className: "reminder-setting-title"
  }, "\u65B0\u4EFB\u52A1\u9ED8\u8BA4\u8BA1\u5212")), React.createElement(TimePicker, {
    label: "\u9ED8\u8BA4\u63D0\u9192\u65F6\u523B",
    value: reminderTime,
    allowUnset: false,
    onChange: function onChange(time) {
      return onReminderTimeChange(time || "10:00");
    }
  })), reminderTasks.length ? React.createElement("div", {
    className: "notice-preview ddl-notice-preview"
  }, React.createElement("div", {
    className: "notice-app"
  }, React.createElement("span", {
    className: "notice-mark"
  }, React.createElement("img", {
    src: "./assets/app-icon-512.png",
    alt: ""
  })), "\u4ECA\u523B \xB7 \u6309\u5404\u4EFB\u52A1\u8BA1\u5212"), React.createElement("div", {
    className: "notice-title"
  }, "\u5173\u952E\u4E8B\u9879 \xB7 ", reminderTasks.length, " \u9879"), React.createElement("div", {
    className: "notice-copy"
  }, summary)) : React.createElement("div", {
    className: "reminder-silent-card"
  }, React.createElement(Icon, {
    name: "bell",
    size: 18
  }), React.createElement("span", null, React.createElement("strong", null, "\u4ECA\u5929\u4E0D\u63D0\u9192"), React.createElement("small", null, "DDL \u4ECD\u4FDD\u7559\u5728\u4ECA\u65E5\u4E0E\u5173\u952E\u5217\u8868"))), React.createElement("div", {
    className: "reminder-rule-grid",
    "aria-label": "\u65B0\u5173\u952E\u4EFB\u52A1\u9ED8\u8BA4\u63D0\u9192\u9891\u7387"
  }, React.createElement("div", {
    className: "reminder-rule-field"
  }, React.createElement("span", {
    className: "reminder-rule-label"
  }, "\u500D\u6570\u8282\u70B9"), React.createElement("span", {
    className: "reminder-rule-value"
  }, "\u6BCF ", React.createElement(Stepper, {
    label: "\u9ED8\u8BA4\u95F4\u9694\u5929\u6570",
    value: reminderMultiple,
    min: 1,
    max: 30,
    onChange: onReminderMultipleChange
  }), " \u5929")), React.createElement("div", {
    className: "reminder-rule-field"
  }, React.createElement("span", {
    className: "reminder-rule-label"
  }, "\u4E34\u8FD1\u622A\u6B62"), React.createElement("span", {
    className: "reminder-rule-value"
  }, "\u6700\u540E ", React.createElement(Stepper, {
    label: "\u9ED8\u8BA4\u6700\u540E\u6BCF\u5929\u63D0\u9192\u5929\u6570",
    value: reminderFinalDays,
    min: 0,
    max: 30,
    onChange: onReminderFinalDaysChange
  }), " \u5929"))), React.createElement(SectionHeader, {
    title: "\u4ECA\u5929\u4F1A\u63D0\u9192",
    note: "".concat(reminderTasks.length, " \u9879")
  }), React.createElement("div", {
    className: "reminder-task-list"
  }, reminderTasks.map(function (task) {
    return React.createElement("div", {
      className: "reminder-task-row",
      key: task.id
    }, React.createElement("span", {
      className: "reminder-task-dot"
    }), React.createElement("span", {
      className: "reminder-task-title"
    }, task.title), React.createElement("span", {
      className: "reminder-task-days"
    }, task.daysLeft === 0 ? "今天" : "".concat(task.daysLeft, " \u5929")));
  }), !reminderTasks.length ? React.createElement("div", {
    className: "reminder-empty"
  }, "\u5C1A\u672A\u5230\u8FBE\u63D0\u9192\u8282\u70B9") : null), React.createElement("button", {
    className: "permission-link pressable",
    type: "button",
    onClick: onOpenPermissions
  }, React.createElement("span", {
    className: "permission-title"
  }, "\u7CFB\u7EDF\u901A\u77E5\u6743\u9650"), React.createElement(Icon, {
    name: "chevronRight",
    size: 17
  })));
}
var JINKE_GITHUB_REPOSITORY = "Junyingjun/jinke-coloros-calendar";
var JINKE_FALLBACK_VERSION = "1.1.2";
function normalizeVersion(value) {
  return String(value || "0.0.0").trim().replace(/^v/i, "").split("-")[0];
}
function compareVersions(left, right) {
  var a = normalizeVersion(left).split(".").map(function (part) {
    return Number(part) || 0;
  });
  var b = normalizeVersion(right).split(".").map(function (part) {
    return Number(part) || 0;
  });
  for (var index = 0; index < Math.max(a.length, b.length); index += 1) {
    if ((a[index] || 0) !== (b[index] || 0)) return (a[index] || 0) > (b[index] || 0) ? 1 : -1;
  }
  return 0;
}
function VersionScreen(_ref36) {
  var onBack = _ref36.onBack;
  var currentVersion = function () {
    try {
      var _window$JinkeAndroid, _window$JinkeAndroid$;
      return ((_window$JinkeAndroid = window.JinkeAndroid) === null || _window$JinkeAndroid === void 0 || (_window$JinkeAndroid$ = _window$JinkeAndroid.getAppVersion) === null || _window$JinkeAndroid$ === void 0 ? void 0 : _window$JinkeAndroid$.call(_window$JinkeAndroid)) || JINKE_FALLBACK_VERSION;
    } catch (_unused3) {
      return JINKE_FALLBACK_VERSION;
    }
  }();
  var _React$useState5 = React.useState({
      status: "idle",
      latest: "",
      apkUrl: "",
      error: ""
    }),
    _React$useState6 = _slicedToArray(_React$useState5, 2),
    state = _React$useState6[0],
    setState = _React$useState6[1];
  var checkVersion = function checkVersion() {
    setState({
      status: "checking",
      latest: "",
      apkUrl: "",
      error: ""
    });
    fetch("https://api.github.com/repos/".concat(JINKE_GITHUB_REPOSITORY, "/releases/latest"), {
      headers: {
        Accept: "application/vnd.github+json"
      }
    }).then(function (response) {
      if (!response.ok) throw new Error("GitHub ".concat(response.status));
      return response.json();
    }).then(function (release) {
      var latest = normalizeVersion(release.tag_name);
      var apk = (release.assets || []).find(function (asset) {
        return /\.apk$/i.test(asset.name);
      });
      var hasUpdate = compareVersions(latest, currentVersion) > 0;
      setState({
        status: hasUpdate ? "update" : "latest",
        latest: latest,
        apkUrl: (apk === null || apk === void 0 ? void 0 : apk.browser_download_url) || "",
        error: hasUpdate && !apk ? "最新版没有 APK 附件" : ""
      });
    }).catch(function () {
      return setState({
        status: "error",
        latest: "",
        apkUrl: "",
        error: "无法连接 GitHub，请稍后重试"
      });
    });
  };
  var installUpdate = function installUpdate() {
    if (!state.apkUrl) return;
    try {
      var _window$JinkeAndroid2;
      if ((_window$JinkeAndroid2 = window.JinkeAndroid) !== null && _window$JinkeAndroid2 !== void 0 && _window$JinkeAndroid2.installApk) window.JinkeAndroid.installApk(state.apkUrl);else window.open(state.apkUrl, "_blank", "noopener,noreferrer");
    } catch (_unused4) {
      window.location.href = state.apkUrl;
    }
  };
  var statusCopy = state.status === "checking" ? "正在检查…" : state.status === "latest" ? "已是最新版" : state.status === "update" ? "\u53D1\u73B0 v".concat(state.latest) : state.status === "error" ? state.error : "尚未检查";
  return React.createElement("main", {
    className: "screen secondary version-screen"
  }, React.createElement(BackHeader, {
    title: "\u7248\u672C\u66F4\u65B0",
    onBack: onBack
  }), React.createElement("div", {
    className: "version-card"
  }, React.createElement("span", {
    className: "version-mark"
  }, React.createElement(Icon, {
    name: "update",
    size: 23
  })), React.createElement("div", null, React.createElement("span", {
    className: "version-name"
  }, "\u4ECA\u523B"), React.createElement("span", {
    className: "version-number"
  }, "v", normalizeVersion(currentVersion))), React.createElement("span", {
    className: "version-status ".concat(state.status)
  }, statusCopy)), React.createElement("button", {
    className: "primary-button accent pressable version-action",
    type: "button",
    disabled: state.status === "checking",
    onClick: state.status === "update" && state.apkUrl ? installUpdate : checkVersion
  }, state.status === "update" && state.apkUrl ? "\u4E0B\u8F7D\u5E76\u5B89\u88C5 v".concat(state.latest) : state.status === "checking" ? "检查中" : "检查更新"), state.error && state.status === "update" ? React.createElement("div", {
    className: "version-error"
  }, state.error) : null, React.createElement("a", {
    className: "github-release-link",
    href: "https://github.com/".concat(JINKE_GITHUB_REPOSITORY, "/releases"),
    target: "_blank",
    rel: "noreferrer"
  }, "GitHub Releases ", React.createElement(Icon, {
    name: "chevronRight",
    size: 15
  })));
}
function VoiceSettingsScreen(_ref37) {
  var capabilities = _ref37.capabilities,
    onBack = _ref37.onBack;
  var modelStatus = capabilities ? capabilities.offlineSpeechReady ? "已加载" : capabilities.offlineSpeechBundled ? "已内置" : "组件缺失" : "APK 内置";
  return React.createElement("main", {
    className: "screen secondary"
  }, React.createElement(BackHeader, {
    title: "\u8BED\u97F3\u6A21\u578B",
    onBack: onBack
  }), React.createElement("div", {
    className: "summary-hero"
  }, React.createElement("div", {
    className: "summary-kicker"
  }, "\u5F53\u524D\u65B9\u6848"), React.createElement("div", {
    className: "summary-value",
    style: {
      fontSize: 28
    }
  }, "Vosk Offline"), React.createElement("div", {
    className: "summary-caption"
  }, "\u7AEF\u4FA7\u666E\u901A\u8BDD\u6D41\u5F0F\u8BC6\u522B\uFF0C\u4E0D\u4F9D\u8D56\u7CFB\u7EDF\u8BED\u97F3\u670D\u52A1\u3002")), React.createElement(SectionHeader, {
    title: "\u8BC6\u522B\u80FD\u529B",
    note: "\u79BB\u7EBF"
  }), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "\u666E\u901A\u8BDD\u6A21\u578B"), React.createElement("div", {
    className: "permission-note"
  }, "vosk-model-small-cn-0.22 \xB7 \u7EA6 68 MB")), React.createElement("span", {
    className: "status-badge"
  }, modelStatus)), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "\u539F\u751F\u8BC6\u522B\u5F15\u64CE"), React.createElement("div", {
    className: "permission-note"
  }, "Vosk Android 0.3.75 \xB7 ARM64")), React.createElement("span", {
    className: "status-badge"
  }, "\u5DF2\u5185\u7F6E")), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "\u81EA\u7136\u8BED\u8A00\u6307\u4EE4\u89E3\u6790"), React.createElement("div", {
    className: "permission-note"
  }, "\u65E5\u671F\u3001\u661F\u671F\u3001\u91CD\u590D\u3001DDL \u4E0E\u5E94\u7528\u64CD\u4F5C")), React.createElement("span", {
    className: "status-badge"
  }, "\u672C\u5730")), React.createElement(SectionHeader, {
    title: "\u5168\u5C40\u8BED\u97F3\u6307\u4EE4",
    note: "\u6267\u884C\u524D\u786E\u8BA4"
  }), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "\u521B\u5EFA\u4E0E\u4FEE\u6539"), React.createElement("div", {
    className: "permission-note"
  }, "\u540D\u79F0\u3001\u65F6\u95F4\u3001\u91CD\u590D\u3001\u63D0\u9192\u548C\u5907\u6CE8")), React.createElement("span", {
    className: "status-badge"
  }, "\u53EF\u7528")), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "\u5B8C\u6210\u4E0E\u5220\u9664"), React.createElement("div", {
    className: "permission-note"
  }, "\u52FE\u9009\u3001\u53D6\u6D88\u52FE\u9009\u3001\u5F52\u6863\u6216\u5220\u9664")), React.createElement("span", {
    className: "status-badge"
  }, "\u53EF\u7528")), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "DDL \u63A7\u5236"), React.createElement("div", {
    className: "permission-note"
  }, "\u8BBE\u7F6E\u671F\u9650\u3001\u6539\u671F\u548C\u6309\u5929\uFF0F\u5468\uFF0F\u6708\u5EF6\u671F")), React.createElement("span", {
    className: "status-badge"
  }, "\u53EF\u7528")), React.createElement("div", {
    className: "permission-row"
  }, React.createElement("div", null, React.createElement("div", {
    className: "permission-title"
  }, "\u9875\u9762\u4E0E\u89C6\u56FE"), React.createElement("div", {
    className: "permission-note"
  }, "\u4ECA\u65E5\u3001\u5173\u952E\u3001\u5386\u53F2\u3001\u62A5\u8868\u53CA\u65E5\uFF0F\u5468\uFF0F\u6708")), React.createElement("span", {
    className: "status-badge"
  }, "\u53EF\u7528")));
}
Object.assign(window, {
  TodayScreen: TodayScreen,
  CriticalScreen: CriticalScreen,
  ViewMenu: ViewMenu,
  VoiceComposer: VoiceComposer,
  DailyEditSheet: DailyEditSheet,
  MoreSheet: MoreSheet,
  CriticalDetailSheet: CriticalDetailSheet,
  CalendarDaySheet: CalendarDaySheet,
  HistoryScreen: HistoryScreen,
  ReportScreen: ReportScreen,
  DeleteConfirmSheet: DeleteConfirmSheet,
  PermissionsScreen: PermissionsScreen,
  SettingsScreen: SettingsScreen,
  CriticalReminderScreen: CriticalReminderScreen,
  VersionScreen: VersionScreen,
  VoiceSettingsScreen: VoiceSettingsScreen,
  compareVersions: compareVersions
});
})();

/* app.jsx */
(() => {
var _excluded = ["objectUrl"],
  _excluded2 = ["type"];
function _objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = _objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var s = Object.getOwnPropertySymbols(e); for (r = 0; r < s.length; r++) o = s[r], t.includes(o) || {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function _objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (e.includes(n)) continue; t[n] = r[n]; } return t; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var _React = React,
  useEffect = _React.useEffect,
  useRef = _React.useRef,
  useState = _React.useState;
var _window = window,
  APP_DATA = _window.APP_DATA,
  getCriticalReminder = _window.getCriticalReminder,
  normalizeCriticalReminderPlan = _window.normalizeCriticalReminderPlan,
  repeatDaysFromValue = _window.repeatDaysFromValue,
  repeatLabelFromDays = _window.repeatLabelFromDays,
  taskOccursOnDate = _window.taskOccursOnDate,
  countScheduledTasksOnDate = _window.countScheduledTasksOnDate,
  normalizeCriticalCompletion = _window.normalizeCriticalCompletion,
  completeCriticalForDate = _window.completeCriticalForDate,
  moveCriticalCompletion = _window.moveCriticalCompletion,
  uncompleteCriticalTask = _window.uncompleteCriticalTask,
  criticalTaskVisibleOnTodayDate = _window.criticalTaskVisibleOnTodayDate,
  shiftDateKeyByDays = _window.shiftDateKeyByDays,
  PhoneFrame = _window.PhoneFrame,
  BottomNav = _window.BottomNav,
  Sheet = _window.Sheet,
  TodayScreen = _window.TodayScreen,
  CriticalScreen = _window.CriticalScreen,
  ViewMenu = _window.ViewMenu,
  VoiceComposer = _window.VoiceComposer,
  DailyEditSheet = _window.DailyEditSheet,
  MoreSheet = _window.MoreSheet,
  CriticalDetailSheet = _window.CriticalDetailSheet,
  CalendarDaySheet = _window.CalendarDaySheet,
  HistoryScreen = _window.HistoryScreen,
  ReportScreen = _window.ReportScreen,
  DeleteConfirmSheet = _window.DeleteConfirmSheet,
  PermissionsScreen = _window.PermissionsScreen,
  SettingsScreen = _window.SettingsScreen,
  CriticalReminderScreen = _window.CriticalReminderScreen,
  VersionScreen = _window.VersionScreen,
  VoiceSettingsScreen = _window.VoiceSettingsScreen;
var DOMAIN_NLU = window.JINKE_DOMAIN_NLU;
var BUILT_IN_REMINDER_SOUNDS = [{
  id: "chime",
  name: "今刻清音",
  source: "built-in"
}, {
  id: "bell",
  name: "轻铃",
  source: "built-in"
}, {
  id: "glass",
  name: "玻璃音",
  source: "built-in"
}, {
  id: "pop",
  name: "短促音",
  source: "built-in"
}, {
  id: "soft",
  name: "柔和提示",
  source: "built-in"
}];
var PHONE_WIDTH = 430;
var EXPANDED_WIDTH = 860;
var DEVICE_HEIGHT = 956;
var SIMULATOR_GAP = 48;
var SIMULATOR_WIDTH = PHONE_WIDTH + EXPANDED_WIDTH + SIMULATOR_GAP;
var SIMULATOR_HEIGHT = DEVICE_HEIGHT + 34;
function localDateKey() {
  var date = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : new Date();
  return "".concat(date.getFullYear(), "-").concat(String(date.getMonth() + 1).padStart(2, "0"), "-").concat(String(date.getDate()).padStart(2, "0"));
}
function readStoredJson(key, fallback, validator) {
  try {
    var parsed = JSON.parse(localStorage.getItem(key) || "null");
    if (parsed !== null && (!validator || validator(parsed))) return parsed;
  } catch (_unused) {}
  return fallback;
}
function migrateLegacySeedData() {
  var markerKey = "jinke-seed-migration-v2";
  var legacyDailyIds = new Set(["wake", "plan", "gym", "journal", "sleep"]);
  var legacyCriticalIds = new Set(["passport", "voice-app", "tax", "autodrive", "health"]);
  var legacyHistoryIds = new Set(["h1", "h2", "h3", "h4"]);
  try {
    if (localStorage.getItem(markerKey)) return;
    var storedDaily = JSON.parse(localStorage.getItem("jinke-daily-tasks") || "null");
    if (Array.isArray(storedDaily)) {
      var retained = storedDaily.filter(function (task) {
        return !legacyDailyIds.has(task.id);
      });
      localStorage.setItem("jinke-daily-tasks", JSON.stringify(retained.length ? retained : APP_DATA.dailyTasks));
    }
    var storedCritical = JSON.parse(localStorage.getItem("jinke-critical-tasks") || "null");
    if (Array.isArray(storedCritical)) {
      var _retained = storedCritical.filter(function (task) {
        return !legacyCriticalIds.has(task.id);
      });
      localStorage.setItem("jinke-critical-tasks", JSON.stringify(_retained.length ? _retained : APP_DATA.criticalTasks));
    }
    var storedHistory = JSON.parse(localStorage.getItem("jinke-task-history") || "null");
    if (Array.isArray(storedHistory)) {
      var _retained2 = storedHistory.filter(function (item) {
        return !legacyHistoryIds.has(item.id) && !legacyCriticalIds.has(item.sourceTaskId);
      });
      localStorage.setItem("jinke-task-history", JSON.stringify(_retained2));
    }
    var storedCompletions = JSON.parse(localStorage.getItem("jinke-daily-completions") || "null");
    if (storedCompletions && _typeof(storedCompletions) === "object" && !Array.isArray(storedCompletions)) {
      var _retained3 = Object.fromEntries(Object.entries(storedCompletions).filter(function (_ref) {
        var _ref2 = _slicedToArray(_ref, 1),
          key = _ref2[0];
        return !legacyDailyIds.has(key.split(":")[0]);
      }));
      localStorage.setItem("jinke-daily-completions", JSON.stringify(_retained3));
    }
    localStorage.setItem(markerKey, "1");
  } catch (_unused2) {}
}
migrateLegacySeedData();
function migrateVoiceCreatedNotes() {
  var markerKey = "jinke-voice-note-migration-v3";
  try {
    if (localStorage.getItem(markerKey)) return;
    ["jinke-daily-tasks", "jinke-critical-tasks"].forEach(function (key) {
      var stored = JSON.parse(localStorage.getItem(key) || "null");
      if (!Array.isArray(stored)) return;
      var cleaned = stored.map(function (task) {
        return _objectSpread(_objectSpread({}, task), {}, {
          note: typeof task.note === "string" ? task.note.replace(/^语音创建(?:\s*·\s*)?/, "").trim() : task.note
        });
      });
      localStorage.setItem(key, JSON.stringify(cleaned));
    });
    localStorage.setItem(markerKey, "1");
  } catch (_unused3) {}
}
migrateVoiceCreatedNotes();
function dateKeyOffset(fromKey, toKey) {
  var _fromKey$split$map = fromKey.split("-").map(Number),
    _fromKey$split$map2 = _slicedToArray(_fromKey$split$map, 3),
    fromYear = _fromKey$split$map2[0],
    fromMonth = _fromKey$split$map2[1],
    fromDay = _fromKey$split$map2[2];
  var _toKey$split$map = toKey.split("-").map(Number),
    _toKey$split$map2 = _slicedToArray(_toKey$split$map, 3),
    toYear = _toKey$split$map2[0],
    toMonth = _toKey$split$map2[1],
    toDay = _toKey$split$map2[2];
  var from = Date.UTC(fromYear, fromMonth - 1, fromDay);
  var to = Date.UTC(toYear, toMonth - 1, toDay);
  return Math.round((to - from) / 86400000);
}
function dateKeyAddDays(dateKey, days) {
  var _dateKey$split$map = dateKey.split("-").map(Number),
    _dateKey$split$map2 = _slicedToArray(_dateKey$split$map, 3),
    year = _dateKey$split$map2[0],
    month = _dateKey$split$map2[1],
    day = _dateKey$split$map2[2];
  var next = new Date(year, month - 1, day + days, 12);
  return localDateKey(next);
}
function criticalDaysLeftOn(task, dateKey, fallbackAnchorKey) {
  if (!Number.isFinite(task === null || task === void 0 ? void 0 : task.daysLeft)) return null;
  var anchorKey = task.anchorDateKey || fallbackAnchorKey;
  return task.daysLeft - dateKeyOffset(anchorKey, dateKey);
}
function editableCriticalDeadline(task, todayKey) {
  if (!(task !== null && task !== void 0 && task.deadline)) return null;
  if (/^\d{4}-\d{2}-\d{2}$/.test(String(task.deadline))) return task.deadline;
  var daysLeft = criticalDaysLeftOn(task, todayKey, todayKey);
  return Number.isFinite(daysLeft) ? dateKeyAddDays(todayKey, daysLeft) : task.deadline;
}
function withCriticalReminderDefaults(task) {
  var _task$deadlineTime;
  var deadlineTime = (_task$deadlineTime = task.deadlineTime) !== null && _task$deadlineTime !== void 0 ? _task$deadlineTime : task.time && task.time !== "待定" ? task.time : null;
  var normalizedPlan = normalizeCriticalReminderPlan(task);
  var plan = normalizedPlan.reminderMode === "final-days" && Number.isFinite(task.daysLeft) ? _objectSpread(_objectSpread({}, normalizedPlan), {}, {
    reminderFinalDays: Math.min(Math.max(1, task.daysLeft), Math.max(1, normalizedPlan.reminderFinalDays))
  }) : normalizedPlan;
  var next = _objectSpread(_objectSpread({}, task), {}, {
    alertMode: task.alertMode || "inherit",
    soundId: task.soundId || "inherit",
    deadlineTime: deadlineTime,
    time: deadlineTime
  }, plan);
  return _objectSpread(_objectSpread({}, next), {}, {
    reminder: getCriticalReminder(next)
  });
}
function criticalHistoryEntry(task, completionDateKey, fallbackAnchorKey) {
  var completionKey = "".concat(task.id, ":").concat(completionDateKey);
  var _completionDateKey$sp = completionDateKey.split("-").map(Number),
    _completionDateKey$sp2 = _slicedToArray(_completionDateKey$sp, 3),
    month = _completionDateKey$sp2[1],
    day = _completionDateKey$sp2[2];
  return {
    id: "done-".concat(task.id, "-").concat(completionDateKey),
    completionKey: completionKey,
    sourceTaskId: task.id,
    title: task.title,
    completed: "".concat(month, "\u6708").concat(day, "\u65E5"),
    completedDateKey: completionDateKey,
    completedAt: task.completedAt || null,
    leadDays: criticalDaysLeftOn(task, completionDateKey, fallbackAnchorKey) || 0
  };
}
var CN_DIGITS = {
  "零": 0,
  "〇": 0,
  "一": 1,
  "二": 2,
  "两": 2,
  "三": 3,
  "四": 4,
  "五": 5,
  "六": 6,
  "七": 7,
  "八": 8,
  "九": 9
};
function normalizeSpeechText(value) {
  var _DOMAIN_NLU$normalize, _DOMAIN_NLU$normalize2;
  return (_DOMAIN_NLU$normalize = DOMAIN_NLU === null || DOMAIN_NLU === void 0 || (_DOMAIN_NLU$normalize2 = DOMAIN_NLU.normalizeTranscript) === null || _DOMAIN_NLU$normalize2 === void 0 ? void 0 : _DOMAIN_NLU$normalize2.call(DOMAIN_NLU, value)) !== null && _DOMAIN_NLU$normalize !== void 0 ? _DOMAIN_NLU$normalize : String(value || "").trim();
}
function parseNumber(value) {
  if (DOMAIN_NLU !== null && DOMAIN_NLU !== void 0 && DOMAIN_NLU.parseNumber) return DOMAIN_NLU.parseNumber(value);
  if (!value) return null;
  if (/^\d+$/.test(value)) return Number(value);
  if (value === "十") return 10;
  if (value.includes("十")) {
    var _value$split = value.split("十"),
      _value$split2 = _slicedToArray(_value$split, 2),
      tens = _value$split2[0],
      ones = _value$split2[1];
    return (tens ? CN_DIGITS[tens] : 1) * 10 + (ones ? CN_DIGITS[ones] : 0);
  }
  return value.split("").reduce(function (total, char) {
    var _CN_DIGITS$char;
    return total * 10 + ((_CN_DIGITS$char = CN_DIGITS[char]) !== null && _CN_DIGITS$char !== void 0 ? _CN_DIGITS$char : 0);
  }, 0);
}
function parseTime(text) {
  var _DOMAIN_NLU$extractTi;
  var mention = DOMAIN_NLU === null || DOMAIN_NLU === void 0 || (_DOMAIN_NLU$extractTi = DOMAIN_NLU.extractTimeMentions) === null || _DOMAIN_NLU$extractTi === void 0 || (_DOMAIN_NLU$extractTi = _DOMAIN_NLU$extractTi.call(DOMAIN_NLU, text)) === null || _DOMAIN_NLU$extractTi === void 0 ? void 0 : _DOMAIN_NLU$extractTi[0];
  return mention || {
    value: "待定",
    source: "",
    sources: [],
    period: "",
    confidence: 0
  };
}
function parseTimeRange(text) {
  var connector = text.match(/(?:到|至|直到|—|–|-)/);
  if (!connector) return null;
  var index = connector.index;
  var before = text.slice(0, index);
  var after = text.slice(index + connector[0].length);
  var start = parseTime(before);
  if (!start.source) return null;
  var inheritedPeriod = start.period && !/^(?:凌晨|早上|上午|中午|下午|傍晚|晚上)/.test(after.trim()) ? start.period : "";
  var end = parseTime("".concat(inheritedPeriod).concat(after));
  if (!end.source) return null;
  var endSources = (end.sources || [end.source]).map(function (source) {
    return source.replace(new RegExp("^".concat(inheritedPeriod)), "");
  }).filter(Boolean);
  return {
    start: start,
    end: end,
    source: "".concat(start.source).concat(connector[0]).concat(end.source.replace(new RegExp("^".concat(inheritedPeriod)), "")),
    sources: _toConsumableArray(new Set([].concat(_toConsumableArray(start.sources || [start.source]), _toConsumableArray(endSources)).filter(Boolean))),
    crossesMidnight: end.value < start.value || start.value.startsWith("24:")
  };
}
function parseRepeat(text) {
  var range = text.match(/(?:每(?:个)?)?(?:周|星期)([一二三四五六日天])(?:到|至|\-)(?:(?:周|星期))?([一二三四五六日天])(?:每(?:天|日))?/);
  if (range) {
    var start = range[1] === "天" ? "日" : range[1];
    var end = range[2] === "天" ? "日" : range[2];
    if (start === "一" && end === "五") return {
      value: "工作日",
      days: [1, 2, 3, 4, 5],
      source: range[0]
    };
    var order = ["一", "二", "三", "四", "五", "六", "日"];
    var startIndex = order.indexOf(start);
    var endIndex = order.indexOf(end);
    var _days = startIndex >= 0 && endIndex >= startIndex ? order.slice(startIndex, endIndex + 1) : [start, end];
    var _numericDays = _days.map(function (day) {
      return ["一", "二", "三", "四", "五", "六", "日"].indexOf(day) + 1;
    }).filter(Boolean);
    return {
      value: repeatLabelFromDays(_numericDays),
      days: _numericDays,
      source: range[0]
    };
  }
  if (/每(天|日)/.test(text)) return {
    value: "每天",
    days: [1, 2, 3, 4, 5, 6, 7],
    source: text.match(/每(天|日)/)[0]
  };
  if (/每个?工作日/.test(text)) return {
    value: "工作日",
    days: [1, 2, 3, 4, 5],
    source: text.match(/每个?工作日/)[0]
  };
  if (/每(个)?周末/.test(text)) return {
    value: "周末",
    days: [6, 7],
    source: text.match(/每(个)?周末/)[0]
  };
  var match = text.match(/(?:每)?(?:周|星期)([一二三四五六日天、，和及到至\-]+)/);
  if (!match) return {
    value: "仅一次",
    days: [],
    source: ""
  };
  var days = [];
  var _iterator = _createForOfIteratorHelper(match[1]),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var char = _step.value;
      var day = char === "天" ? "日" : char;
      if (/[一二三四五六日]/.test(day) && !days.includes(day)) days.push(day);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  var numericDays = days.map(function (day) {
    return ["一", "二", "三", "四", "五", "六", "日"].indexOf(day) + 1;
  }).filter(Boolean);
  return {
    value: numericDays.length ? repeatLabelFromDays(numericDays) : "仅一次",
    days: numericDays,
    source: match[0]
  };
}
function parseDeadline(text) {
  var explicitNone = text.match(/(?:无|没有)\s*(?:ddl|deadline|截止(?:日期)?|期限|死线)/i);
  if (explicitNone) return {
    deadline: null,
    daysLeft: null,
    source: explicitNone[0],
    kind: "explicit-none"
  };
  var iso = text.match(/(\d{4})-(\d{2})-(\d{2})/);
  if (iso) {
    var year = Number(iso[1]);
    var _month = Number(iso[2]);
    var _day = Number(iso[3]);
    var _target = new Date(year, _month - 1, _day, 12);
    if (_target.getFullYear() !== year || _target.getMonth() !== _month - 1 || _target.getDate() !== _day) {
      return {
        deadline: null,
        daysLeft: null,
        source: iso[0],
        kind: "invalid"
      };
    }
    var _now = new Date();
    var _today = new Date(_now.getFullYear(), _now.getMonth(), _now.getDate(), 12);
    return {
      deadline: iso[0],
      daysLeft: Math.round((_target - _today) / 86400000),
      source: iso[0],
      kind: "absolute-iso"
    };
  }
  var relative = text.match(/(今天|明天|后天)(?:截止|到期)?/);
  if (relative) {
    var _daysLeft = relative[1] === "今天" ? 0 : relative[1] === "明天" ? 1 : 2;
    return {
      deadline: _daysLeft === 0 ? "今天截止" : relative[1],
      daysLeft: _daysLeft,
      source: relative[0],
      kind: "relative"
    };
  }
  var absolute = text.match(/([零〇一二三四五六七八九十两\d]{1,3})月([零〇一二三四五六七八九十两\d]{1,3})[日号]?(?:截止|到期)?/);
  if (!absolute) return {
    deadline: null,
    daysLeft: null,
    source: "",
    kind: "none"
  };
  var month = parseNumber(absolute[1]);
  var day = parseNumber(absolute[2]);
  var now = new Date();
  var target = new Date(now.getFullYear(), month - 1, day);
  var today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  if (target < today) target = new Date(now.getFullYear() + 1, month - 1, day);
  var daysLeft = Math.ceil((target - today) / 86400000);
  return {
    deadline: "".concat(month, "\u6708").concat(day, "\u65E5"),
    daysLeft: daysLeft,
    source: absolute[0],
    kind: "absolute"
  };
}
function datePointFromDate(target, source) {
  var now = new Date();
  var today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  var normalized = new Date(target.getFullYear(), target.getMonth(), target.getDate());
  return {
    deadline: "".concat(normalized.getMonth() + 1, "\u6708").concat(normalized.getDate(), "\u65E5"),
    daysLeft: Math.round((normalized - today) / 86400000),
    source: source,
    date: normalized
  };
}
function parseDatePoints(text) {
  var points = [];
  var absolutePattern = /([零〇一二三四五六七八九十两\d]{1,3})月([零〇一二三四五六七八九十两\d]{1,3})[日号]?/g;
  var _iterator2 = _createForOfIteratorHelper(text.matchAll(absolutePattern)),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var match = _step2.value;
      var month = parseNumber(match[1]);
      var day = parseNumber(match[2]);
      var now = new Date();
      var target = new Date(now.getFullYear(), month - 1, day);
      var today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
      if (target < today) target = new Date(now.getFullYear() + 1, month - 1, day);
      points.push(_objectSpread(_objectSpread({}, datePointFromDate(target, match[0])), {}, {
        index: match.index
      }));
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  var relativePattern = /(今天|明天|后天|([零〇一二三四五六七八九十两\d]{1,3})天后)/g;
  var _iterator3 = _createForOfIteratorHelper(text.matchAll(relativePattern)),
    _step3;
  try {
    var _loop = function _loop() {
      var match = _step3.value;
      if (points.some(function (point) {
        return match.index >= point.index && match.index < point.index + point.source.length;
      })) return 1;
      var offset = match[1] === "今天" ? 0 : match[1] === "明天" ? 1 : match[1] === "后天" ? 2 : parseNumber(match[2]);
      var target = new Date();
      target.setDate(target.getDate() + offset);
      points.push(_objectSpread(_objectSpread({}, datePointFromDate(target, match[0])), {}, {
        index: match.index
      }));
    };
    for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
      if (_loop()) continue;
    }
  } catch (err) {
    _iterator3.e(err);
  } finally {
    _iterator3.f();
  }
  var weekdayPattern = /((?:本|下)?周)([一二三四五六日天])/g;
  var _iterator4 = _createForOfIteratorHelper(text.matchAll(weekdayPattern)),
    _step4;
  try {
    for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
      var _match = _step4.value;
      var weekday = _match[2] === "天" ? 7 : ["一", "二", "三", "四", "五", "六", "日"].indexOf(_match[2]) + 1;
      var _now2 = new Date();
      var todayWeekday = _now2.getDay() || 7;
      var offset = weekday - todayWeekday;
      if (_match[1] === "下周") offset += offset <= 0 ? 7 : 7;else if (offset < 0) offset += 7;
      var _target2 = new Date(_now2.getFullYear(), _now2.getMonth(), _now2.getDate() + offset);
      points.push(_objectSpread(_objectSpread({}, datePointFromDate(_target2, _match[0])), {}, {
        index: _match.index
      }));
    }
  } catch (err) {
    _iterator4.e(err);
  } finally {
    _iterator4.f();
  }
  return points.sort(function (a, b) {
    return a.index - b.index;
  });
}
function parseTaskSpan(text) {
  var datePoints = parseDatePoints(text);
  var durationMatch = text.match(/(?:待|住|停留|相隔|隔|过|持续)?\s*([零〇一二三四五六七八九十两\d]{1,3})\s*天(?:后)?(?=\s*(?:回|回来|返程|回程|结束))/);
  var durationDays = durationMatch ? Math.max(1, parseNumber(durationMatch[1])) : null;
  var hasSpanLanguage = /(?:去|出发|前往|开始).*(?:回来|返程|回程|结束|回)|(?:从).*(?:到|至)/.test(text);
  if (!hasSpanLanguage || !durationDays && datePoints.length < 2) return null;
  var start = datePoints[0] || null;
  var end = datePoints[1] || null;
  if (start && !end && durationDays) {
    var target = new Date(start.date);
    target.setDate(target.getDate() + durationDays);
    end = datePointFromDate(target, "".concat(durationDays, "\u5929\u540E"));
  }
  if (!start || !end) return null;
  var subject = text;
  [].concat(_toConsumableArray(datePoints.map(function (point) {
    return point.source;
  })), [durationMatch === null || durationMatch === void 0 ? void 0 : durationMatch[0]]).filter(Boolean).forEach(function (part) {
    subject = subject.replace(part, " ");
  });
  subject = subject.replace(/(?:然后|之后|再)?\s*(?:回来|返程|回程|结束|回)/g, " ").replace(/(?:待|住|停留|相隔|隔|过|持续)/g, " ").replace(/(?:我|我们)?\s*(?:要|想|打算|计划)?\s*(?:去|出发去|前往|到)/g, " ").replace(/(?:开始|出发)/g, " ").replace(/[，,。；;！？!?\s]+/g, " ").trim();
  var title = subject ? "".concat(subject, "\u884C\u7A0B") : "行程";
  return {
    start: start,
    end: end,
    durationDays: durationDays,
    title: title,
    original: text
  };
}
var TASK_ACTION_WORDS = ["取消预约", "提交", "打电话", "参加", "领取", "预约", "续期", "复习", "整理", "完成", "准备", "修改", "购买", "学习", "阅读", "跑步", "健身", "睡觉", "起床", "开会", "上课", "体检", "写", "读", "看", "买", "取", "拿", "办", "考"];
function extractTaskSemantics(rawText, removableParts, durationSource) {
  var title = rawText;
  [].concat(_toConsumableArray(removableParts), [durationSource]).filter(Boolean).forEach(function (part) {
    title = title.replace(part, " ");
  });
  title = title.replace(/(?:没有|无)\s*(?:ddl|deadline|截止日期|截止|期限|死线)/ig, " ").replace(/(?:有|带|包含)?\s*(?:ddl|deadline|截止日期|截止期限|期限|死线)(?:的)?/ig, " ").replace(/(?:帮我|给我|请)?(?:创建|添加|新增|安排|记下|记一下|提醒我)\s*(?:一个|一条)?/g, " ").replace(/(?:重要|关键|特殊)(?:任务|事项|事件)?|(?:任务|事项|日程)[:：]?/g, " ").replace(/^\s*(?:的\s*)+/, "").replace(/(?:什么时候|何时|哪天|几点|到时候|那个时候|有空的时候|等有空(?:的)?时候)/g, " ").replace(/(?:的)?时候/g, " ").replace(/(?:之前|以前)?(?:截止|到期)/g, " ").replace(/[，,。；;！？!?]+/g, " ").replace(/^\s*(?:我想要?|我要|我需要|我打算|我计划|我准备要?|我希望|麻烦|然后|就是|你帮我|可以帮我)\s*/g, " ").replace(/(?:大概|大约|差不多|可能|最好)\s*/g, " ").replace(/(?:计划|安排|设定|设置|定|放)\s*(?:在|到|为)\s*/g, " ").replace(/^\s*(?:在|于)\s*/g, " ").replace(/\s*(?:一下|一会儿|一会)\s*/g, "").replace(/去(?=考|办|看|买|取|拿|做|参加|提交|领取|预约|体检|健身|开会|上课)/g, "").replace(/(?:的|啊|呀|吧|呢|嘛|啦|咯|呗)+\s*$/g, "").replace(/\s+/g, " ").trim();
  var action = TASK_ACTION_WORDS.find(function (word) {
    return title.includes(word);
  }) || "";
  var actionIndex = action ? title.indexOf(action) : -1;
  var object = "";
  if (actionIndex >= 0) {
    var before = title.slice(0, actionIndex).replace(/^(?:给|向|和|跟)/, "").trim();
    var after = title.slice(actionIndex + action.length).replace(/^(?:给|向|到|去)/, "").trim();
    object = after || before;
  }
  return {
    title: title || "未命名事项",
    action: action,
    object: object,
    keywords: [action, object].filter(Boolean)
  };
}
function formatDailyReminder(totalMinutes) {
  var safe = Math.min(1435, Math.max(0, Math.round((Number(totalMinutes) || 0) / 5) * 5));
  if (safe === 0) return "到点提醒";
  var hours = Math.floor(safe / 60);
  var minutes = safe % 60;
  return "\u63D0\u524D".concat(hours ? "".concat(hours, "\u5C0F\u65F6") : "").concat(minutes ? "".concat(minutes, "\u5206\u949F") : "");
}
function dailyReminderLeadMinutes(value) {
  var _text$match, _text$match2;
  var text = String(value || "到点提醒").replace(/\s+/g, "");
  if (text === "不提醒") return null;
  if (text === "到点提醒") return 0;
  var hours = Number(((_text$match = text.match(/提前(\d+)小时/)) === null || _text$match === void 0 ? void 0 : _text$match[1]) || 0);
  var minutes = Number(((_text$match2 = text.match(/(\d+)分钟/)) === null || _text$match2 === void 0 ? void 0 : _text$match2[1]) || 0);
  return Math.min(1435, Math.max(0, hours * 60 + minutes));
}
function parseVoiceTask(rawText) {
  var _timeRange$end;
  var text = normalizeSpeechText(rawText);
  var span = parseTaskSpan(text);
  var repeat = span ? {
    value: "仅一次",
    days: [],
    source: ""
  } : parseRepeat(text);
  var withoutRepeat = repeat.source ? text.replace(repeat.source, " ") : text;
  var timeRange = parseTimeRange(withoutRepeat);
  var time = (timeRange === null || timeRange === void 0 ? void 0 : timeRange.start) || parseTime(withoutRepeat);
  var deadline = parseDeadline(text);
  var reminderMatch = text.match(/提前\s*([零〇一二三四五六七八九十两\d]{1,3})\s*(分钟|小时)(?:提醒)?/);
  var reminderAmount = reminderMatch ? parseNumber(reminderMatch[1]) : 0;
  var reminder = reminderMatch ? formatDailyReminder(reminderAmount * (reminderMatch[2] === "小时" ? 60 : 1)) : "到点提醒";
  var withoutReminder = reminderMatch ? text.replace(reminderMatch[0], "") : text;
  var durationMatch = withoutReminder.match(/([零〇一二三四五六七八九十两\d]{1,3})\s*(分钟|小时)/);
  var duration = durationMatch ? "".concat(parseNumber(durationMatch[1]), " ").concat(durationMatch[2]) : "";
  var hasTemporalInstruction = Boolean(span || repeat.source || time.source || deadline.deadline || reminderMatch);
  var isCritical = Boolean(span) || deadline.kind === "explicit-none" || !repeat.source && (/(重要|关键|特殊|ddl|deadline|截止|期限|到期|死线)/i.test(text) || deadline.kind === "absolute") || !hasTemporalInstruction;
  var spanSources = span ? [span.start.source, span.end.source] : [];
  var timeSources = (timeRange === null || timeRange === void 0 ? void 0 : timeRange.sources) || time.sources || [time.source];
  var semantics = extractTaskSemantics(text, [reminderMatch === null || reminderMatch === void 0 ? void 0 : reminderMatch[0]].concat(_toConsumableArray(timeSources), [repeat.source, deadline.source], spanSources), durationMatch === null || durationMatch === void 0 ? void 0 : durationMatch[0]);
  var noteParts = [];
  if (duration) noteParts.push("\u6301\u7EED ".concat(duration));
  var criticalPlan = normalizeCriticalReminderPlan({
    deadline: deadline.deadline,
    reminderEnabled: Boolean(deadline.deadline)
  });
  return _objectSpread(_objectSpread({
    type: isCritical ? "critical" : "daily",
    title: (span === null || span === void 0 ? void 0 : span.title) || semantics.title,
    time: time.value,
    endTime: (timeRange === null || timeRange === void 0 || (_timeRange$end = timeRange.end) === null || _timeRange$end === void 0 ? void 0 : _timeRange$end.value) || null,
    spansMidnight: Boolean(timeRange === null || timeRange === void 0 ? void 0 : timeRange.crossesMidnight),
    repeat: repeat.source ? repeat.value : !isCritical && deadline.deadline ? deadline.deadline : repeat.value,
    repeatDays: repeat.days,
    reminder: isCritical ? getCriticalReminder(criticalPlan) : reminder,
    deadline: deadline.deadline,
    deadlineTime: isCritical && time.source ? time.value : null
  }, isCritical ? criticalPlan : {}), {}, {
    daysLeft: deadline.daysLeft,
    note: noteParts.join(" · "),
    action: semantics.action,
    object: semantics.object,
    keywords: semantics.keywords,
    hasTime: Boolean(time.source),
    hasRepeat: Boolean(repeat.source),
    hasReminder: Boolean(reminderMatch),
    hasDeadline: Boolean(deadline.deadline),
    alertMode: "inherit",
    soundId: "inherit",
    span: span
  });
}
function normalizeTaskText(value) {
  return value.toLowerCase().replace(/^(?:请|帮我|麻烦|把|给|将)*/, "").replace(/^(?:取消勾选|取消完成|清除|清空|删除|移除|删掉|修改|更改|改名|更名|调整|设置|完成|勾选|标记|整理|安排|学习|写|提交|进行|开始)/, "").replace(/[\d０-９]+\s*(?:分钟|小时|天)/g, "").replace(/[\s\d０-９·。、，,：:（）()\-]/g, "").replace(/[零〇一二三四五六七八九十两百]+(?:分钟|小时|天)?/g, "").replace(/任务|事项|日程|ddl/ig, "");
}
function findMentionedTask(text, dailyTasks, criticalTasks) {
  var haystack = normalizeTaskText(text);
  var candidates = [].concat(_toConsumableArray(dailyTasks.map(function (task) {
    return {
      kind: "daily",
      task: task
    };
  })), _toConsumableArray(criticalTasks.map(function (task) {
    return {
      kind: "critical",
      task: task
    };
  })));
  var best = null;
  var bestScore = 0;
  var ambiguous = false;
  candidates.forEach(function (candidate) {
    var title = normalizeTaskText(candidate.task.title);
    if (!title) return;
    var aliases = [title, title.replace(/^(今天|今日|每天|每日|年度|本周)/, "")].filter(function (value, index, array) {
      return value.length >= 2 && array.indexOf(value) === index;
    });
    var score = aliases.reduce(function (highest, alias) {
      return haystack.includes(alias) ? Math.max(highest, alias.length + 20) : highest;
    }, 0);
    for (var size = Math.min(6, title.length); size >= 3 && !score; size -= 1) {
      for (var index = 0; index <= title.length - size; index += 1) {
        if (haystack.includes(title.slice(index, index + size))) {
          score = size;
          break;
        }
      }
    }
    if (!score && DOMAIN_NLU !== null && DOMAIN_NLU !== void 0 && DOMAIN_NLU.similarity) {
      var fuzzyScore = DOMAIN_NLU.similarity(haystack, title);
      if (fuzzyScore >= 0.68) score = fuzzyScore * 10;
    }
    if (score > bestScore) {
      bestScore = score;
      best = candidate;
      ambiguous = false;
    } else if (score > 0 && score === bestScore) {
      ambiguous = true;
    }
  });
  return ambiguous ? null : best;
}
function parseDelayDays(text) {
  var match = text.match(/([零〇一二三四五六七八九十两\d]{1,3})\s*(天|日|周|个月|月)/);
  if (!match) return 30;
  var amount = parseNumber(match[1]) || 1;
  if (match[2] === "周") return amount * 7;
  if (match[2].includes("月")) return amount * 30;
  return amount;
}
function deadlineLabelFromDays(days) {
  var target = new Date();
  target.setDate(target.getDate() + Math.max(days, 0));
  return "".concat(target.getMonth() + 1, "\u6708").concat(target.getDate(), "\u65E5");
}
function commandResult(intent, heading, rows) {
  var options = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
  return _objectSpread({
    intent: intent,
    heading: heading,
    rows: rows,
    confirmLabel: options.confirmLabel || "确认执行",
    valid: options.valid !== false,
    error: options.error || ""
  }, options);
}
function parseVoiceCommand(rawText, dailyTasks, criticalTasks) {
  var _DOMAIN_NLU$analyze, _domainAnalysis$inten, _domainAnalysis$inten2;
  var text = normalizeSpeechText(rawText);
  var domainAnalysis = (DOMAIN_NLU === null || DOMAIN_NLU === void 0 || (_DOMAIN_NLU$analyze = DOMAIN_NLU.analyze) === null || _DOMAIN_NLU$analyze === void 0 ? void 0 : _DOMAIN_NLU$analyze.call(DOMAIN_NLU, text)) || null;
  var target = findMentionedTask(text, dailyTasks, criticalTasks);
  var wantsCreate = (domainAnalysis === null || domainAnalysis === void 0 || (_domainAnalysis$inten = domainAnalysis.intent) === null || _domainAnalysis$inten === void 0 ? void 0 : _domainAnalysis$inten.intent) === "create" || /(创建|添加|新增|记下|记一下|提醒我)/.test(text) || /^(?:帮我|请)?安排/.test(text);
  var hasAll = /(全部|所有)/.test(text);
  var arrangementNoun = /(安排|日程|任务|事项)/.test(text);
  if (/(切换|改成|设置|使用|启用).*(暗色|深色|夜间|亮色|浅色|跟随系统|系统主题)|^(暗色|深色|夜间|亮色|浅色|跟随系统|系统主题)(?:模式)?$/.test(text)) {
    var themeMode = /(跟随系统|系统主题)/.test(text) ? "system" : /(亮色|浅色)/.test(text) ? "light" : "dark";
    var themeLabel = themeMode === "system" ? "跟随系统" : themeMode === "light" ? "亮色" : "暗色";
    return commandResult("theme", "\u5207\u6362\u4E3A".concat(themeLabel), [["外观", themeLabel]], {
      themeMode: themeMode,
      confirmLabel: "切换"
    });
  }
  if (!wantsCreate && /(ddl|deadline|关键事项|关键任务|截止任务).*(默认提醒时间|提醒时间|默认时间)/i.test(text) && /(设置|设为|改为|改到|调整)/.test(text)) {
    var reminderTime = parseTime(text);
    return reminderTime.source ? commandResult("set-ddl-reminder-time", "修改 DDL 默认提醒时间", [["时间", reminderTime.value], ["频率", "5 的倍数天；最后 5 天每日"]], {
      reminderTime: reminderTime.value,
      confirmLabel: "保存"
    }) : commandResult("set-ddl-reminder-time", "没有识别到提醒时间", [["示例", "把 DDL 默认提醒时间改为早上九点"]], {
      valid: false,
      error: "请说出具体时间"
    });
  }
  if (!wantsCreate && /(ddl|deadline|关键事项|关键任务|截止任务).*(倍数|每隔|节点)/i.test(text) && /(设置|设为|改为|改成|调整)/.test(text)) {
    var multiple = parseDelayDays(text);
    return commandResult("set-ddl-reminder-policy", "修改 DDL 提醒倍数", [["倍数节点", "\u6BCF ".concat(multiple, " \u5929")]], {
      policy: "multiple",
      value: multiple,
      confirmLabel: "保存"
    });
  }
  if (!wantsCreate && /(ddl|deadline|关键事项|关键任务|截止任务).*(最后|临近|连续).*提醒/i.test(text) && /(设置|设为|改为|改成|调整)/.test(text)) {
    var finalDays = parseDelayDays(text);
    return commandResult("set-ddl-reminder-policy", "修改 DDL 连续提醒天数", [["临近截止", "\u6700\u540E ".concat(finalDays, " \u5929")]], {
      policy: "final-days",
      value: finalDays,
      confirmLabel: "保存"
    });
  }
  var dateSelection = text.match(/(?:切换|打开|查看|前往|去|到)\s*(?:到)?\s*([零〇一二三四五六七八九十两\d]{1,3})[日号]/);
  if (dateSelection) {
    var day = parseNumber(dateSelection[1]);
    var now = new Date();
    var runtimeDateKey = "".concat(now.getFullYear(), "-").concat(String(now.getMonth() + 1).padStart(2, "0"), "-").concat(String(now.getDate()).padStart(2, "0"));
    var currentWeek = window.getWeekDates(runtimeDateKey);
    var dateItem = currentWeek.find(function (item) {
      return item.date === day;
    });
    var first = currentWeek[0];
    var last = currentWeek[currentWeek.length - 1];
    var range = "".concat(first.month, "\u6708").concat(first.date, "\u65E5\u2014").concat(last.month, "\u6708").concat(last.date, "\u65E5");
    return dateItem ? commandResult("select-date", "\u5207\u6362\u5230 ".concat(dateItem.month, " \u6708 ").concat(day, " \u65E5"), [["日期", "".concat(dateItem.month, "\u6708").concat(day, "\u65E5")]], {
      dateKey: dateItem.dateKey,
      confirmLabel: "切换"
    }) : commandResult("select-date", "当前周没有这个日期", [["当前范围", range]], {
      valid: false,
      error: "请说当前周日期"
    });
  }
  if (!wantsCreate && hasAll && arrangementNoun && /(清空|清除|删除|移除|删掉)/.test(text)) {
    var scope = /(日常|每日)/.test(text) ? "daily" : /(关键|ddl|deadline|截止任务)/i.test(text) ? "critical" : "all";
    var dailyCount = scope === "critical" ? 0 : dailyTasks.length;
    var criticalCount = scope === "daily" ? 0 : criticalTasks.length;
    var scopeLabel = scope === "daily" ? "全部日常事项" : scope === "critical" ? "全部关键事项" : "全部未完成安排";
    return commandResult("clear-all", "\u6E05\u9664".concat(scopeLabel), [["日常", "".concat(dailyCount, " \u9879")], ["关键", "".concat(criticalCount, " \u9879")], ["历史记录", "保留"]], {
      scope: scope,
      confirmLabel: "确认清除"
    });
  }
  if (!wantsCreate && hasAll && /(完成|做完|勾选|打勾)/.test(text) && /(今天|今日|日常|任务|事项|安排)/.test(text)) {
    return commandResult("complete-all", "完成所选日期的全部日常事项", [["日常事项", "".concat(dailyTasks.length, " \u9879")], ["日期", "当前所选日期"]], {
      confirmLabel: "全部完成"
    });
  }
  if (!wantsCreate && /(有什么|有哪些|列出|告诉我|汇总|查询|多少|还剩什么|还有什么|需要做什么|该做什么)/.test(text) && (arrangementNoun || /(还剩什么|还有什么|需要做什么|该做什么)/.test(text))) {
    var ddlCount = criticalTasks.filter(function (task) {
      return task.deadline;
    }).length;
    var noDdlCount = criticalTasks.length - ddlCount;
    return commandResult("query", "当前安排", [["日常", "".concat(dailyTasks.length, " \u9879")], ["有 DDL", "".concat(ddlCount, " \u9879")], ["无 DDL", "".concat(noDdlCount, " \u9879")]], {
      confirmLabel: "关闭"
    });
  }
  if (/(打开|进入|查看|切换|回到)/.test(text)) {
    if (/(月报|月度总结|上月复盘)/.test(text)) return commandResult("navigate", "打开月度复盘", [["页面", "月度复盘"]], {
      route: "month",
      confirmLabel: "打开"
    });
    if (/(年报|年度总结|年度复盘)/.test(text)) return commandResult("navigate", "打开年度复盘", [["页面", "年度复盘"]], {
      route: "year",
      confirmLabel: "打开"
    });
    if (/历史/.test(text)) return commandResult("navigate", "打开历史记录", [["页面", "历史记录"]], {
      route: "history",
      confirmLabel: "打开"
    });
    if (/(ddl|deadline|关键|截止).*(提醒|通知)/i.test(text)) return commandResult("navigate", "打开关键提醒", [["页面", "关键提醒"]], {
      route: "critical-reminders",
      confirmLabel: "打开"
    });
    if (/(版本|更新)/.test(text)) return commandResult("navigate", "打开版本更新", [["页面", "版本更新"]], {
      route: "version",
      confirmLabel: "打开"
    });
    if (/(通知|权限|后台|电池)/.test(text)) return commandResult("navigate", "打开提醒与权限", [["页面", "提醒与权限"]], {
      route: "permissions",
      confirmLabel: "打开"
    });
    if (/(语音模型|离线语音|语音设置)/.test(text)) return commandResult("navigate", "打开语音设置", [["页面", "语音模型"]], {
      route: "voice",
      confirmLabel: "打开"
    });
    if (/(周视图|周检视|切换到周|查看周)/.test(text)) return commandResult("navigate", "日视图已包含本周", [["视图", "日（含本周）"]], {
      route: "today",
      confirmLabel: "前往"
    });
    if (/(月视图|月检视|切换到月|查看整月)/.test(text)) return commandResult("navigate", "切换到月检视", [["视图", "月"]], {
      route: "month-view",
      confirmLabel: "切换"
    });
    if (/(日视图|今日|今天|日常)/.test(text)) return commandResult("navigate", "回到今天", [["页面", "日常事务"], ["视图", "日"]], {
      route: "today",
      confirmLabel: "前往"
    });
    if (/关键/.test(text)) return commandResult("navigate", "打开关键事项", [["页面", "关键事项"]], {
      route: "critical",
      confirmLabel: "打开"
    });
  }
  if (!wantsCreate && /(删除|移除|删掉|清除)/.test(text)) {
    return target ? commandResult("delete", "\u5220\u9664\u300C".concat(target.task.title, "\u300D"), [["操作", "永久删除"], ["列表", target.kind === "daily" ? "日常事务" : "关键事项"]], {
      target: target,
      confirmLabel: "确认删除"
    }) : commandResult("delete", "没有找到目标任务", [["建议", "说出任务名称中的关键词"]], {
      valid: false,
      error: "请再说一次要删除哪项任务"
    });
  }
  if (!wantsCreate && /(取消勾选|取消完成|标记为未完成|恢复未完成)/.test(text)) {
    return (target === null || target === void 0 ? void 0 : target.kind) === "daily" ? commandResult("uncomplete", "\u53D6\u6D88\u300C".concat(target.task.title, "\u300D\u7684\u52FE\u9009"), [["状态", "未完成"]], {
      target: target,
      confirmLabel: "取消勾选"
    }) : commandResult("uncomplete", "没有找到可取消勾选的日常任务", [["建议", "说出日常任务名称"]], {
      valid: false,
      error: "关键事项完成后会进入历史记录，不能在此取消"
    });
  }
  if (!wantsCreate && /(延期|延长|再续期|续期\s*[零〇一二三四五六七八九十两\d]+\s*天)/.test(text)) {
    var days = parseDelayDays(text);
    return (target === null || target === void 0 ? void 0 : target.kind) === "critical" ? commandResult("extend", "\u5EF6\u957F\u300C".concat(target.task.title, "\u300D"), [["延长", "".concat(days, " \u5929")], ["新的剩余时间", "".concat((target.task.daysLeft || 0) + days, " \u5929")]], {
      target: target,
      days: days,
      confirmLabel: "确认延期"
    }) : commandResult("extend", "没有找到对应的 DDL 任务", [["建议", "说出关键事项名称"]], {
      valid: false,
      error: "只有关键事项可以延期"
    });
  }
  if (!wantsCreate && /(设置|设为|改到|调整).*(ddl|deadline|截止|期限|[零〇一二三四五六七八九十两\d]{1,3}月[零〇一二三四五六七八九十两\d]{1,3}[日号]?)/i.test(text)) {
    var _target$task;
    var deadline = parseDeadline(text);
    var eventTime = parseTime(text);
    var nextTime = eventTime.source ? eventTime.value : (target === null || target === void 0 || (_target$task = target.task) === null || _target$task === void 0 ? void 0 : _target$task.time) || null;
    return (target === null || target === void 0 ? void 0 : target.kind) === "critical" && deadline.deadline ? commandResult("set-deadline", "\u8BBE\u7F6E\u300C".concat(target.task.title, "\u300D\u7684\u671F\u9650"), [["截止日期", deadline.deadline], ["截止时刻", nextTime || "未设置"], ["提醒计划", getCriticalReminder(target.task)]], {
      target: target,
      deadline: deadline,
      eventTime: eventTime.source ? eventTime.value : null,
      confirmLabel: "设置期限"
    }) : commandResult("set-deadline", "期限或目标任务不完整", [["示例", "把年度体检设置到 9 月 30 日截止"]], {
      valid: false,
      error: "请同时说出关键事项名称和具体日期"
    });
  }
  if (!wantsCreate && /(打勾|勾选|标记完成|做完|完成了|^完成)/.test(text)) {
    return target ? commandResult("complete", "\u5B8C\u6210\u300C".concat(target.task.title, "\u300D"), [["结果", target.kind === "daily" ? "计入完成统计" : "移入历史记录"]], {
      target: target,
      confirmLabel: "标记完成"
    }) : commandResult("complete", "没有找到要完成的任务", [["建议", "说出任务名称中的关键词"]], {
      valid: false,
      error: "请再说一次要勾选哪项任务"
    });
  }
  if (!wantsCreate && /(修改|更改|改成|改为|改名|更名|改到|改在|调到|设置|调整|挪到|提前到|延后到)/.test(text)) {
    var _contentMatch$;
    if (!target) return commandResult("edit", "没有找到要修改的任务", [["建议", "先说任务名称，再说新内容"]], {
      valid: false,
      error: "请再说一次要修改哪项任务"
    });
    var contentMatch = text.match(/(?:改成|修改为|更改为|改名为|更名为|改为|改到|改在|调到|设置为?|调整(?:到|为)?|挪到|提前到|延后到)(.+)$/);
    var content = (contentMatch === null || contentMatch === void 0 || (_contentMatch$ = contentMatch[1]) === null || _contentMatch$ === void 0 ? void 0 : _contentMatch$.trim()) || "";
    var parsed = parseVoiceTask(content);
    var noteMatch = text.match(/备注(?:改成|修改为|更改为|改为)(.+)$/);
    var changes = {};
    if (noteMatch) changes.note = noteMatch[1].trim();
    if (parsed.hasTime) {
      if (target.kind === "critical") changes.deadlineTime = parsed.time;else changes.time = parsed.time;
    }
    if (parsed.hasRepeat) {
      changes.repeat = parsed.repeat;
      changes.repeatDays = parsed.repeatDays;
    }
    if (parsed.hasReminder && target.kind === "daily") changes.reminder = parsed.reminder;
    if (parsed.hasDeadline) {
      changes.deadline = parsed.deadline;
      changes.daysLeft = parsed.daysLeft;
    }
    if (/(?:改成|改为|设为|调整为).*(关键|特殊)/.test(text)) changes.type = "critical";
    if (/(?:改成|改为|设为|调整为).*(日常|每日)/.test(text)) changes.type = "daily";
    if (!noteMatch && parsed.title !== "未命名事项") changes.title = parsed.title;
    var _rows = Object.entries(changes).filter(function (_ref3) {
      var _ref4 = _slicedToArray(_ref3, 1),
        key = _ref4[0];
      return key !== "repeatDays";
    }).map(function (_ref5) {
      var _ref6 = _slicedToArray(_ref5, 2),
        key = _ref6[0],
        value = _ref6[1];
      return [{
        title: "名称",
        type: "类型",
        time: "时间",
        deadlineTime: "截止时刻",
        repeat: "重复",
        reminder: "提醒",
        deadline: "截止日期",
        daysLeft: "剩余天数",
        note: "备注"
      }[key] || key, key === "type" ? value === "critical" ? "关键事项" : "日常事项" : value];
    });
    return _rows.length ? commandResult("edit", "\u4FEE\u6539\u300C".concat(target.task.title, "\u300D"), _rows, {
      target: target,
      changes: changes,
      confirmLabel: "确认修改"
    }) : commandResult("edit", "没有识别到修改内容", [["示例", "把健身改成慢跑 30 分钟"]], {
      valid: false,
      error: "请说明要改成什么"
    });
  }
  if (!wantsCreate && /(清空|清除|删除|移除|完成|勾选|取消|修改|更改|设置|调整|切换|打开|查看|查询|延期|延长|续期)/.test(text)) {
    return commandResult("clarify", "还需要一点信息", [["原话", text], ["需要", "任务名称或更明确的操作"]], {
      valid: false,
      error: "我不会把这句话创建成任务，请补充要操作的事项"
    });
  }
  var task = parseVoiceTask(text);
  var rows = task.span ? [["类型", "时间段"], ["去程", task.span.start.deadline], ["返程", task.span.end.deadline], ["记录", "生成两个关联 DDL"]] : task.type === "critical" ? [["类型", "关键事务"], ["截止日期", task.deadline || "未设置"], ["截止时刻", task.deadlineTime || "未设置"], ["提醒计划", task.reminder]] : [["类型", "日常事务"], ["重复", task.repeat], ["时间", task.endTime ? "".concat(task.time, "\u2014").concat(task.endTime) : task.time], ["提醒", task.reminder]];
  return commandResult("create", task.title, rows, {
    task: task,
    analysis: domainAnalysis,
    confidence: domainAnalysis === null || domainAnalysis === void 0 || (_domainAnalysis$inten2 = domainAnalysis.intent) === null || _domainAnalysis$inten2 === void 0 ? void 0 : _domainAnalysis$inten2.confidence,
    confirmLabel: "创建任务"
  });
}
function useViewportScale(width, height) {
  var _useState = useState(1),
    _useState2 = _slicedToArray(_useState, 2),
    scale = _useState2[0],
    setScale = _useState2[1];
  useEffect(function () {
    var update = function update() {
      var availableWidth = window.innerWidth - 40;
      var availableHeight = window.innerHeight - 56;
      setScale(Math.min(availableWidth / width, availableHeight / height, 1));
    };
    update();
    window.addEventListener("resize", update);
    return function () {
      return window.removeEventListener("resize", update);
    };
  }, [width, height]);
  return scale;
}
function getNativeWindowState(payload) {
  try {
    var _window$JinkeAndroid, _window$JinkeAndroid$;
    var previewMode = new URLSearchParams(window.location.search).get("native");
    var hasNativeBridge = Boolean((_window$JinkeAndroid = window.JinkeAndroid) === null || _window$JinkeAndroid === void 0 || (_window$JinkeAndroid$ = _window$JinkeAndroid.isNativeApp) === null || _window$JinkeAndroid$ === void 0 ? void 0 : _window$JinkeAndroid$.call(_window$JinkeAndroid));
    if (!hasNativeBridge && !["phone", "expanded"].includes(previewMode)) return null;
    if (!hasNativeBridge) {
      var _width = Math.max(1, window.innerWidth || 1);
      var _height = Math.max(1, window.innerHeight || 1);
      return {
        width: _width,
        height: _height,
        ratio: Math.min(_width, _height) / Math.max(_width, _height),
        expanded: previewMode === "expanded"
      };
    }
    var supplied = typeof payload === "string" ? JSON.parse(payload) : payload;
    var nativeInfo = supplied || JSON.parse(window.JinkeAndroid.getWindowLayout());
    var width = Math.max(1, Number(nativeInfo.widthDp) || window.innerWidth || 1);
    var height = Math.max(1, Number(nativeInfo.heightDp) || window.innerHeight || 1);
    var ratio = Math.min(width, height) / Math.max(width, height);
    return {
      width: width,
      height: height,
      ratio: ratio,
      expanded: nativeInfo.expanded === true || ratio >= 0.68
    };
  } catch (_unused4) {
    return null;
  }
}
function getNativeCapabilities(payload) {
  try {
    var _window$JinkeAndroid2;
    var supplied = typeof payload === "string" ? JSON.parse(payload) : payload;
    if (supplied && _typeof(supplied) === "object") return supplied;
    if ((_window$JinkeAndroid2 = window.JinkeAndroid) !== null && _window$JinkeAndroid2 !== void 0 && _window$JinkeAndroid2.getSystemCapabilities) return JSON.parse(window.JinkeAndroid.getSystemCapabilities());
  } catch (_unused5) {}
  return null;
}
function MobileDesignApp() {
  var _useState3 = useState(function () {
      try {
        return localStorage.getItem("jinke-theme") || "dark";
      } catch (_unused6) {
        return "dark";
      }
    }),
    _useState4 = _slicedToArray(_useState3, 2),
    themeMode = _useState4[0],
    setThemeMode = _useState4[1];
  var _useState5 = useState(function () {
      try {
        return localStorage.getItem("jinke-default-alert-mode") === "silent" ? "silent" : "sound";
      } catch (_unused7) {
        return "sound";
      }
    }),
    _useState6 = _slicedToArray(_useState5, 2),
    defaultAlertMode = _useState6[0],
    setDefaultAlertMode = _useState6[1];
  var _useState7 = useState(function () {
      try {
        return localStorage.getItem("jinke-default-sound-id") || "chime";
      } catch (_unused8) {
        return "chime";
      }
    }),
    _useState8 = _slicedToArray(_useState7, 2),
    defaultSoundId = _useState8[0],
    setDefaultSoundId = _useState8[1];
  var _useState9 = useState(function () {
      return readStoredJson("jinke-custom-reminder-sounds", [], Array.isArray);
    }),
    _useState10 = _slicedToArray(_useState9, 2),
    customSounds = _useState10[0],
    setCustomSounds = _useState10[1];
  var _useState11 = useState("today"),
    _useState12 = _slicedToArray(_useState11, 2),
    activeTab = _useState12[0],
    setActiveTab = _useState12[1];
  var _useState13 = useState([]),
    _useState14 = _slicedToArray(_useState13, 2),
    secondaryStack = _useState14[0],
    setSecondaryStack = _useState14[1];
  var secondary = secondaryStack[secondaryStack.length - 1] || null;
  var setSecondary = function setSecondary(route) {
    return setSecondaryStack(route ? [route] : []);
  };
  var pushSecondary = function pushSecondary(route) {
    return setSecondaryStack(function (current) {
      return [].concat(_toConsumableArray(current), [route]);
    });
  };
  var _useState15 = useState(null),
    _useState16 = _slicedToArray(_useState15, 2),
    overlay = _useState16[0],
    setOverlay = _useState16[1];
  var _useState17 = useState("day"),
    _useState18 = _slicedToArray(_useState17, 2),
    viewMode = _useState18[0],
    setViewMode = _useState18[1];
  var _useState19 = useState(function () {
      return localDateKey();
    }),
    _useState20 = _slicedToArray(_useState19, 2),
    todayDateKey = _useState20[0],
    setTodayDateKey = _useState20[1];
  var _useState21 = useState(function () {
      return localDateKey();
    }),
    _useState22 = _slicedToArray(_useState21, 2),
    selectedDateKey = _useState22[0],
    setSelectedDateKey = _useState22[1];
  var _useState23 = useState(function () {
      return readStoredJson("jinke-daily-tasks", APP_DATA.dailyTasks, Array.isArray);
    }),
    _useState24 = _slicedToArray(_useState23, 2),
    dailyTasks = _useState24[0],
    setDailyTasks = _useState24[1];
  var _useState25 = useState(function () {
      return readStoredJson("jinke-daily-completions", Object.fromEntries(APP_DATA.dailyTasks.map(function (task) {
        return ["".concat(task.id, ":").concat(localDateKey()), Boolean(task.done)];
      })), function (value) {
        return value && _typeof(value) === "object" && !Array.isArray(value);
      });
    }),
    _useState26 = _slicedToArray(_useState25, 2),
    dailyCompletionByDate = _useState26[0],
    setDailyCompletionByDate = _useState26[1];
  var _useState27 = useState(function () {
      var anchorDateKey = localDateKey();
      return readStoredJson("jinke-critical-tasks", APP_DATA.criticalTasks, Array.isArray).map(function (task) {
        return withCriticalReminderDefaults(normalizeCriticalCompletion(task.deadline && Number.isFinite(task.daysLeft) && !task.anchorDateKey ? _objectSpread(_objectSpread({}, task), {}, {
          anchorDateKey: anchorDateKey
        }) : task, anchorDateKey));
      });
    }),
    _useState28 = _slicedToArray(_useState27, 2),
    criticalTasks = _useState28[0],
    setCriticalTasks = _useState28[1];
  var _useState29 = useState(function () {
      var value = "10:00";
      try {
        value = localStorage.getItem("jinke-ddl-reminder-time") || value;
      } catch (_unused9) {}
      window.JINKE_DDL_REMINDER_TIME = value;
      return value;
    }),
    _useState30 = _slicedToArray(_useState29, 2),
    ddlReminderTime = _useState30[0],
    setDdlReminderTime = _useState30[1];
  var _useState31 = useState(function () {
      var value = 5;
      try {
        value = Math.max(1, Number(localStorage.getItem("jinke-ddl-reminder-multiple")) || value);
      } catch (_unused10) {}
      window.JINKE_DDL_REMINDER_MULTIPLE = value;
      return value;
    }),
    _useState32 = _slicedToArray(_useState31, 2),
    ddlReminderMultiple = _useState32[0],
    setDdlReminderMultiple = _useState32[1];
  var _useState33 = useState(function () {
      var value = 5;
      try {
        var stored = localStorage.getItem("jinke-ddl-reminder-final-days");
        if (stored !== null) value = Math.max(0, Number(stored) || 0);
      } catch (_unused11) {}
      window.JINKE_DDL_REMINDER_FINAL_DAYS = value;
      return value;
    }),
    _useState34 = _slicedToArray(_useState33, 2),
    ddlReminderFinalDays = _useState34[0],
    setDdlReminderFinalDays = _useState34[1];
  var _useState35 = useState(function () {
      var stored = readStoredJson("jinke-task-history", APP_DATA.history, Array.isArray).map(function (item) {
        if (!item.completedDateKey || !item.sourceTaskId) return item;
        var _item$completedDateKe = item.completedDateKey.split("-").map(Number),
          _item$completedDateKe2 = _slicedToArray(_item$completedDateKe, 3),
          month = _item$completedDateKe2[1],
          day = _item$completedDateKe2[2];
        return _objectSpread(_objectSpread({}, item), {}, {
          completed: "".concat(month, "\u6708").concat(day, "\u65E5")
        });
      });
      var migrated = criticalTasks.filter(function (task) {
        return task.done && task.completedDateKey;
      }).map(function (task) {
        return criticalHistoryEntry(task, task.completedDateKey, task.anchorDateKey || task.completedDateKey);
      });
      return [].concat(_toConsumableArray(migrated.filter(function (entry) {
        return !stored.some(function (item) {
          return item.completionKey === entry.completionKey;
        });
      })), _toConsumableArray(stored));
    }),
    _useState36 = _slicedToArray(_useState35, 2),
    history = _useState36[0],
    setHistory = _useState36[1];
  var _useState37 = useState(null),
    _useState38 = _slicedToArray(_useState37, 2),
    deleteTarget = _useState38[0],
    setDeleteTarget = _useState38[1];
  var _useState39 = useState(null),
    _useState40 = _slicedToArray(_useState39, 2),
    selectedDaily = _useState40[0],
    setSelectedDaily = _useState40[1];
  var _useState41 = useState(null),
    _useState42 = _slicedToArray(_useState41, 2),
    dailyDraft = _useState42[0],
    setDailyDraft = _useState42[1];
  var _useState43 = useState(null),
    _useState44 = _slicedToArray(_useState43, 2),
    selectedCritical = _useState44[0],
    setSelectedCritical = _useState44[1];
  var _useState45 = useState(null),
    _useState46 = _slicedToArray(_useState45, 2),
    criticalDraft = _useState46[0],
    setCriticalDraft = _useState46[1];
  var _useState47 = useState(7),
    _useState48 = _slicedToArray(_useState47, 2),
    renewDays = _useState48[0],
    setRenewDays = _useState48[1];
  var _useState49 = useState("listening"),
    _useState50 = _slicedToArray(_useState49, 2),
    voicePhase = _useState50[0],
    setVoicePhase = _useState50[1];
  var _useState51 = useState(""),
    _useState52 = _slicedToArray(_useState51, 2),
    transcript = _useState52[0],
    setTranscript = _useState52[1];
  var _useState53 = useState(null),
    _useState54 = _slicedToArray(_useState53, 2),
    voiceDraft = _useState54[0],
    setVoiceDraft = _useState54[1];
  var _useState55 = useState("china"),
    _useState56 = _slicedToArray(_useState55, 2),
    archiveActive = _useState56[0],
    setArchiveActive = _useState56[1];
  var _useState57 = useState(0),
    _useState58 = _slicedToArray(_useState57, 2),
    archiveIndex = _useState58[0],
    setArchiveIndex = _useState58[1];
  var _useState59 = useState(true),
    _useState60 = _slicedToArray(_useState59, 2),
    speechAvailable = _useState60[0],
    setSpeechAvailable = _useState60[1];
  var _useState61 = useState("idle"),
    _useState62 = _slicedToArray(_useState61, 2),
    speechStatus = _useState62[0],
    setSpeechStatus = _useState62[1];
  var speechCandidatesRef = useRef([]);
  var _useState63 = useState(""),
    _useState64 = _slicedToArray(_useState63, 2),
    toast = _useState64[0],
    setToast = _useState64[1];
  var _useState65 = useState(function () {
      return getNativeWindowState();
    }),
    _useState66 = _slicedToArray(_useState65, 2),
    nativeWindow = _useState66[0],
    setNativeWindow = _useState66[1];
  var _useState67 = useState(function () {
      return getNativeCapabilities();
    }),
    _useState68 = _slicedToArray(_useState67, 2),
    nativeCapabilities = _useState68[0],
    setNativeCapabilities = _useState68[1];
  var recognitionRef = useRef(null);
  var voiceInputModeRef = useRef("offline");
  var toastTimerRef = useRef(null);
  var overlayClosingRef = useRef(false);
  var overlayCloseTimerRef = useRef(null);
  var secondaryClosingRef = useRef(false);
  var secondaryCloseTimerRef = useRef(null);
  var soundImportCallbackRef = useRef(null);
  var reminderSounds = [].concat(BUILT_IN_REMINDER_SOUNDS, _toConsumableArray(customSounds));
  var scale = useViewportScale(SIMULATOR_WIDTH, SIMULATOR_HEIGHT);
  var currentCriticalTasks = criticalTasks.map(function (task) {
    return _objectSpread(_objectSpread({}, task), {}, {
      daysLeft: criticalDaysLeftOn(task, todayDateKey, todayDateKey)
    });
  });
  var activeCriticalTasks = currentCriticalTasks.filter(function (task) {
    return !task.done;
  });
  var parsedVoiceCommand = transcript.trim() ? parseVoiceCommand(transcript, dailyTasks, currentCriticalTasks) : commandResult("invalid", "没有识别到内容", [["建议", "再说一次，或直接输入安排"]], {
    valid: false,
    error: "没有听清，请重试"
  });
  var displayedDailyTasks = dailyTasks.filter(function (task) {
    return taskOccursOnDate(task, selectedDateKey, todayDateKey);
  }).map(function (task) {
    return _objectSpread(_objectSpread({}, task), {}, {
      done: Boolean(dailyCompletionByDate["".concat(task.id, ":").concat(selectedDateKey)])
    });
  });
  var displayedDeadlineTasks = criticalTasks.filter(function (task) {
    return criticalTaskVisibleOnTodayDate(task, selectedDateKey);
  }).map(function (task) {
    return _objectSpread(_objectSpread({}, task), {}, {
      daysLeft: criticalDaysLeftOn(task, selectedDateKey, todayDateKey)
    });
  });
  var getDateTaskLoad = function getDateTaskLoad(dateKey) {
    return countScheduledTasksOnDate(dailyTasks, criticalTasks, dateKey, todayDateKey);
  };
  useEffect(function () {
    if (voicePhase === "review" && parsedVoiceCommand.intent === "create") setVoiceDraft(_objectSpread({}, parsedVoiceCommand.task));
  }, [voicePhase, transcript]);
  useEffect(function () {
    var refreshSystemDate = function refreshSystemDate() {
      var nextDateKey = localDateKey(new Date());
      setTodayDateKey(function (currentDateKey) {
        if (currentDateKey === nextDateKey) return currentDateKey;
        setSelectedDateKey(function (selected) {
          return selected === currentDateKey ? nextDateKey : selected;
        });
        return nextDateKey;
      });
    };
    var onVisibilityChange = function onVisibilityChange() {
      if (document.visibilityState === "visible") refreshSystemDate();
    };
    window.JINKE_REFRESH_SYSTEM_TIME = refreshSystemDate;
    window.addEventListener("focus", refreshSystemDate);
    window.addEventListener("pageshow", refreshSystemDate);
    document.addEventListener("visibilitychange", onVisibilityChange);
    var clockTimer = window.setInterval(refreshSystemDate, 30000);
    refreshSystemDate();
    return function () {
      window.clearInterval(clockTimer);
      window.removeEventListener("focus", refreshSystemDate);
      window.removeEventListener("pageshow", refreshSystemDate);
      document.removeEventListener("visibilitychange", onVisibilityChange);
      if (window.JINKE_REFRESH_SYSTEM_TIME === refreshSystemDate) delete window.JINKE_REFRESH_SYSTEM_TIME;
    };
  }, []);
  useEffect(function () {
    var media = window.matchMedia("(prefers-color-scheme: dark)");
    var applyTheme = function applyTheme() {
      var _document$querySelect;
      var resolved = themeMode === "system" ? media.matches ? "dark" : "light" : themeMode;
      document.documentElement.dataset.theme = resolved;
      document.documentElement.style.colorScheme = resolved;
      (_document$querySelect = document.querySelector('meta[name="theme-color"]')) === null || _document$querySelect === void 0 || _document$querySelect.setAttribute("content", resolved === "dark" ? "#171512" : "#F8F6F2");
    };
    applyTheme();
    try {
      localStorage.setItem("jinke-theme", themeMode);
    } catch (_unused12) {}
    if (themeMode === "system") media.addEventListener("change", applyTheme);
    return function () {
      return media.removeEventListener("change", applyTheme);
    };
  }, [themeMode]);
  useEffect(function () {
    if (!nativeWindow) return undefined;
    var updateNativeWindow = function updateNativeWindow(payload) {
      var next = getNativeWindowState(payload);
      if (next) setNativeWindow(next);
    };
    window.JINKE_NATIVE_WINDOW_CHANGED = updateNativeWindow;
    var onResize = function onResize() {
      return updateNativeWindow();
    };
    window.addEventListener("resize", onResize);
    updateNativeWindow();
    return function () {
      window.removeEventListener("resize", onResize);
      if (window.JINKE_NATIVE_WINDOW_CHANGED === updateNativeWindow) delete window.JINKE_NATIVE_WINDOW_CHANGED;
    };
  }, [Boolean(nativeWindow)]);
  useEffect(function () {
    var _window$JinkeAndroid3;
    if (!((_window$JinkeAndroid3 = window.JinkeAndroid) !== null && _window$JinkeAndroid3 !== void 0 && _window$JinkeAndroid3.getSystemCapabilities)) return undefined;
    var updateCapabilities = function updateCapabilities(payload) {
      return setNativeCapabilities(getNativeCapabilities(payload));
    };
    window.JINKE_NATIVE_CAPABILITIES_CHANGED = updateCapabilities;
    updateCapabilities();
    return function () {
      if (window.JINKE_NATIVE_CAPABILITIES_CHANGED === updateCapabilities) delete window.JINKE_NATIVE_CAPABILITIES_CHANGED;
    };
  }, []);
  useEffect(function () {
    try {
      localStorage.setItem("jinke-daily-tasks", JSON.stringify(dailyTasks));
    } catch (_unused13) {}
  }, [dailyTasks]);
  useEffect(function () {
    try {
      localStorage.setItem("jinke-daily-completions", JSON.stringify(dailyCompletionByDate));
    } catch (_unused14) {}
  }, [dailyCompletionByDate]);
  useEffect(function () {
    try {
      localStorage.setItem("jinke-critical-tasks", JSON.stringify(criticalTasks));
    } catch (_unused15) {}
  }, [criticalTasks]);
  useEffect(function () {
    try {
      localStorage.setItem("jinke-task-history", JSON.stringify(history));
    } catch (_unused16) {}
  }, [history]);
  useEffect(function () {
    try {
      localStorage.setItem("jinke-default-alert-mode", defaultAlertMode);
      localStorage.setItem("jinke-default-sound-id", defaultSoundId);
      localStorage.setItem("jinke-custom-reminder-sounds", JSON.stringify(customSounds.map(function (_ref7) {
        var objectUrl = _ref7.objectUrl,
          sound = _objectWithoutProperties(_ref7, _excluded);
        return sound;
      })));
    } catch (_unused17) {}
  }, [defaultAlertMode, defaultSoundId, customSounds]);
  useEffect(function () {
    var receiveImportedSound = function receiveImportedSound(payload) {
      try {
        var sound = typeof payload === "string" ? JSON.parse(payload) : payload;
        if (!(sound !== null && sound !== void 0 && sound.id) || !(sound !== null && sound !== void 0 && sound.name)) return;
        var normalizedSound = _objectSpread(_objectSpread({}, sound), {}, {
          source: sound.source || "local"
        });
        setCustomSounds(function (current) {
          return [].concat(_toConsumableArray(current.filter(function (item) {
            return item.id !== sound.id;
          })), [normalizedSound]);
        });
        var callback = soundImportCallbackRef.current;
        soundImportCallbackRef.current = null;
        callback === null || callback === void 0 || callback(sound.id);
        showToast(normalizedSound.source === "system-alarm" ? "\u5DF2\u9009\u62E9\u95F9\u94C3\uFF1A".concat(sound.name) : "\u5DF2\u5BFC\u5165\uFF1A".concat(sound.name));
      } catch (_unused18) {
        showToast("音效导入失败");
      }
    };
    window.JINKE_SOUND_IMPORTED = receiveImportedSound;
    return function () {
      if (window.JINKE_SOUND_IMPORTED === receiveImportedSound) delete window.JINKE_SOUND_IMPORTED;
    };
  }, []);
  useEffect(function () {
    window.JINKE_DDL_REMINDER_TIME = ddlReminderTime;
    window.JINKE_DDL_REMINDER_MULTIPLE = ddlReminderMultiple;
    window.JINKE_DDL_REMINDER_FINAL_DAYS = ddlReminderFinalDays;
    try {
      localStorage.setItem("jinke-ddl-reminder-time", ddlReminderTime);
    } catch (_unused19) {}
    try {
      localStorage.setItem("jinke-ddl-reminder-multiple", String(ddlReminderMultiple));
    } catch (_unused20) {}
    try {
      localStorage.setItem("jinke-ddl-reminder-final-days", String(ddlReminderFinalDays));
    } catch (_unused21) {}
  }, [ddlReminderTime, ddlReminderMultiple, ddlReminderFinalDays]);
  useEffect(function () {
    var _window$JinkeAndroid4;
    if (!((_window$JinkeAndroid4 = window.JinkeAndroid) !== null && _window$JinkeAndroid4 !== void 0 && _window$JinkeAndroid4.syncDdlReminders)) return;
    var payload = criticalTasks.filter(function (task) {
      return !task.done;
    }).map(function (task) {
      return _objectSpread(_objectSpread({}, task), {}, {
        daysLeft: criticalDaysLeftOn(task, todayDateKey, todayDateKey)
      });
    }).filter(function (task) {
      return task.deadline && Number.isFinite(task.daysLeft) && task.daysLeft >= 0;
    }).map(function (task) {
      var plan = normalizeCriticalReminderPlan(task, ddlReminderTime, ddlReminderMultiple, ddlReminderFinalDays);
      return _objectSpread({
        id: task.id,
        title: task.title,
        daysLeft: task.daysLeft,
        deadlineTime: task.deadlineTime || null,
        alertMode: task.alertMode && task.alertMode !== "inherit" ? task.alertMode : defaultAlertMode,
        soundId: task.soundId && task.soundId !== "inherit" ? task.soundId : defaultSoundId
      }, plan);
    });
    try {
      window.JinkeAndroid.syncDdlReminders(JSON.stringify(payload), ddlReminderTime, ddlReminderMultiple, ddlReminderFinalDays);
    } catch (_unused22) {}
  }, [criticalTasks, todayDateKey, ddlReminderTime, ddlReminderMultiple, ddlReminderFinalDays, defaultAlertMode, defaultSoundId]);
  useEffect(function () {
    var _window$JinkeAndroid5;
    if (!((_window$JinkeAndroid5 = window.JinkeAndroid) !== null && _window$JinkeAndroid5 !== void 0 && _window$JinkeAndroid5.syncDailyReminders)) return;
    var payload = dailyTasks.map(function (task) {
      var reminderLeadMinutes = dailyReminderLeadMinutes(task.reminder);
      var completionPrefix = "".concat(task.id, ":");
      var completedDateKeys = Object.entries(dailyCompletionByDate).filter(function (_ref8) {
        var _ref9 = _slicedToArray(_ref8, 2),
          key = _ref9[0],
          completed = _ref9[1];
        return completed && key.startsWith(completionPrefix);
      }).map(function (_ref10) {
        var _ref11 = _slicedToArray(_ref10, 1),
          key = _ref11[0];
        return key.slice(completionPrefix.length);
      });
      return {
        id: task.id,
        title: task.title,
        time: task.time,
        repeatDays: repeatDaysFromValue(task.repeat, task.repeatDays),
        scheduledDateKey: task.scheduledDateKey || "",
        activeFrom: task.activeFrom || todayDateKey,
        activeUntil: task.activeUntil || "",
        reminderEnabled: reminderLeadMinutes !== null,
        reminderLeadMinutes: reminderLeadMinutes || 0,
        completionDateKey: todayDateKey,
        completed: Boolean(dailyCompletionByDate["".concat(task.id, ":").concat(todayDateKey)]),
        completedDateKeys: completedDateKeys,
        alertMode: task.alertMode && task.alertMode !== "inherit" ? task.alertMode : defaultAlertMode,
        soundId: task.soundId && task.soundId !== "inherit" ? task.soundId : defaultSoundId
      };
    });
    try {
      window.JinkeAndroid.syncDailyReminders(JSON.stringify(payload));
    } catch (_unused23) {}
  }, [dailyTasks, dailyCompletionByDate, todayDateKey, defaultAlertMode, defaultSoundId]);
  useEffect(function () {
    return function () {
      if (recognitionRef.current) recognitionRef.current.abort();
      if (toastTimerRef.current) window.clearTimeout(toastTimerRef.current);
      if (overlayCloseTimerRef.current) window.clearTimeout(overlayCloseTimerRef.current);
      if (secondaryCloseTimerRef.current) window.clearTimeout(secondaryCloseTimerRef.current);
    };
  }, []);
  var showToast = function showToast(message) {
    setToast(message);
    if (toastTimerRef.current) window.clearTimeout(toastTimerRef.current);
    toastTimerRef.current = window.setTimeout(function () {
      return setToast("");
    }, 2200);
  };
  var previewReminderSound = function previewReminderSound(soundId) {
    try {
      var _window$JinkeAndroid6;
      if ((_window$JinkeAndroid6 = window.JinkeAndroid) !== null && _window$JinkeAndroid6 !== void 0 && _window$JinkeAndroid6.previewReminderSound) {
        window.JinkeAndroid.previewReminderSound(soundId || "chime");
        return;
      }
      var custom = customSounds.find(function (sound) {
        return sound.id === soundId && sound.objectUrl;
      });
      if (custom) {
        var audio = new Audio(custom.objectUrl);
        audio.play().catch(function () {
          return showToast("浏览器未允许播放音效");
        });
        return;
      }
      var AudioContext = window.AudioContext || window.webkitAudioContext;
      if (!AudioContext) return;
      var context = new AudioContext();
      var patterns = {
        chime: [[660, 0, 0.16], [880, 0.13, 0.25]],
        bell: [[784, 0, 0.35], [1046, 0.05, 0.45]],
        glass: [[988, 0, 0.12], [1318, 0.11, 0.32]],
        pop: [[520, 0, 0.12]],
        soft: [[440, 0, 0.22], [554, 0.17, 0.28]]
      };
      (patterns[soundId] || patterns.chime).forEach(function (_ref12) {
        var _ref13 = _slicedToArray(_ref12, 3),
          frequency = _ref13[0],
          delay = _ref13[1],
          duration = _ref13[2];
        var oscillator = context.createOscillator();
        var gain = context.createGain();
        var start = context.currentTime + delay;
        oscillator.frequency.value = frequency;
        oscillator.type = "sine";
        gain.gain.setValueAtTime(0.0001, start);
        gain.gain.exponentialRampToValueAtTime(0.2, start + 0.02);
        gain.gain.exponentialRampToValueAtTime(0.0001, start + duration);
        oscillator.connect(gain).connect(context.destination);
        oscillator.start(start);
        oscillator.stop(start + duration + 0.02);
      });
      window.setTimeout(function () {
        return context.close();
      }, 900);
    } catch (_unused24) {
      showToast("音效试听失败");
    }
  };
  var importReminderSound = function importReminderSound(onSelected) {
    var _window$JinkeAndroid7;
    soundImportCallbackRef.current = onSelected || null;
    if ((_window$JinkeAndroid7 = window.JinkeAndroid) !== null && _window$JinkeAndroid7 !== void 0 && _window$JinkeAndroid7.pickReminderSound) {
      try {
        window.JinkeAndroid.pickReminderSound();
        return;
      } catch (_unused25) {}
    }
    var input = document.createElement("input");
    input.type = "file";
    input.accept = "audio/*";
    input.onchange = function () {
      var _input$files;
      var file = (_input$files = input.files) === null || _input$files === void 0 ? void 0 : _input$files[0];
      if (!file) {
        soundImportCallbackRef.current = null;
        return;
      }
      var sound = {
        id: "web-".concat(Date.now()),
        name: file.name.replace(/\.[^.]+$/, "") || "本地音效",
        source: "local",
        objectUrl: URL.createObjectURL(file)
      };
      setCustomSounds(function (current) {
        return [].concat(_toConsumableArray(current), [sound]);
      });
      var callback = soundImportCallbackRef.current;
      soundImportCallbackRef.current = null;
      callback === null || callback === void 0 || callback(sound.id);
      showToast("\u5DF2\u5BFC\u5165\uFF1A".concat(sound.name));
    };
    input.click();
  };
  var pickSystemAlarmSound = function pickSystemAlarmSound(onSelected) {
    var _window$JinkeAndroid8;
    var currentSoundId = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "";
    soundImportCallbackRef.current = onSelected || null;
    if ((_window$JinkeAndroid8 = window.JinkeAndroid) !== null && _window$JinkeAndroid8 !== void 0 && _window$JinkeAndroid8.pickSystemAlarmSound) {
      try {
        window.JinkeAndroid.pickSystemAlarmSound(currentSoundId !== null && currentSoundId !== void 0 && currentSoundId.startsWith("alarm:") ? currentSoundId : "");
        return;
      } catch (_unused26) {}
    }
    soundImportCallbackRef.current = null;
    showToast("系统闹铃库仅在手机 App 中可用");
  };
  var changeDdlReminderTime = function changeDdlReminderTime(time) {
    var nextTime = /^\d{2}:\d{2}$/.test(time) ? time : "10:00";
    window.JINKE_DDL_REMINDER_TIME = nextTime;
    setDdlReminderTime(nextTime);
    showToast("DDL \u63D0\u9192\u65F6\u95F4\u5DF2\u6539\u4E3A ".concat(nextTime));
  };
  var changeDdlReminderMultiple = function changeDdlReminderMultiple(value) {
    var next = Math.min(3650, Math.max(1, Number(value) || 1));
    window.JINKE_DDL_REMINDER_MULTIPLE = next;
    setDdlReminderMultiple(next);
    showToast("DDL \u63D0\u9192\u8282\u70B9\u5DF2\u6539\u4E3A\u6BCF ".concat(next, " \u5929"));
  };
  var changeDdlReminderFinalDays = function changeDdlReminderFinalDays(value) {
    var next = Math.min(3650, Math.max(0, Number(value) || 0));
    window.JINKE_DDL_REMINDER_FINAL_DAYS = next;
    setDdlReminderFinalDays(next);
    showToast("DDL \u4E34\u8FD1\u63D0\u9192\u5DF2\u6539\u4E3A\u6700\u540E ".concat(next, " \u5929"));
  };
  var toggleDaily = function toggleDaily(taskId) {
    var completionKey = "".concat(taskId, ":").concat(selectedDateKey);
    var nowDone = !Boolean(dailyCompletionByDate[completionKey]);
    setDailyCompletionByDate(function (current) {
      return _objectSpread(_objectSpread({}, current), {}, _defineProperty({}, completionKey, nowDone));
    });
    if (nowDone) showToast("已完成，记入本月统计");
  };
  var setCriticalCompleted = function setCriticalCompleted(taskId) {
    var shouldComplete = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
    var task = criticalTasks.find(function (item) {
      return item.id === taskId;
    });
    if (!task) return;
    if (shouldComplete) {
      var completionDateKey = localDateKey();
      var completionKey = "".concat(taskId, ":").concat(completionDateKey);
      var completedTask = _objectSpread(_objectSpread({}, completeCriticalForDate(task, completionDateKey)), {}, {
        completionKey: completionKey
      });
      setCriticalTasks(function (current) {
        return current.map(function (item) {
          return item.id === taskId ? completedTask : item;
        });
      });
      setHistory(function (current) {
        return current.some(function (item) {
          return item.completionKey === completionKey;
        }) ? current : [criticalHistoryEntry(task, completionDateKey, todayDateKey)].concat(_toConsumableArray(current));
      });
      showToast("已完成，保留在今天的任务列表");
      return;
    }
    setCriticalTasks(function (current) {
      return current.map(function (item) {
        return item.id === taskId ? uncompleteCriticalTask(item) : item;
      });
    });
    setHistory(function (current) {
      return current.filter(function (item) {
        return item.sourceTaskId !== taskId;
      });
    });
    showToast("已取消完成，恢复到关键事项");
  };
  var toggleCriticalCheck = function toggleCriticalCheck(taskId) {
    var task = criticalTasks.find(function (item) {
      return item.id === taskId;
    });
    if (task) setCriticalCompleted(taskId, !task.done);
  };
  var selectDate = function selectDate(dateKey) {
    setSelectedDateKey(dateKey);
  };
  var openDaily = function openDaily(task) {
    setSelectedDaily(task);
    setDailyDraft(_objectSpread(_objectSpread({}, task), {}, {
      alertMode: task.alertMode || "inherit",
      soundId: task.soundId || "inherit",
      repeatDays: repeatDaysFromValue(task.repeat, task.repeatDays),
      reminder: task.reminder || "到点提醒"
    }));
    setOverlay("daily-edit");
  };
  var requestDelete = function requestDelete(task, kind) {
    setDeleteTarget({
      task: task,
      kind: kind
    });
    setOverlay("delete-confirm");
  };
  var confirmDelete = function confirmDelete() {
    var mode = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "all";
    if (!(deleteTarget !== null && deleteTarget !== void 0 && deleteTarget.task)) return;
    var task = deleteTarget.task,
      kind = deleteTarget.kind;
    if (kind === "daily") {
      if (mode === "future") {
        var activeUntil = shiftDateKeyByDays(selectedDateKey, -1);
        setDailyTasks(function (current) {
          return current.flatMap(function (item) {
            if (item.id !== task.id) return [item];
            if (item.activeFrom && item.activeFrom > activeUntil) return [];
            return [_objectSpread(_objectSpread({}, item), {}, {
              activeUntil: item.activeUntil && item.activeUntil < activeUntil ? item.activeUntil : activeUntil
            })];
          });
        });
        setDailyCompletionByDate(function (current) {
          return Object.fromEntries(Object.entries(current).filter(function (_ref14) {
            var _ref15 = _slicedToArray(_ref14, 1),
              key = _ref15[0];
            var separator = key.indexOf(":");
            var taskId = separator < 0 ? key : key.slice(0, separator);
            var dateKey = separator < 0 ? "" : key.slice(separator + 1);
            return taskId !== task.id || dateKey < selectedDateKey;
          }));
        });
      } else {
        setDailyTasks(function (current) {
          return current.filter(function (item) {
            return item.id !== task.id;
          });
        });
        setDailyCompletionByDate(function (current) {
          return Object.fromEntries(Object.entries(current).filter(function (_ref16) {
            var _ref17 = _slicedToArray(_ref16, 1),
              key = _ref17[0];
            return key.split(":")[0] !== task.id;
          }));
        });
      }
    } else {
      setCriticalTasks(function (current) {
        return current.filter(function (item) {
          return item.id !== task.id;
        });
      });
    }
    closeOverlay();
    showToast(kind === "daily" && mode === "future" ? "\u5DF2\u505C\u6B62\u300C".concat(task.title, "\u300D\u7684\u540E\u7EED\u5B89\u6392") : "\u5DF2\u5220\u9664\u300C".concat(task.title, "\u300D"));
  };
  var saveDaily = function saveDaily(taskId, changes) {
    setDailyTasks(function (current) {
      return current.map(function (task) {
        return task.id === taskId ? _objectSpread(_objectSpread({}, task), changes) : task;
      }).sort(function (a, b) {
        return a.time.localeCompare(b.time);
      });
    });
    closeOverlay();
    showToast("日常事务已更新");
  };
  var openMore = function openMore(route) {
    pushSecondary(route);
  };
  var openCritical = function openCritical(task) {
    var storedTask = criticalTasks.find(function (item) {
      return item.id === task.id;
    }) || task;
    var editableTask = _objectSpread(_objectSpread({}, storedTask), {}, {
      daysLeft: criticalDaysLeftOn(storedTask, todayDateKey, todayDateKey),
      deadline: editableCriticalDeadline(storedTask, todayDateKey)
    });
    setSelectedCritical(editableTask);
    setCriticalDraft(withCriticalReminderDefaults(editableTask));
    setRenewDays(7);
    setOverlay("critical-detail");
  };
  var saveCritical = function saveCritical(taskId, changes) {
    var _changes$deadline;
    var originalTask = criticalTasks.find(function (task) {
      return task.id === taskId;
    });
    if (!originalTask) return;
    var deadlineText = ((_changes$deadline = changes.deadline) === null || _changes$deadline === void 0 ? void 0 : _changes$deadline.trim()) || null;
    var parsed = deadlineText ? parseDeadline(deadlineText) : null;
    var deadlineTime = changes.deadlineTime || (changes.time && changes.time !== "待定" ? changes.time : null);
    var updatedTask = withCriticalReminderDefaults(_objectSpread(_objectSpread(_objectSpread({}, originalTask), changes), {}, {
      deadline: deadlineText,
      daysLeft: deadlineText ? parsed !== null && parsed !== void 0 && parsed.deadline ? parsed.daysLeft : Number.isFinite(changes.daysLeft) ? changes.daysLeft : criticalDaysLeftOn(originalTask, todayDateKey, todayDateKey) : null,
      anchorDateKey: deadlineText ? todayDateKey : null,
      deadlineTime: deadlineTime,
      time: deadlineTime,
      reminderEnabled: deadlineText ? changes.reminderEnabled : false
    }));
    if (updatedTask.done && updatedTask.completedDateKey) {
      updatedTask = moveCriticalCompletion(updatedTask, updatedTask.completedDateKey);
    }
    setCriticalTasks(function (current) {
      return current.map(function (task) {
        return task.id === taskId ? updatedTask : task;
      });
    });
    if (updatedTask.done && updatedTask.completedDateKey) {
      var historyEntry = criticalHistoryEntry(updatedTask, updatedTask.completedDateKey, updatedTask.anchorDateKey || todayDateKey);
      setHistory(function (current) {
        return [historyEntry].concat(_toConsumableArray(current.filter(function (item) {
          return item.sourceTaskId !== taskId;
        })));
      });
      if (updatedTask.completedDateKey !== originalTask.completedDateKey) {
        setSelectedDateKey(updatedTask.completedDateKey);
        setActiveTab("today");
      }
    }
    closeOverlay();
    showToast(updatedTask.done && updatedTask.completedDateKey !== originalTask.completedDateKey ? "完成日期已更新" : "关键事项已更新");
  };
  var renewCritical = function renewCritical(taskId) {
    var requestedDays = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 7;
    var days = Math.min(3650, Math.max(1, Number(requestedDays) || 7));
    setCriticalTasks(function (current) {
      return current.map(function (task) {
        if (task.id !== taskId) return task;
        var currentDaysLeft = criticalDaysLeftOn(task, todayDateKey, todayDateKey) || 0;
        var daysLeft = currentDaysLeft + days;
        return _objectSpread(_objectSpread({}, task), {}, {
          daysLeft: daysLeft,
          anchorDateKey: todayDateKey,
          deadline: deadlineLabelFromDays(daysLeft)
        });
      });
    });
    closeOverlay();
    showToast("DDL \u5DF2\u7EED\u671F ".concat(days, " \u5929"));
  };
  var startVoice = function startVoice() {
    var _window$JinkeAndroid9;
    voiceInputModeRef.current = "offline";
    setTranscript("");
    setVoiceDraft(null);
    setVoicePhase("listening");
    setSpeechStatus("starting");
    speechCandidatesRef.current = [];
    setOverlay("voice");
    if ((_window$JinkeAndroid9 = window.JinkeAndroid) !== null && _window$JinkeAndroid9 !== void 0 && _window$JinkeAndroid9.startSpeechRecognition) {
      window.JINKE_NATIVE_SPEECH_STATUS = function (status) {
        setSpeechStatus(String(status || "idle"));
        setSpeechAvailable(status !== "error" && status !== "permission-denied");
      };
      window.JINKE_NATIVE_SPEECH_PARTIAL = function (next) {
        setTranscript(String(next || ""));
        setSpeechAvailable(true);
      };
      window.JINKE_NATIVE_SPEECH_CANDIDATES = function (payload) {
        try {
          var candidates = typeof payload === "string" ? JSON.parse(payload) : payload;
          speechCandidatesRef.current = Array.isArray(candidates) ? candidates : [];
        } catch (_unused27) {
          speechCandidatesRef.current = [];
        }
      };
      window.JINKE_NATIVE_SPEECH_RESULT = function (next) {
        var _DOMAIN_NLU$rankCandi, _ranked$best;
        var ranked = DOMAIN_NLU === null || DOMAIN_NLU === void 0 || (_DOMAIN_NLU$rankCandi = DOMAIN_NLU.rankCandidates) === null || _DOMAIN_NLU$rankCandi === void 0 ? void 0 : _DOMAIN_NLU$rankCandi.call(DOMAIN_NLU, speechCandidatesRef.current, {
          dailyTasks: dailyTasks,
          criticalTasks: criticalTasks
        });
        setTranscript(String((ranked === null || ranked === void 0 || (_ranked$best = ranked.best) === null || _ranked$best === void 0 ? void 0 : _ranked$best.text) || next || ""));
        setSpeechStatus("idle");
        window.setTimeout(function () {
          return setVoicePhase("review");
        }, 80);
      };
      try {
        window.JinkeAndroid.startSpeechRecognition();
        setSpeechAvailable(true);
      } catch (_unused28) {
        setSpeechAvailable(false);
      }
      return;
    }
    var Recognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    if (!Recognition) {
      setSpeechAvailable(false);
      return;
    }
    try {
      var recognition = new Recognition();
      recognition.lang = "zh-CN";
      recognition.interimResults = true;
      recognition.continuous = false;
      recognition.onresult = function (event) {
        var next = Array.from(event.results).map(function (result) {
          return result[0].transcript;
        }).join("");
        setTranscript(next);
      };
      recognition.onerror = function () {
        return setSpeechAvailable(false);
      };
      recognitionRef.current = recognition;
      recognition.start();
      setSpeechAvailable(true);
    } catch (_unused29) {
      setSpeechAvailable(false);
    }
  };
  var stopVoice = function stopVoice() {
    var _window$JinkeAndroid10;
    if (voiceInputModeRef.current === "input-method") {
      setSpeechStatus("idle");
      window.setTimeout(function () {
        return setVoicePhase("review");
      }, 50);
      return;
    }
    if ((_window$JinkeAndroid10 = window.JinkeAndroid) !== null && _window$JinkeAndroid10 !== void 0 && _window$JinkeAndroid10.stopSpeechRecognition) {
      setSpeechStatus("processing");
      try {
        window.JinkeAndroid.stopSpeechRecognition();
      } catch (_unused30) {
        setSpeechAvailable(false);
      }
      return;
    }
    if (recognitionRef.current) {
      try {
        recognitionRef.current.stop();
      } catch (_unused31) {}
      recognitionRef.current = null;
    }
    window.setTimeout(function () {
      return setVoicePhase("review");
    }, 100);
  };
  var useInputMethodVoice = function useInputMethodVoice() {
    var _window$JinkeAndroid11, _window$JinkeAndroid12;
    if (voiceInputModeRef.current === "input-method") return;
    voiceInputModeRef.current = "input-method";
    if ((_window$JinkeAndroid11 = window.JinkeAndroid) !== null && _window$JinkeAndroid11 !== void 0 && _window$JinkeAndroid11.cancelSpeechRecognition) {
      try {
        window.JinkeAndroid.cancelSpeechRecognition();
      } catch (_unused32) {}
    } else if ((_window$JinkeAndroid12 = window.JinkeAndroid) !== null && _window$JinkeAndroid12 !== void 0 && _window$JinkeAndroid12.stopSpeechRecognition) {
      try {
        window.JinkeAndroid.stopSpeechRecognition();
      } catch (_unused33) {}
    }
    if (recognitionRef.current) {
      try {
        recognitionRef.current.abort();
      } catch (_unused34) {}
      recognitionRef.current = null;
    }
    setSpeechStatus("idle");
  };
  var confirmVoiceCommand = function confirmVoiceCommand(commandOverride) {
    var command = commandOverride || parsedVoiceCommand;
    if (!command.valid) return;
    var intent = command.intent,
      target = command.target;
    if (intent === "create") {
      var task = command.task;
      if (task.span) {
        var spanId = "voice-span-".concat(Date.now());
        var created = [withCriticalReminderDefaults({
          id: "".concat(spanId, "-start"),
          spanId: spanId,
          spanRole: "start",
          title: "".concat(task.title, " \xB7 \u51FA\u53D1"),
          note: task.note || "",
          deadline: task.span.start.deadline,
          daysLeft: task.span.start.daysLeft,
          anchorDateKey: todayDateKey,
          deadlineTime: task.hasTime && task.time !== "待定" ? task.time : null,
          alertMode: task.alertMode || "inherit",
          soundId: task.soundId || "inherit",
          progress: 0
        }), withCriticalReminderDefaults({
          id: "".concat(spanId, "-end"),
          spanId: spanId,
          spanRole: "end",
          title: "".concat(task.title, " \xB7 \u8FD4\u7A0B"),
          note: task.note || "",
          deadline: task.span.end.deadline,
          daysLeft: task.span.end.daysLeft,
          anchorDateKey: todayDateKey,
          deadlineTime: task.endTime || null,
          alertMode: task.alertMode || "inherit",
          soundId: task.soundId || "inherit",
          progress: 0
        })];
        setCriticalTasks(function (current) {
          return [].concat(created, _toConsumableArray(current));
        });
        setActiveTab("critical");
      } else if (task.type === "critical") {
        var _normalizedDeadline$d;
        var normalizedDeadline = task.deadline ? parseDeadline(task.deadline) : null;
        var _created = withCriticalReminderDefaults({
          id: "voice-critical-".concat(Date.now()),
          title: task.title,
          note: task.note,
          deadline: task.deadline,
          daysLeft: (_normalizedDeadline$d = normalizedDeadline === null || normalizedDeadline === void 0 ? void 0 : normalizedDeadline.daysLeft) !== null && _normalizedDeadline$d !== void 0 ? _normalizedDeadline$d : task.daysLeft,
          anchorDateKey: task.deadline ? todayDateKey : null,
          deadlineTime: task.deadlineTime || (task.hasTime && task.time !== "待定" ? task.time : null),
          reminderEnabled: Boolean(task.deadline && task.reminderEnabled !== false),
          reminderTime: task.reminderTime,
          reminderMode: task.reminderMode,
          reminderMultiple: task.reminderMultiple,
          reminderFinalDays: task.reminderFinalDays,
          progress: 0,
          alertMode: task.alertMode || "inherit",
          soundId: task.soundId || "inherit"
        });
        setCriticalTasks(function (current) {
          return [_created].concat(_toConsumableArray(current));
        });
        setActiveTab("critical");
      } else {
        var _created2 = {
          id: "voice-".concat(Date.now()),
          time: task.time,
          endTime: task.endTime || null,
          spansMidnight: Boolean(task.spansMidnight),
          title: task.title,
          note: task.note || "",
          repeat: task.repeat,
          repeatDays: repeatDaysFromValue(task.repeat, task.repeatDays),
          scheduledDateKey: repeatDaysFromValue(task.repeat, task.repeatDays).length ? null : task.scheduledDateKey && task.scheduledDateKey >= todayDateKey ? task.scheduledDateKey : todayDateKey,
          reminder: task.reminder || "到点提醒",
          alertMode: task.alertMode || "inherit",
          soundId: task.soundId || "inherit",
          activeFrom: todayDateKey,
          done: false
        };
        setDailyTasks(function (current) {
          return [].concat(_toConsumableArray(current), [_created2]).sort(function (a, b) {
            return a.time.localeCompare(b.time);
          });
        });
        setActiveTab("today");
      }
      setSecondary(null);
      showToast("\u5DF2\u521B\u5EFA\uFF1A".concat(task.title));
    } else if (intent === "clear-all") {
      if (command.scope !== "critical") {
        setDailyTasks([]);
        setDailyCompletionByDate({});
      }
      if (command.scope !== "daily") setCriticalTasks([]);
      setActiveTab(command.scope === "critical" ? "critical" : "today");
      setSecondary(null);
      showToast("已清除，历史记录已保留");
    } else if (intent === "complete-all") {
      setDailyCompletionByDate(function (current) {
        return dailyTasks.reduce(function (next, task) {
          return _objectSpread(_objectSpread({}, next), {}, _defineProperty({}, "".concat(task.id, ":").concat(selectedDateKey), true));
        }, _objectSpread({}, current));
      });
      setActiveTab("today");
      setSecondary(null);
      showToast("所选日期的日常事项已全部完成");
    } else if (intent === "theme") {
      setThemeMode(command.themeMode);
      showToast(command.heading);
    } else if (intent === "set-ddl-reminder-time") {
      changeDdlReminderTime(command.reminderTime);
      setSecondary("critical-reminders");
    } else if (intent === "set-ddl-reminder-policy") {
      if (command.policy === "multiple") changeDdlReminderMultiple(command.value);else changeDdlReminderFinalDays(command.value);
      setSecondary("critical-reminders");
    } else if (intent === "select-date") {
      selectDate(command.dateKey);
      setActiveTab("today");
      setSecondary(null);
      showToast(command.heading);
    } else if (intent === "query") {
      showToast("已汇总当前安排");
    } else if (intent === "delete") {
      if (target.kind === "daily") {
        closeOverlay(function () {
          return requestDelete(target.task, "daily");
        });
        return;
      }
      setCriticalTasks(function (current) {
        return current.filter(function (task) {
          return task.id !== target.task.id;
        });
      });
      showToast("\u5DF2\u5220\u9664\uFF1A".concat(target.task.title));
    } else if (intent === "complete") {
      if (target.kind === "daily") {
        setDailyCompletionByDate(function (current) {
          return _objectSpread(_objectSpread({}, current), {}, _defineProperty({}, "".concat(target.task.id, ":").concat(selectedDateKey), true));
        });
        setActiveTab("today");
        setSecondary(null);
        showToast("已勾选，并记入完成统计");
      } else {
        setCriticalCompleted(target.task.id, true);
        setActiveTab("today");
        setSelectedDateKey(localDateKey());
        setSecondary(null);
      }
    } else if (intent === "uncomplete") {
      if (target.kind === "critical") setCriticalCompleted(target.task.id, false);else setDailyCompletionByDate(function (current) {
        return _objectSpread(_objectSpread({}, current), {}, _defineProperty({}, "".concat(target.task.id, ":").concat(selectedDateKey), false));
      });
      setActiveTab("today");
      setSecondary(null);
      if (target.kind !== "critical") showToast("已取消勾选");
    } else if (intent === "extend") {
      setCriticalTasks(function (current) {
        return current.map(function (task) {
          if (task.id !== target.task.id) return task;
          var currentDaysLeft = criticalDaysLeftOn(task, todayDateKey, todayDateKey) || 0;
          var daysLeft = currentDaysLeft + command.days;
          return _objectSpread(_objectSpread({}, task), {}, {
            daysLeft: daysLeft,
            anchorDateKey: todayDateKey,
            deadline: deadlineLabelFromDays(daysLeft)
          });
        });
      });
      setActiveTab("critical");
      setSecondary(null);
      showToast("\u5DF2\u5EF6\u671F ".concat(command.days, " \u5929"));
    } else if (intent === "set-deadline") {
      setCriticalTasks(function (current) {
        return current.map(function (task) {
          return task.id === target.task.id ? _objectSpread(_objectSpread({}, task), {}, {
            deadline: command.deadline.deadline,
            daysLeft: command.deadline.daysLeft,
            anchorDateKey: todayDateKey,
            deadlineTime: command.eventTime || task.deadlineTime || task.time || null,
            time: command.eventTime || task.deadlineTime || task.time || null,
            reminderEnabled: task.deadline ? task.reminderEnabled : true
          }) : task;
        });
      });
      setActiveTab("critical");
      setSecondary(null);
      showToast("\u671F\u9650\u5DF2\u8BBE\u4E3A ".concat(command.deadline.deadline));
    } else if (intent === "edit") {
      var _command$changes = command.changes,
        nextType = _command$changes.type,
        changes = _objectWithoutProperties(_command$changes, _excluded2);
      if (target.kind === "daily" && nextType === "critical") {
        setDailyTasks(function (current) {
          return current.filter(function (task) {
            return task.id !== target.task.id;
          });
        });
        setCriticalTasks(function (current) {
          var _changes$daysLeft;
          return [withCriticalReminderDefaults({
            id: target.task.id,
            title: changes.title || target.task.title,
            note: changes.note || target.task.note,
            deadline: changes.deadline || null,
            daysLeft: (_changes$daysLeft = changes.daysLeft) !== null && _changes$daysLeft !== void 0 ? _changes$daysLeft : null,
            anchorDateKey: changes.deadline ? todayDateKey : null,
            deadlineTime: changes.deadlineTime || (changes.time && changes.time !== "待定" ? changes.time : null),
            progress: 0
          })].concat(_toConsumableArray(current));
        });
        setActiveTab("critical");
      } else if (target.kind === "critical" && nextType === "daily") {
        setCriticalTasks(function (current) {
          return current.filter(function (task) {
            return task.id !== target.task.id;
          });
        });
        setDailyTasks(function (current) {
          return [].concat(_toConsumableArray(current), [{
            id: target.task.id,
            time: changes.time || target.task.time || "待定",
            title: changes.title || target.task.title,
            note: changes.note || target.task.note,
            repeat: changes.repeat || "仅一次",
            repeatDays: repeatDaysFromValue(changes.repeat || "仅一次", changes.repeatDays),
            activeFrom: todayDateKey,
            scheduledDateKey: repeatDaysFromValue(changes.repeat || "仅一次", changes.repeatDays).length ? null : todayDateKey,
            reminder: changes.reminder || "到点提醒",
            done: false
          }]).sort(function (a, b) {
            return a.time.localeCompare(b.time);
          });
        });
        setActiveTab("today");
      } else {
        var applyChanges = function applyChanges(task) {
          return task.id === target.task.id ? _objectSpread(_objectSpread(_objectSpread({}, task), changes), target.kind === "critical" && Object.prototype.hasOwnProperty.call(changes, "daysLeft") ? {
            anchorDateKey: todayDateKey
          } : {}) : task;
        };
        if (target.kind === "daily") setDailyTasks(function (current) {
          return current.map(applyChanges);
        });else setCriticalTasks(function (current) {
          return current.map(applyChanges);
        });
        setActiveTab(target.kind === "daily" ? "today" : "critical");
      }
      setSecondary(null);
      showToast("\u5DF2\u4FEE\u6539\uFF1A".concat(target.task.title));
    } else if (intent === "navigate") {
      var route = command.route;
      if (["history", "month", "year", "permissions", "settings", "critical-reminders", "version", "voice"].includes(route)) {
        setSecondary(route);
      } else {
        setSecondary(null);
        if (route === "critical") {
          setActiveTab("critical");
          setViewMode("day");
        } else {
          setActiveTab("today");
          setViewMode(route === "month-view" ? "month" : "day");
          if (route !== "month-view") setSelectedDateKey(todayDateKey);
        }
      }
      showToast(command.heading);
    }
    closeOverlay();
  };
  var closeOverlay = function closeOverlay(afterClose) {
    var _window$JinkeAndroid13, _window$JinkeAndroid14;
    if (overlayClosingRef.current) return;
    if ((_window$JinkeAndroid13 = window.JinkeAndroid) !== null && _window$JinkeAndroid13 !== void 0 && _window$JinkeAndroid13.cancelSpeechRecognition) {
      try {
        window.JinkeAndroid.cancelSpeechRecognition();
      } catch (_unused35) {}
    } else if ((_window$JinkeAndroid14 = window.JinkeAndroid) !== null && _window$JinkeAndroid14 !== void 0 && _window$JinkeAndroid14.stopSpeechRecognition) {
      try {
        window.JinkeAndroid.stopSpeechRecognition();
      } catch (_unused36) {}
    }
    if (recognitionRef.current) {
      try {
        recognitionRef.current.abort();
      } catch (_unused37) {}
      recognitionRef.current = null;
    }
    var finish = function finish() {
      if (!overlayClosingRef.current && overlay) return;
      overlayClosingRef.current = false;
      if (overlayCloseTimerRef.current) window.clearTimeout(overlayCloseTimerRef.current);
      overlayCloseTimerRef.current = null;
      setOverlay(null);
      setSelectedDaily(null);
      setDailyDraft(null);
      setSelectedCritical(null);
      setCriticalDraft(null);
      setRenewDays(7);
      setTranscript("");
      setVoicePhase("listening");
      setSpeechStatus("idle");
      setVoiceDraft(null);
      setDeleteTarget(null);
      afterClose === null || afterClose === void 0 || afterClose();
    };
    if (!overlay) {
      finish();
      return;
    }
    overlayClosingRef.current = true;
    var detail = {
      depth: 0,
      completed: false,
      complete: finish
    };
    window.dispatchEvent(new CustomEvent("jinke-sheet-dismiss", {
      detail: detail
    }));
    overlayCloseTimerRef.current = window.setTimeout(function () {
      if (detail.completed) return;
      detail.completed = true;
      finish();
    }, 290);
  };
  var closeSecondary = function closeSecondary() {
    if (!secondaryStack.length || secondaryClosingRef.current) return;
    var depth = secondaryStack.length;
    var finish = function finish() {
      if (!secondaryClosingRef.current) return;
      secondaryClosingRef.current = false;
      if (secondaryCloseTimerRef.current) window.clearTimeout(secondaryCloseTimerRef.current);
      secondaryCloseTimerRef.current = null;
      setSecondaryStack(function (current) {
        return current.slice(0, -1);
      });
    };
    secondaryClosingRef.current = true;
    var detail = {
      depth: depth,
      completed: false,
      complete: finish
    };
    window.dispatchEvent(new CustomEvent("jinke-sheet-dismiss", {
      detail: detail
    }));
    secondaryCloseTimerRef.current = window.setTimeout(function () {
      if (detail.completed) return;
      detail.completed = true;
      finish();
    }, 290);
  };
  useEffect(function () {
    var handleNativeBack = function handleNativeBack() {
      if (secondary) {
        closeSecondary();
        return true;
      }
      if (overlay) {
        closeOverlay();
        return true;
      }
      if (viewMode === "month") {
        setViewMode("day");
        setSelectedDateKey(todayDateKey);
        return true;
      }
      if (activeTab === "critical") {
        setActiveTab("today");
        return true;
      }
      return false;
    };
    window.JINKE_NATIVE_BACK = handleNativeBack;
    return function () {
      if (window.JINKE_NATIVE_BACK === handleNativeBack) delete window.JINKE_NATIVE_BACK;
    };
  }, [overlay, secondary, secondaryStack.length, viewMode, activeTab, todayDateKey]);
  useEffect(function () {
    var openTodayFromNotification = function openTodayFromNotification() {
      setOverlay(null);
      setSecondaryStack([]);
      setActiveTab("today");
      setViewMode("day");
      var currentDateKey = localDateKey();
      setTodayDateKey(currentDateKey);
      setSelectedDateKey(currentDateKey);
    };
    window.JINKE_OPEN_TODAY = openTodayFromNotification;
    return function () {
      if (window.JINKE_OPEN_TODAY === openTodayFromNotification) delete window.JINKE_OPEN_TODAY;
    };
  }, []);
  var renderScreen = function renderScreen() {
    if (activeTab === "critical") return React.createElement(CriticalScreen, {
      tasks: activeCriticalTasks,
      onToggle: toggleCriticalCheck,
      onOpen: openCritical,
      onDelete: function onDelete(task) {
        return requestDelete(task, "critical");
      },
      onMenu: function onMenu() {
        setSecondaryStack([]);
        setOverlay("more");
      },
      onOpenReminders: function onOpenReminders() {
        return setSecondary("critical-reminders");
      }
    });
    return React.createElement(TodayScreen, {
      tasks: displayedDailyTasks,
      deadlineTasks: displayedDeadlineTasks,
      onToggle: toggleDaily,
      onEdit: openDaily,
      onDeleteDaily: function onDeleteDaily(task) {
        return requestDelete(task, "daily");
      },
      onToggleCritical: toggleCriticalCheck,
      onOpenCritical: openCritical,
      onDeleteCritical: function onDeleteCritical(task) {
        return requestDelete(task, "critical");
      },
      onMenu: function onMenu() {
        setSecondaryStack([]);
        setOverlay("more");
      },
      viewMode: viewMode,
      onOpenView: function onOpenView() {
        return setOverlay("view");
      },
      selectedDateKey: selectedDateKey,
      todayDateKey: todayDateKey,
      onSelectDate: selectDate,
      getDateLoad: getDateTaskLoad,
      onOpenDayArchive: function onOpenDayArchive() {
        setArchiveActive("china");
        setArchiveIndex(0);
        setOverlay("day-archive");
      }
    });
  };
  var renderSecondaryScreen = function renderSecondaryScreen(route) {
    if (route === "history") return React.createElement(HistoryScreen, {
      items: history,
      onBack: closeSecondary
    });
    if (route === "month") return React.createElement(ReportScreen, {
      type: "month",
      dailyTasks: dailyTasks,
      dailyCompletionByDate: dailyCompletionByDate,
      history: history,
      todayDateKey: todayDateKey,
      onBack: closeSecondary
    });
    if (route === "year") return React.createElement(ReportScreen, {
      type: "year",
      dailyTasks: dailyTasks,
      dailyCompletionByDate: dailyCompletionByDate,
      history: history,
      todayDateKey: todayDateKey,
      onBack: closeSecondary
    });
    if (route === "permissions") return React.createElement(PermissionsScreen, {
      capabilities: nativeCapabilities,
      onOpenCapability: function onOpenCapability(key) {
        try {
          var _window$JinkeAndroid15, _window$JinkeAndroid16;
          (_window$JinkeAndroid15 = window.JinkeAndroid) === null || _window$JinkeAndroid15 === void 0 || (_window$JinkeAndroid16 = _window$JinkeAndroid15.openCapabilitySettings) === null || _window$JinkeAndroid16 === void 0 || _window$JinkeAndroid16.call(_window$JinkeAndroid15, key);
        } catch (_unused38) {}
      },
      onBack: closeSecondary
    });
    if (route === "settings") return React.createElement(SettingsScreen, {
      alertMode: defaultAlertMode,
      defaultSoundId: defaultSoundId,
      sounds: reminderSounds,
      onAlertModeChange: function onAlertModeChange(mode) {
        setDefaultAlertMode(mode);
        showToast(mode === "silent" ? "默认改为静音提醒" : "默认改为响铃提醒");
      },
      onDefaultSoundChange: function onDefaultSoundChange(soundId) {
        setDefaultSoundId(soundId);
        showToast("默认音效已更新");
      },
      onPreviewSound: previewReminderSound,
      onImportSound: importReminderSound,
      onPickSystemAlarm: pickSystemAlarmSound,
      onOpenPermissions: function onOpenPermissions() {
        return pushSecondary("permissions");
      },
      onBack: closeSecondary
    });
    if (route === "critical-reminders") return React.createElement(CriticalReminderScreen, {
      tasks: activeCriticalTasks,
      reminderTime: ddlReminderTime,
      onReminderTimeChange: changeDdlReminderTime,
      reminderMultiple: ddlReminderMultiple,
      onReminderMultipleChange: changeDdlReminderMultiple,
      reminderFinalDays: ddlReminderFinalDays,
      onReminderFinalDaysChange: changeDdlReminderFinalDays,
      onOpenPermissions: function onOpenPermissions() {
        return pushSecondary("permissions");
      },
      onBack: closeSecondary
    });
    if (route === "version") return React.createElement(VersionScreen, {
      onBack: closeSecondary
    });
    if (route === "voice") return React.createElement(VoiceSettingsScreen, {
      capabilities: nativeCapabilities,
      onBack: closeSecondary
    });
    return null;
  };
  var renderDevice = function renderDevice(variant) {
    return React.createElement(PhoneFrame, {
      variant: variant
    }, renderScreen(), React.createElement(BottomNav, {
      activeTab: activeTab,
      onTabChange: function onTabChange(tab) {
        setActiveTab(tab);
        setViewMode("day");
        if (tab === "today") setSelectedDateKey(todayDateKey);
        setSecondaryStack([]);
      },
      onVoice: startVoice
    }), overlay === "view" ? React.createElement(ViewMenu, {
      onClose: closeOverlay,
      onSelect: function onSelect(mode) {
        setViewMode(mode);
        if (mode === "day") setSelectedDateKey(todayDateKey);
        closeOverlay();
      }
    }) : null, overlay === "more" ? React.createElement(MoreSheet, {
      onClose: closeOverlay,
      onOpen: openMore,
      themeMode: themeMode,
      onThemeChange: setThemeMode
    }) : null, secondaryStack.map(function (route, index) {
      return React.createElement(Sheet, {
        depth: index + 1,
        onClose: closeSecondary,
        label: "".concat(route, " \u9762\u677F"),
        key: "".concat(index, "-").concat(route)
      }, renderSecondaryScreen(route));
    }), overlay === "voice" ? React.createElement(VoiceComposer, {
      phase: voicePhase,
      transcript: transcript,
      parsedCommand: parsedVoiceCommand,
      draftTask: voiceDraft,
      onDraftTaskChange: setVoiceDraft,
      onTranscript: setTranscript,
      onStop: stopVoice,
      onUseInputMethod: useInputMethodVoice,
      onConfirm: confirmVoiceCommand,
      onClose: closeOverlay,
      speechAvailable: speechAvailable,
      speechStatus: speechStatus,
      sounds: reminderSounds,
      defaultAlertMode: defaultAlertMode,
      defaultSoundId: defaultSoundId,
      onPreviewSound: previewReminderSound,
      onImportSound: importReminderSound,
      onPickSystemAlarm: pickSystemAlarmSound
    }) : null, overlay === "daily-edit" ? React.createElement(DailyEditSheet, {
      task: selectedDaily,
      draft: dailyDraft,
      onDraftChange: setDailyDraft,
      onSave: saveDaily,
      onClose: closeOverlay,
      sounds: reminderSounds,
      defaultAlertMode: defaultAlertMode,
      defaultSoundId: defaultSoundId,
      onPreviewSound: previewReminderSound,
      onImportSound: importReminderSound,
      onPickSystemAlarm: pickSystemAlarmSound
    }) : null, overlay === "critical-detail" ? React.createElement(CriticalDetailSheet, {
      task: selectedCritical,
      draft: criticalDraft,
      renewDays: renewDays,
      onRenewDaysChange: setRenewDays,
      onDraftChange: setCriticalDraft,
      onClose: closeOverlay,
      onRenew: renewCritical,
      onSave: saveCritical,
      sounds: reminderSounds,
      defaultAlertMode: defaultAlertMode,
      defaultSoundId: defaultSoundId,
      onPreviewSound: previewReminderSound,
      onImportSound: importReminderSound,
      onPickSystemAlarm: pickSystemAlarmSound
    }) : null, overlay === "day-archive" ? React.createElement(CalendarDaySheet, {
      dateKey: selectedDateKey,
      active: archiveActive,
      index: archiveIndex,
      onActiveChange: setArchiveActive,
      onIndexChange: setArchiveIndex,
      onClose: closeOverlay
    }) : null, overlay === "delete-confirm" ? React.createElement(DeleteConfirmSheet, {
      target: deleteTarget,
      selectedDateKey: selectedDateKey,
      onClose: closeOverlay,
      onConfirm: confirmDelete
    }) : null, toast ? React.createElement("div", {
      className: "sr-only",
      role: "status",
      "aria-live": "polite"
    }, toast) : null);
  };
  if (nativeWindow) {
    var nativeVariant = nativeWindow.expanded ? "expanded" : "phone";
    return React.createElement("div", {
      className: "native-app ".concat(nativeWindow.expanded ? "native-expanded" : "native-phone"),
      "data-window-ratio": nativeWindow.ratio.toFixed(3)
    }, renderDevice(nativeVariant));
  }
  return React.createElement("div", {
    className: "viewer-root"
  }, React.createElement("div", {
    className: "viewer-stage"
  }, React.createElement("div", {
    className: "viewer-simulator-wrap",
    style: {
      transform: "scale(".concat(scale, ")")
    }
  }, React.createElement("div", {
    className: "simulator-pair",
    "aria-label": "\u540C\u6B65\u8BBE\u5907\u9884\u89C8"
  }, React.createElement("section", {
    className: "device-preview device-preview-phone",
    "aria-label": "\u666E\u901A\u624B\u673A\u9884\u89C8"
  }, React.createElement("div", {
    className: "device-preview-label"
  }, "\u666E\u901A\u624B\u673A \xB7 430 \xD7 956"), React.createElement("div", {
    className: "viewer-device-wrap viewer-device-phone"
  }, renderDevice("phone"))), React.createElement("section", {
    className: "device-preview device-preview-expanded",
    "aria-label": "\u5C55\u5F00\u5168\u9762\u5C4F\u9884\u89C8"
  }, React.createElement("div", {
    className: "device-preview-label"
  }, "\u5C55\u5F00\u5168\u9762\u5C4F \xB7 860 \xD7 956"), React.createElement("div", {
    className: "viewer-device-wrap viewer-device-expanded"
  }, renderDevice("expanded")))))));
}
var root = ReactDOM.createRoot(document.getElementById("root"));
root.render(React.createElement(MobileDesignApp, null));
})();
