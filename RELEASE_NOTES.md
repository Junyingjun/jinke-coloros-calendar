# 今刻 1.0.17

- 增加仅面向今刻软件功能的领域语音模型：统一识别创建、编辑、删除、完成、续期、查询和页面操作意图，不扩展为泛聊天助手。
- 时间语义改为按同一语句片段双向绑定，支持“十一点晚上洗漱”“晚上洗漱十一点”等不同语序，并从任务名称中清理时间状语。
- 离线 Vosk 识别启用 5 条候选结果，由今刻结合声学置信度、任务字段和现有任务做领域重排，提升 DDL、同音字及任务操作识别稳定性。
- 当非空日常列表全部勾选后，待办状态改为红色“全部完成”。
- 展开内屏的左右分隔线固定于折痕中心，使用居中的 61.8% 高度线段，不再随任一侧任务数量伸缩。

# 今刻 1.0.16

- 移除周历与月历中按日期随机生成的演示点，日期标记现在只由真实日常任务和实际 DDL 截止日计算。
- 删除任务后，周历小点与月历红点会随任务状态同一轮刷新；清空所有任务后不再残留任何任务标记。
- 在月视图选择其他日期后，通过返回手势或切回日视图都会恢复到设备今天，不再停留在旧日期所在周。
- 再次点击底部“今日”也会同时回到系统当天，确保任何日期检视状态都有明确返回入口。

# 今刻 1.0.15

- 菜单、二级与三级功能页统一改为可拖拽底部滑块；进入子级时保留底层面板，返回按层级逐层退出，不再先收回菜单再跳页。
- 顶部横杠支持向下关闭，也支持带阻尼的向上弹性拉动，松手后以克制的弹簧感回位。
- 删除日常任务时可选择“仅删除后续安排”或“删除全部记录”：前者保留删除日前的完成记录和复盘统计，后者清除该任务的全部历史。
- 新建日常任务从创建当天开始生效，不再补写到此前日期；停止后的历史任务仍可在旧日期中再次执行“删除全部记录”。
- 月度和年度复盘改为按任务实际生效区间、停止日期与逐日完成记录实时计算。

# 今刻 1.0.14

- 菜单、任务编辑和语音等底部面板统一改为从底部滑入/滑出，顶部横杠支持直接下拖关闭。
- 从“更多”进入历史、复盘、权限、语音模型、版本更新或设置后，返回会回到“更多”。
- 日常事项与关键事项共用新的左滑操作样式：任务、编辑、删除均使用独立圆弧，编辑与删除之间保留透明间隙。
- 修复保存、删除、完成和续期后的面板瞬间消失，统一使用克制的退场动画。

# 今刻 1.0.13

- 将关键任务的“每天”提醒改为“仅最后 X 天”，X 的上限按设备今天到截止日期的天数实时计算，并支持首尾循环选择。
- 设置截止时刻后，“仅截止日”改为配置截止时刻前的小时与分钟，提醒时刻与截止时刻不再互相覆盖。
- 截止月份和日期支持循环，日期会按当前年月自动限制在合法范围内。
- 修复任务左滑操作区覆盖不完整，并支持点击其他位置或滚动时自动收回。
- 重排续期控件，修复窄屏下天数、单位与确认按钮拥挤越界。

# 今刻 1.0.12

- 重写关键任务提醒：截止日期、截止时刻、提醒时刻和提醒频率互相独立。
- 支持“智能节点 / 每天 / 仅截止日”三种任务级提醒计划；当天无符合节点任务时不发送通知。
- 强化中英文 DDL / deadline 语音路由，并兼容离线中文识别常见的“滴滴艾尔”等误转写。
- 修复关键任务日期编辑窄屏溢出，提醒设置改为自定义选项 UI。
- 更新安装成功后在下次启动自动清理已下载 APK。

# 今刻 1.0.11

- 修复首页日期被演示数据固定在 8 月 24 日的问题，启动时直接读取设备系统日期
- 增加每 30 秒、回到前台、页面恢复、系统改时间/时区及跨零点后的日期刷新
- DDL 剩余天数以系统日期锚点实时计算，并在日期变化后同步 Android 提醒调度
- 删除重复的“输入法”按钮，点击唯一输入框即可调用当前输入法
- 修复多次点击输入框后“停止并处理”无响应的问题，输入法模式不再等待已取消的离线识别回调

# 今刻 1.0.10

- 语音弹层将“使用示例”改为“取消”，取消会释放麦克风并彻底丢弃本轮输入
- 支持“十一点半”以及识别成“十一点办 / 伴 / 班 / 版 / 般”的半小时表达，时间词不再进入任务名称
- 增加“输入法”入口：先释放离线识别麦克风，再唤起当前豆包或微信输入法，允许使用输入法自带的高精度语音
- 正确理解“无 deadline / 无 DDL”是无截止日期的关键事务，例如生成“修桑顿皮卡德相机”

## 1.0.9

- 离线语音会累积停顿前后的多段内容，不再用后一句覆盖前一句；识别末尾发生音频异常时会保留已有文字
- 监听窗口延长到 30 秒，支持先说主体、再补充时长或返程信息；空识别结果不再偷换成示例指令
- 支持“晚上十二点 / 12 点睡觉 / 12 电睡觉”等模糊说法，午夜可明确保留为所选日期的 24:00，零点保持 00:00
- 小时在 0—24 间循环，分钟按 5 分钟在 0—55 间循环；日常事项支持开始—结束时间段
- 去程与返程或“出发后停留 N 天”会生成一对关联 DDL，并在确认页同时展示头尾日期
- 新建任务备注默认留空，升级时也会清理旧数据中的“语音创建”前缀

## 1.0.8

- 关闭 Android WebView 的系统蓝色点击矩形，为按钮、任务、日期、导航与设置项保留今刻自己的克制按压反馈
- 所有时钟和提前提醒统一为“小时每次 1 小时、分钟每次 5 分钟”，语音中的非整 5 分钟也会自动规范
- 修复普通手机 DDL 提醒页的时间控件与频率卡片横向越界；展开内屏仍使用适合双栏的布局
- ColorOS 后台运行入口会优先直达自启动或后台活动设置，并在返回后不再错误显示为未配置
- 增加针对触摸高亮、五分钟步进、提醒页比例约束和 ColorOS 设置跳转的自动化审计

## 1.0.7

- 重复设置改为周一至周日七日独立勾选，并自动归纳“每天 / 工作日 / 周末 / 每周某几天 / 仅一次”
- 日常提醒改为今刻自定义小时、分钟步进控件，严格限制在提前 24 小时以内
- 类型、时间、重复、提醒、截止日期、完成度、续期和 DDL 提醒策略全部使用今刻自定义控件，不再弹出 Android 原生下拉框或日期选择器
- 单击日常和关键任务只会勾选或取消勾选；编辑与删除只在向左滑动后出现
- 修复滑动容器过早抢占触摸事件、导致真实点按无法勾选的问题
- 日常任务按所选日期与星期规则过滤，工作日任务不会再出现在周六、周日
- 语音创建同步保存七日重复规则；超出 24 小时的日常提醒会自动限制为 23 小时 59 分钟

## 1.0.6

- 修复“周一到周五点外卖”被误判为五点任务：星期范围会先识别为工作日，再解析时间与任务名称
- 兼容 Vosk 逐字带空格的中文转写，“周 一 到 周 五”仍可正确识别
- 重复、提醒、截止日期、完成度、续期天数和 DDL 提醒策略全部改为系统选择器或预设选项；仅名称、备注与助手命令保留文字输入

- APK 内置 Vosk Android 0.3.75、ARM64 原生识别库和 `vosk-model-small-cn-0.22` 中文离线模型
- 语音识别不再要求手机预装系统语音服务，也不需要首次联网下载模型
- 首次点击语音键申请麦克风权限；模型准备、监听、处理和失败状态均在界面实时显示
- 修正“语音模型”页面此前虚报 sherpa-onnx、Zipformer 和已下载状态的问题，改为显示实际打包组件
- “权限与组件”页面实时读取麦克风、通知、精确闹钟、电池限制、安装更新和联网状态
- 未授权项目可以直接进入对应的 ColorOS / Android 系统设置，不再硬编码“已开启”或“已加入白名单”
- 增加构建期 APK 审计：缺少权限、ARM64 原生库、中文模型或第三方声明时直接构建失败
- 保留此前的比例周历、折叠屏布局、左滑编辑删除、DDL 提醒与返回手势修复
