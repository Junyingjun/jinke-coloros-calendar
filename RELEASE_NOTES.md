# 今刻 1.0.2

- 原生界面按窗口比例全屏适配，Find N3 外屏单列、展开内屏沿折痕等分双列
- 折叠或展开时实时切换布局，不再依赖固定像素断点或重启应用
- 每个日常与关键事项均支持左滑显示“编辑 / 删除”
- ColorOS 边缘返回先退出弹层、二级页、月视图与关键页，根页再次返回才退出应用
- 清除旧版内置假任务、假历史与假复盘；正式版仅保留一个日常和一个 DDL 演示
- 升级时只移除已知内置数据，保留用户自行创建的任务和历史
- 演示任务引导用户左滑删除并创建自己的第一项日程与 DDL
- 延续 1.0.1 的预编译 WebView 启动方案，避免本地文件和 APK 黑屏
